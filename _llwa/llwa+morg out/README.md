# llwa+morg out

<!-- meta
пара: LLWA × Morgenröte
статус: out
версии: —
позиция: —
файлов: 8
генератор: —
зависит от: —
-->

Чужой компач автора LLWA (`xyz.1230james.LLWA_morgenrote_compatch`, версия 1.0.8), распакован как источник.

Голыми полными телами переопределяет `building_railway` и `building_airport`, вручную сводя вклад LLWA и
Morgenröte. E&F в его `metadata.json` не заявлен — обе группы E&F на этих зданиях выпадают; чинится в
`_llwa/llwa+morg+ef done` (задача LLWA.7).
