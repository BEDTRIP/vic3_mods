# LLWA x VC compatch -- restore VC's job-attractiveness modifier on three rail production methods
# Generated by tools/regen_llwa_vc.py on 2026-08-26. Do not hand-edit: the next
# run overwrites this file. Change the generator instead.
#
# Load order: VC -> ... -> LLWA -> THIS.
# Why this file exists: LLWA's REPLACE: on these three records (loading last in the target chain) is a bare full body -- on pm_no_passenger_trains there is no building_modifiers block at all -- so VC's `INJECT:building_modifiers = { unscaled = { building_job_attractiveness_mult = 2 } }` is silently dropped. LLWA's own numbers (where it has any) are NOT touched. These three records are ALSO shared with TGR (see _llwa/llwa+tgr done) -- TGR's own numeric rebalance on them is not restored anywhere (LLWA loads last, LLWA's numbers win, same rule as the TGR compatch), so there is nothing of TGR's left to add here and this key isn't written twice across the two compaches.

REPLACE_OR_CREATE:pm_no_passenger_trains = {
	texture = "gfx/interface/icons/production_method_icons/no_passenger_trains.dds"
	building_modifiers = {
		unscaled = {
			building_job_attractiveness_mult = 2
		}
	}
}

REPLACE_OR_CREATE:pm_steel_passenger_carriages = {
	texture = "gfx/interface/icons/production_method_icons/passenger_carriages.dds"

	unlocking_production_methods = {
        pm_steam_trains
        pm_steam_trains_principle_transport_3
        pm_electric_trains
        pm_electric_trains_principle_transport_3
        pm_diesel_trains
        pm_diesel_trains_principle_transport_3
    }
    
    unlocking_technologies = {
		steel_railway_cars
	}

	building_modifiers = {
		workforce_scaled = {
			goods_input_steel_add = 12
			goods_output_transportation_add = 30
		}
		level_scaled = {
			building_employment_clerks_add = 200
		}
		unscaled = {
			building_job_attractiveness_mult = 2
		}
	}			
}

REPLACE_OR_CREATE:pm_wooden_passenger_carriages = {
	texture = "gfx/interface/icons/production_method_icons/passenger_trains.dds"

	building_modifiers = {
		workforce_scaled = {
			goods_input_wood_add = 10
			goods_output_transportation_add = 10
		}
		level_scaled = {
			building_employment_clerks_add = 100
		}
		unscaled = {
			building_job_attractiveness_mult = 2
		}
	}			
}
