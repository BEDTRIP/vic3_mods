# LLWA × TGR — conflicts report

Machine matrix (`tools/pair_matrix.py --pair "LLWA,TGR"`, 2026-08-26): **11 shared keys, 0 shared paths.**

```
common/ai_strategies: 1
    ai_strategy_default
common/production_methods: 10
    pm_diesel_trains
    pm_diesel_trains_principle_transport_3
    pm_early_trains
    pm_electric_trains
    pm_electric_trains_principle_transport_3
    pm_no_passenger_trains
    pm_steam_trains
    pm_steam_trains_principle_transport_3
    pm_steel_passenger_carriages
    pm_wooden_passenger_carriages
```

## `ai_strategy_default` — not a conflict

LLWA's only touch is `INJECT:ai_strategy_default = { subsidies = {...} }` in `LLWA_default_strategy.txt` — additive, into a sub-block (`subsidies`) TGR never mentions. Confirmed `ai_strategy_default` is not defined anywhere in LLWA's stale `03_political_strategies.txt` (see LLWA × KAI report) — this key's only exposure in LLWA is the clean INJECT file. Survives regardless of load order. Same reasoning as the already-closed LLWA × HC+GoB+MoH `noneed`.

## The 10 production_methods keys

All ten are full records in `LLWA_rails.txt` (`REPLACE:`) vs. `TGR_TRADE_private_infrastructure_railway.txt` (`REPLACE_OR_CREATE:`), vanilla `11_private_infrastructure.txt` as common ancestor. Read all three bodies (`vfs_order.py`, then direct `cat`), not just the key names.

* **TGR's own numeric rebalance is not a loss.** Both TGR and LLWA independently rebalanced `building_modifiers`/`state_modifiers` numbers (goods in/out, employment, infrastructure). LLWA loads last in the target chain (…TGR → … → LLWA), so LLWA's numbers are what actually ends up in the loaded game with or without this patch. Restoring TGR's numbers on top would be an editorial overrule, not a fix.
* **TGR's `unscaled = { state_market_access_price_impact = 0.05 }` addition is a real loss** on 7 of the 10: LLWA's body doesn't declare an `unscaled` sub-block under either `building_modifiers` or `state_modifiers` on any of the ten records — it's a pure absence, not a competing value. TGR adds it on `pm_early_trains`, `pm_steam_trains`(+ptc3), `pm_electric_trains`(+ptc3), `pm_diesel_trains`(+ptc3) — 7 records. Six put it under `state_modifiers`; `pm_diesel_trains_principle_transport_3` puts it under `building_modifiers` in TGR's own file (checked directly, not assumed — every sibling record uses `state_modifiers`, so this one is very likely a slip in TGR's own file, but per "не чинить чужой замысел" it's reproduced exactly as TGR wrote it).
* **The other 3 — `pm_no_passenger_trains`, `pm_steel_passenger_carriages`, `pm_wooden_passenger_carriages`** — TGR's numbers differ from LLWA's (again not restored, same reasoning) but TGR adds nothing structurally new beyond that; **VC** is the one adding something these three lose (`building_modifiers.unscaled.building_job_attractiveness_mult = 2`, via `INJECT:`) — see `_llwa/llwa+vc done`. Nothing to write against TGR specifically for these three, which is why they aren't in this compach.
* **LLWA's own addition, `pm_steel_passenger_carriages.unlocking_production_methods`** (a list neither vanilla nor TGR has) is untouched either way — LLWA's body is the base of the merge, so it's carried forward automatically.

## Compach

`common/production_methods/zz_llwa_tgr_rails.txt`, generated by `tools/regen_llwa_tgr.py`. Seven full `REPLACE_OR_CREATE:` records (LLWA's body + TGR's `unscaled` sub-block). Self-check: 7 top-level keys, 0 problems, `--check` idempotent.
