# аддон-LLWA

Собран и закрыт 26.08.2026 (LLWA.1-8). Перенесено из `План проекта.md` 26.08.2026.
Полный разбор нового контента — `_llwa/analysis_llwa_new_content_report.md`,
отчёт сборки — `__addon/addon llwa/README.md`.

## аддон-LLWA

После удаления T&R от аддона осталось три пары вместо пяти, и он стал маленьким.

- [x] **LLWA.1 × TGR — 11 ключей, десять железнодорожных методов + `ai_strategy_default`. Закрыто 26.08.2026.** Компач — `_llwa/llwa+tgr done`, разбор — `conflicts_llwa_vs_tgr_report.md` там же, генератор — `tools/regen_llwa_tgr.py`. **1 файл, 7 записей, самопроверка `0 problem(s)`, `--check` сходится.**
  * **TGR-овский ребаланс чисел НЕ восстанавливается.** Оба мода независимо перебалансировали одни и те же поля (`state_infrastructure_add`, `goods_output_transportation_add` и т.д.) — LLWA грузится последним в целевой цепочке, значит его числа и остаются в игре независимо от этого компача; вернуть числа TGR поверх значило бы отменить решение LLWA, а не починить потерю.
  * **Реальная потеря — под-блок `unscaled = { state_market_access_price_impact = 0.05 }`,** которого у LLWA нет вовсе ни под одним родителем ни в одной из десяти записей (не расхождение значений — отсутствие ключа). TGR добавляет его на семи из десяти: `pm_early_trains`, `pm_steam_trains`(+ptc3), `pm_electric_trains`(+ptc3), `pm_diesel_trains`(+ptc3). На шести — под `state_modifiers`; на `pm_diesel_trains_principle_transport_3` — под `building_modifiers` в самом файле TGR (у всех соседних записей `state_modifiers`, похоже на опечатку автора TGR, воспроизведена как есть — «не чинить чужой замысел, не разобравшись»).
  * **Три оставшиеся записи** (`pm_no_passenger_trains`, `pm_steel_passenger_carriages`, `pm_wooden_passenger_carriages`) пересекаются также с VC — по правилу «один файл на обе пары не делать» их восстановление целиком лежит в `_llwa/llwa+vc done`, здесь их нет.
  * **`ai_strategy_default` — не конфликт.** LLWA трогает его только отдельным файлом `LLWA_default_strategy.txt` (`INJECT:subsidies`, аддитивно, в под-блок, которого TGR не касается) — тот же класс, что уже закрытый `noneed` LLWA × HC+GoB+MoH.
- [x] **LLWA.2 × KAI — 9 ключей, все `common/ai_strategies`. Закрыто 26.08.2026.** Компач — `_llwa/llwa+kai done`, генератор — `tools/regen_llwa_kai.py`. **3 файла, самопроверка `0 problem(s)`, `--check` сходится.**
  * **Механизм — не ключевой конфликт, а перекрытие файла по ПУТИ.** `03_political_strategies.txt` у LLWA лежит по точно ванильному относительному пути — по правилу раздела 3 это файловое перекрытие: побеждает файл САМОГО ПОЗДНЕГО мода целиком, ванильный файл по этому пути выпадает из мерджа полностью, а не патчится по ключам. LLWA грузится последним в цепочке — его устаревшее тело (диф 125 строк, слова «LLWA» в файле нет) побеждает и голыми ключами стирает всё, что успело накопиться (INJECT KAI в том числе).
  * **`ai_strategy_default` — НЕ в этом файле** (проверено grep'ом по обеим версиям файла, ключа нет ни у LLWA, ни в текущей вании по этому пути) — не задет багом, здесь не чинится, см. LLWA.1.
  * **`ai_strategy_maintain_mandate_of_heaven` — никем больше не тронут** (не входит в пересечения ни с KAI, ни с VC, ни с TGR). Восстановления текущей вании по пути достаточно, доинжектить нечего.
  * **Три файла, в этом порядке загрузки ВНУТРИ мода** (сортировка имён: `0` < `z`): 1) `03_political_strategies.txt` — побайтовая копия ТЕКУЩЕЙ вании по тому же пути (наш мод грузится после LLWA — забирает то же файловое перекрытие себе); 2) `zzz_llwa_kai_political_strategies.txt` — пять чистых `INJECT:` KAI (`*_agenda`), читаются вживую и переиздаются как есть поверх восстановленной вании; 3) `zzzz_llwa_kai_vc_reforms.txt` — три записи, общие с VC (см. LLWA.3), переиспользуют уже закрытый мердж KAI×VC из `_vc/kai+vc done` **дословно** (читается вживую, не передран руками), а не выводятся заново.
- [x] **LLWA.3 × VC — 6 ключей. Закрыто 26.08.2026.** Три метода в `_llwa/llwa+vc done` (компач, генератор `tools/regen_llwa_vc.py`, **1 файл, 3 записи, `0 problem(s)`, `--check` сходится**), три стратегии — фактически в `_llwa/llwa+kai done` (см. LLWA.2, файл 3): подтверждена трёхсторонняя запись LLWA×KAI×VC, README `llwa+vc done` явно ссылается туда вместо повторного `REPLACE_OR_CREATE:` тех же трёх ключей из другого генератора (ровно та мина из раздела 8, которую предупреждение и называет).
  * **Методы:** VC инжектит одно и то же во все три (`building_modifiers.unscaled.building_job_attractiveness_mult = 2`), LLWA-шный `REPLACE:` — голое тело без этого под-блока вовсе (на `pm_no_passenger_trains` у LLWA нет даже `building_modifiers`). Восстановлено поверх тела LLWA, включая собственную добавку LLWA (`unlocking_production_methods` у `pm_steel_passenger_carriages`), TGR-овский ребаланс тех же трёх записей по-прежнему не восстанавливается (см. LLWA.1) — писать нечего сверх VC-шной добавки.
- [x] **LLWA.4 Пары LLWA × PBE, × PSC, × HC+GoB+MoH — `noneed`, перепроверено 26.08.2026 на актуальном наборе.** PBE 0/0 — вся инфраструктурная поверхность PBE это два общих модификатора штата (`state_infrastructure_mult`, `state_market_access_price_impact`), не список зданий; баффы применяются ко ВСЕЙ инфраструктуре штата, включая LLWA-шную, сами по себе. PSC 0/0. HC+GoB+MoH 1/0 (`ai_strategy_default`, см. LLWA.1 — тот же noneed). LLWA × Grey's — задача аддона Grey's (GR.9, `usu_llwa` compatch).
  * **LLWA × E&F+hotfix здесь БОЛЬШЕ НЕ ЧИСЛИТСЯ noneed — см. LLWA.6, ниже, статус пересмотрен 26.08.2026.** Старая запись «1/0, аддитивно» была верна для `building_railway` (единственного ключа, который трогает голый LLWA), но `pair_matrix.py` в принципе не может увидеть класс проблемы «мод добавил контент, которого нет в чужом списке» — новое здание никогда не попадёт в пересечение ключей, потому что пересечение требует, чтобы оба мода определяли ОДИН И ТОТ ЖЕ ключ. LLWA добавляет шесть новых зданий (`LLWA_building_roadway/waterway/riverway/airway`, `llwa_building_freight_depot`, `llwa_building_exchange`), и ни одно из них не значится в E&F-шном рукописном списке регистрации акций — это не то же самое, что «конфликта нет».
  * **`_llwa/llwa+morg out` тоже больше не в этой строке — см. LLWA.7.** Компач автора закрывает LLWA × Morgenröte по машинной матрице (1 общий ключ, аддитивно), но сам компач вносит новую потерю, которую матрица блока `LLWA` (`tools/blocks.json`, голый мод без компачей) тоже не видит.
  * **Урок для правил (записать в раздел 8):** `noneed`, выведенный из `pair_matrix.py`, закрывает ровно класс «оба мода трогают одну запись» и ничего не говорит про класс «один мод добавил контент, которого другой не видит». Для нового контента (здания, компании и т.п.) нужна отдельная проверка «кто ещё перечисляет это по имени» — по индексу всех вхождений токена по набору, не по пересечению ключей.
- [x] **LLWA.6 × E&F — 6 новых зданий LLWA не зарегистрированы в акциях/ликвидности E&F. Закрыто 26.08.2026, статус пересмотрен по вопросу пользователя.** Компач — `_llwa/llwa+ef done`, генератор — `tools/regen_llwa_ef.py`. **4 файла, самопроверка `0 problem(s)`, `--check` сходится.**
  * **Механизм E&F — не `building_group`, а рукописный список по имени здания,** сверено вживую в двух местах: `INJECT:` в `common/buildings/ef_*.txt` (частная инфраструктура покрывает только `building_port`, `building_railway`, `building_trade_center`, `building_manor_house`, `building_financial_district`) и `private_ownership_production_stocks` в `common/scripted_effects/01_financial_scripted_effects.txt` (1669 строк, 49 зданий, `railroad_stock` — только `building_railway`). Ни одно из шести зданий LLWA нигде не встречается; больше того, ни один мод в наборе, кроме самого LLWA, ни разу не упоминает ни одно из этих шести имён (сверено индексом по всем модам).
  * **Прецедент — `_ef/ef+morg done` в мегапаке,** решает точно ту же задачу для 62 своих зданий Моргенрёте тем же приёмом: инжект групп в здания + свой `_private_ownership_production_stocks`-переключатель + аддитивный хук в `on_yearly_pulse_country` + инициализация из `history/global` (иначе первый год всё висит на «No Stock»). Компач LLWA скопировал форму, урезав под шесть зданий.
  * **Тип акций — решение пользователя, не выводится из файлов.** Все шесть получили `pmg_private_ownership_railroad_stock` («добавляем LLWA в железнодорожные акции») — вся сеть LLWA считается одной транспортной категорией, а не делится на manufacture/agricultural/mining. Группа уже существует (обслуживает `building_railway`), новой локализации не потребовалось.
  * **`LLWA_building_logistics_hub` не тронут намеренно** — `ownership_type = no_ownership`, приватным владением быть не может.
  * **Известное безвредное взаимодействие с `llwa+morg out`** (см. LLWA.7): та компача обнуляет `LLWA_building_airway` до `{}` («Aerodromes? GONE!»), наш `TRY_INJECT:` в этом случае просто добавляет PMG-группу в пустышку — безвредно, но бессмысленно при этой комбинации. Задокументировано в README компача.
- [x] **LLWA.7 × Morgenröte (через компач автора) — E&F теряется на `building_railway`/`building_airport`. Закрыто 26.08.2026.** Компач — `_llwa/llwa+morg+ef done`, генератор — `tools/regen_llwa_morg_ef.py`. **1 файл, 2 записи, самопроверка `0 problem(s)`, `--check` сходится.**
  * **Повод — прямой вопрос пользователя** «моргенрота файлов не вижу»: `_llwa/llwa+morg out` (компач автора LLWA, id `xyz.1230james.LLWA_morgenrote_compatch`, версия 1.0.8) голыми полными телами переопределяет `building_railway` и `building_airport`, вручную сводя вклад LLWA и Morgenröte — но E&F в его `metadata.json` не заявлен вовсе, и обе группы E&F (`pmg_market_liquidity` + `pmg_private_ownership_railroad_stock` на рельсах из собственного файла E&F; `pmg_market_liquidity` + `pmg_private_ownership_manufacture_stock` на аэропорту из закрытой пары E&F×Morgenröte, свёрнутой в мегапак) молча выпадают.
  * **Это ровно та находка, что уже записана в правилах по итогам игры** — «в панели железной дороги остаются три группы вместо девяти — ни `pmg_market_liquidity` от E&F…» — формулировка называет «LLWA и его компачи» коллективно, не только голый LLWA.
  * **`tools/blocks.json` определяет блок `LLWA` только как голый мод** (`["../../vic3_mods_out/llwa"]`), без `llwa+morg out` — то есть вся машинная матрица по LLWA, на которую опирался прежний `noneed`, ни разу не мерила эту компачу.
  * **Не чинится намеренно:** тот же `building_airport` у `llwa+morg out` выбрасывает и собственную авиацию Morgenröte (`pmg_base_building_airport`, `pmg_cargo_airport`, `pmg_tourism_airport`) в пользу воздушных групп LLWA — не съеденный `INJECT:`, а выбор между двумя взаимоисключающими редизайнами одного здания, уже сделанный автором компача. «Не чинить чужой замысел, не разобравшись».
- [x] **LLWA.8 Компании — 142 исторических и финансовых компании по набору не знают про здания LLWA. Закрыто 26.08.2026, дополнено в тот же день по прямому вопросу пользователя.** Компач — `_llwa/llwa+companies done`, генератор — `tools/regen_llwa_companies.py`. **1 файл, самопроверка `0 problem(s)`, `--check` сходится.**
  * **Не пара, а раздача по всему набору.** LLWA закрыло свои семь зданий четырьмя своими компаниями (`LLWA_companies.txt`), но ~300 исторических и финансовых компаний по вании/TGR/E&F/VC/HC/MoH ни разу не упоминают ни одно здание LLWA — не конфликт в обычном смысле (ничего не стирается), просто новая сеть невидима для компаний, чей смысл — строить ровно такую инфраструктуру.
  * **Правило, применено механически, не вручную:** есть `building_railway` в `building_types` → `extension_building_types += LLWA_building_roadway`; есть `building_port` → `+= LLWA_building_waterway, LLWA_building_riverway`. 45 компаний прошли правило: 29 только дорога, 16 только вода, 2 (`company_hbc`, `company_yasuda`) — всё сразу.
  * **Членство проверено по ФИНАЛЬНОМУ определению компании в цепочке, не по первому автору.** 11 компаний вылетели из чернового списка ровно по этой причине: TGR определяет их как железнодорожные, но VC-шный `REPLACE_OR_CREATE:` на тот же ключ (VC грузится позже TGR) полностью меняет им профиль — например, `company_krupp` становится оружейно-металлургической, `company_east_india_company` — плантационной, `building_railway` не остаётся вовсе. Собственный ассерт генератора поймал это при первом прогоне.
  * **Догрузка того же дня: `llwa_building_exchange` — банкам E&F, по прямому вопросу пользователя «а что с llwa_building_exchange».** Здание не биржа в буквальном смысле, несмотря на название — собственные группы методов `LLWA_pmg_exchange_base` + `LLWA_pmg_mapi_comms` (телеграф/телефон/пресса, снижают market access price impact), но пользователь решил раздать его банкам всё равно: диверсифицированная финансовая компания вкладывается в инфраструктуру, двигающую рынок, — тот же принцип, что уже работает для железных дорог. **97 из 103 компаний E&F** прошли правило «есть `building_financial_centre_<страна>` в `building_types`» (сверка по префиксу — E&F суффиксирует ключ по стране, не голым `building_financial_centre`). Шесть без него: `company_private_construction`, три чисто добывающие, `company_PennsylvaniaRailroad` (голая железнодорожная, без финансового здания вовсе — уже покрыта правилом RAIL через финальное определение VC), `company_standard_oil` (у E&F пустой `building_types`-заглушка на этот ключ; финальное определение в цепочке — VC-шное, туда и попадает правило RAIL).
  * **Порядок источников в `SOURCES` — не порядок удобства, а порядок реальной цепочки.** Первая версия поставила E&F последним в словаре (после HC/MoH) — из-за этого `find_company()` для `company_standard_oil` взял пустую E&F-заглушку вместо настоящего VC-шного тела, и собственный ассерт генератора это сразу поймал (`AssertionError: ... no longer has building_railway`). Поправлено на реальный порядок правил (вания → TGR → E&F → VC → HC → MoH).
  * **Намеренно не тронуто:** `llwa_building_freight_depot`, `LLWA_building_logistics_hub` — не даны ни одной компании нигде, включая свои четыре у LLWA (похоже на умышленный выбор автора, не поломку); ~70 железнодорожных компаний Grey's/USU — территория GR.9, туда отсюда не лезем.
- [x] **LLWA.5 Сборка аддона-LLWA — закрыто 26.08.2026, пересобрано после LLWA.6-8.** `tools/build_addon_llwa.py` (по образцу `build_addon_vc.py`). **11 файлов, `ALL CHECKS PASS`, `--check` идемпотентно.**
  * **Семь пар не пересекаются ни по одному пути** (в отличие от `buy_packages` в аддоне-VC) — прямое копирование, отдельный merge-файл не понадобился.
  * **8 «дублей» ключей — все задекларированы, реальных нет.** Все восемь — один и тот же приём внутри `llwa+kai done`: голое тело-пол в `03_political_strategies.txt`, потом явный `INJECT:`/`REPLACE_OR_CREATE:` поверх в файле с более поздним именем — умышленная цепочка, а не два компача, спорящих за одну запись.
  * **`scan_conflicts.py` против LLWA, TGR, KAI, VC, E&F, `llwa+morg out`, аддона-VC, аддона-HC и мегапака `no t&r`:** 26 / 22 / 8 / 28 / 101 / 3 / 6 / 0 / 101 — каждое число сверено содержательно, ничего лишнего. Рост чисел против TGR/VC — новые компании (LLWA.8); против E&F/мегапака/`llwa+morg out` — новые находки LLWA.6-7, плюс 97 банков E&F после догрузки `llwa_building_exchange` тем же днём; `GLOBAL`/`on_yearly_pulse_country` пересечения — аддитивный паттерн (блоки складываются), не конфликт.
  * **Локализация и товары не задеты:** ни один из семи компачей не несёт новой локализации; `common/goods` файлов нет (потолок остаётся 74 из 128).
  * **Открытые вопросы не закрыты аддоном, только унаследованы** — `mobilization_option_chemical_weapons` (Morgenröte × VC, VC.3) и `anti_interest_groups`/`interest_group_government_weight` у `ai_strategy_meiji_restoration` (KAI × VC, VC.4) остаются открытыми там же, где были; аддон-LLWA просто переиздаёт `kai+vc done` как есть. Плюс новые от LLWA.6/8: тип акций для банков E&F (не решён), объём раздачи компаниям за пределами «естественных кандидатов».
  * **VC, KAI, TGR, E&F — не опциональны для этого набора** (решения №6/№7), поэтому `.off`-парная разметка (решение №9) не применена; если это когда-нибудь изменится, `zzzz_llwa_kai_vc_reforms.txt` первым потребует такой разметки (зависит от `_vc/kai+vc done`), как и `llwa+ef done`/`llwa+morg+ef done` (зависят от E&F).
  * Отчёт: `__addon/addon llwa/README.md`, полный разбор — `_llwa/analysis_llwa_new_content_report.md`, сырые прогоны `scan_conflicts.py` — `__addon/addon llwa/_scan_raw/`.
  * **Не сделано:** `thumbnail.png` (только у готовых полных аддонов VC/HC есть картинка, у самих парных компачей их обычно тоже нет — та же незакрытая мелочь) и публикация в мастерской (id нет, BBCode в README готов — тот же статус, что у HC.11).
- [x] ~~LLWA × T&R — те же шесть методов, у T&R теряется `goods_input_steel_add = 1`.~~ **Снято 25.08.2026: T&R убран из набора.**
