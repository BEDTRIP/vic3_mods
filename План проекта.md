# План проекта: девять блоков, три аддона

Обновлено 2026-08-25. Правила работы — `Правила работы с модами Victoria 3.md`. Инструмент — `tools/pair_matrix.py --blocks tools/blocks.json`.

**9 блоков → 36 пар.** 15 закрыто раньше (шесть блоков мегапака), 21 новая: **7 `noneed`**, **1 «есть»**, **1 «out, устарел»**, **3 сделано (аддон 1)**, **9 делать**.

## Целевой порядок запуска

```
1  основная пачка      CMF → ETF/Dence UI → TGR → PSC → KAI → E&F → E&F Hotfix → Morgenröte → T&R → PBE
2  MegaComPatch
3  HC + GoB + MoH      Hail Columbia → Gates of the Bosphorus → Mandate of Heaven
4  АДДОН 1             HC+GoB+MoH × мегапак
5  LLWA                LLWA + llwa_morgen compatch + usu_llwa comatch
6  АДДОН 2             LLWA × всё ниже
7  пачка Grey's        soft_econ, soft_pop, USU, food, ranch, diplo, subject, cinosphere
8  АДДОН 3             Grey's × всё ниже
9  косметика           Cold Water Ports, More Natural Straits, Merciful Incorporation, CoA for Powerblocs, shaded relief map, Thicker Fonts
```


`_multiline` из состава **убран**: CMF 1.63 везёт те же шесть файлов MPM в более свежей редакции, отдельный мод их откатывает. Мердж семи виджетов E&F × MPM делается в аддоне 3 (пара Grey's × E&F).

Требования к порядку, на которых держатся `noneed` — **в README каждого аддона отдельной строкой**:

* **LLWA после Mandate of Heaven** — иначе `INJECT: subsidies` LLWA ложится под `REPLACE:ai_strategy_default` MoH (9075 строк) и пропадает;
* **Mandate of Heaven после Hail, Columbia!** — иначе `INJECT:` MoH в `ig_rural_folk` ложится под голое тело `00_rural_folk.txt` от HC и пропадает.

## Матрица

| мод        | Grey's         | LLWA          | HC+GoB+MoH | E&F+hotfix | Morgenrote | PBE    | PSC       | TGR  | T&R+KAI |
| ---------- | -------------- | ------------- | ---------- | ---------- | ---------- | ------ | --------- | ---- | ------- |
| Grey's     | -              |               |            |            |            |        |           |      |         |
| LLWA       | out, устарел   | -             |            |            |            |        |           |      |         |
| HC+GoB+MoH | А3 · средний   | **noneed** \* | -          |            |            |        |           |      |         |
| E&F+hotfix | А3 · большой   | **noneed**    | **noneed** | -          |            |        |           |      |         |
| Morgenrote | А3 · большой   | есть          | **done**   | done       | -          |        |           |      |         |
| PBE        | А3 · 1 запись  | **noneed**    | **noneed** | noneed     | noneed     | -      |           |      |         |
| PSC        | А3 · критичный | **noneed**    | **noneed** | done       | noneed     | noneed | -         |      |         |
| TGR        | А3 · большой   | А2            | **done**   | done       | noneed     | done   | noneed    | -    |         |
| T&R+KAI    | А3 · большой   | А2            | **done**   | out fixed  | out fixed  | noneed | out fixed | done | -       |

Легенда: `А1/А2/А3` — в какой аддон идёт · `done` — компач написан · `noneed` — проверено, не нужен · `есть` — компач автора чужого мода · `out …` — чужой компач как источник.

\* держится на порядке загрузки, см. выше.

Машинная матрица пересечений (не-аддитивные общие ключи / общие пути файлов) — `pair_matrix.py --blocks blocks.json`. Текущий прогон:

```
           |  Grey's   LLWA  HC+GoB  E&F+hf  Morgen   PBE    PSC    TGR   T&R+KAI
Grey's     |    -      11/0  11/1    24/0    35/0    1/0    8/0   97/0   128/0
LLWA       |  11/0      -     1/0     1/0     1/0    0/0    0/0   11/0    16/0
HC+GoB+MoH |  11/1     1/0     -      0/0    26/21   0/0    0/0   16/3     9/0
```

**TGR × T&R+KAI, `ai_strategy_default` — дополнено 25.08.2026.** Статус пары остаётся `done`, но одна запись в ней была разобрана неверно: отчёт числил `ai_strategy_default` среди «у обоих авторов полные осознанно разные стратегии», хотя у TGR своей стратегии нет — есть три `INJECT:`-файла, и голое тело KAI по ванильному пути уносит их все. Восстановлено в мегапаке (`zz_tr_kai_tgr_ai_default_strategy.txt`) и в аддоне 1 (`zz_hctr_tgr_default_strategy.txt`); `wanted_construction_output` осознанно оставлен KAI, `diplomatic_play_support` смерджен. Разбор — в `_tgr/tgr+tr+kai done/conflicts_tgr_vs_kai_report.md`.

Пачка Grey's выросла с шести модов до восьми (`_grey_soft_pop`, `grey_deeper_cinosphere`) — пересчёт 2026-08-24, разбор в `__addon/conflicts_addon3_greys_report.md`. Прирост: **+13 к TGR**, **+4 к T&R+KAI**, **+8 к HC+GoB+MoH**; остальные пары без изменений, товары по-прежнему 111/128.

---

# АДДОН 1 — HC+GoB+MoH × мегапак  ← собран, 24–25.08.2026

Три пары из шести. С E&F, PBE и PSC — `noneed`, значит **аддон 1 совместим с вариантом мегапака `no e&f+psc` как есть**; для `no tgr` и `no t&r` нужны свои композиции (см. правило «один файл = одна пара» в правилах, раздел 2).

### Задачи

- [x] **1.1 HC+GoB+MoH × T&R+KAI — четыре закона рабства.** **Сделано.** `common/laws/zz_hctr_slavery.txt` — тело HC плюс `on_activate`, `modifier` и условия `can_enact` от T&R. `can_enact` — конъюнкция, поэтому гейт HC (правило кляпа, поправка Корвина) и гейт T&R (права человека ООН) держатся вместе, выбирать сторону не пришлось. Мелочь той же пары разобрана: `decree_greener_grass_campaign` и `je_warlord_china` смерджены (см. ниже), `pmg_luxury_building_glassworks` и `NAI` — аддитивны, не трогаем. Исходно: `usfp_law_slavery_overrides.txt` (HC) делает `REPLACE:` на `law_slave_trade`, `law_debt_slavery`, `law_legacy_slavery`, `law_colonial_slavery`; T&R делает `REPLACE:` на те же четыре из `ztr_un_updated_slavery.txt` (128 строк против 119). HC грузится позже — переработка рабства T&R пропадает. ~~Ждёт решения №4.~~
  Мелочь в той же паре, проверить и, скорее всего, закрыть: `decree_greener_grass_campaign`, `je_warlord_china`, `pmg_luxury_building_glassworks`, `NAI`, `ai_strategy_default`.

- [x] **1.2 HC+GoB+MoH × TGR — группы интересов.** **Сделано.** `zz_hct_ig_landowners.txt` и `zz_hct_ig_rural_folk.txt` — тела HC с трёхсторонним мерджем правок TGR против ванили. TGR меняет здесь четыре числа, а не структуру, поэтому мердж вышел дешевле оценки. В `ig_rural_folk` заодно вычищена до-1.13 копия ванили из `moh_rural_folk.txt` — оставлены только два реальных добавления MoH (идеология лидера Гоминьдана и переименование Нунмин). Исходно: HC везёт `common/interest_groups/00_landowners.txt` (788 строк) и `00_rural_folk.txt` (560) голыми телами по ванильным путям; TGR правит те же группы через `REPLACE_OR_CREATE` (772 и 563). HC позже — все правки TGR пропадают: идеологии, трейты, `on_enable`, `priority_cultures`. Два мерджа по ~780 строк, самый дорогой кусок аддона 1.

- [x] **1.3 HC+GoB+MoH × TGR — три перекрытия по пути.** **Сделано.** `chi - china.txt` — тело MoH + `company_ong_lung_sheng_tea_company` от TGR; `tur - ottoman empire.txt` — тело GoB + `company_imperial_arsenal`. `manifest_destiny.txt` **осознанно не трогаем**: HC везёт файл с полностью закомментированным решением, он убирает ванильное Manifest Destiny нарочно и заменяет его журнальной цепочкой на 1100 строк. Вернуть переработку TGR — значит дать игроку два Manifest Destiny. Исходно: `common/decisions/manifest_destiny.txt` (HC), `common/history/countries/chi - china.txt` (MoH), `common/history/countries/tur - ottoman empire.txt` (GoB). Во всех трёх побеждает мод аддона, история от TGR пропадает.

- [x] **1.4 HC+GoB+MoH × TGR — `ai_strategy_default`.** **Сделано.** `zz_hctr_ai_strategy_default.txt` — трёхсторонний мердж KAI и MoH против ванили как общей базы (0 конфликтов, все расхождения — отступы или совпадение), плюс `zzz_hctr_tr_default_strategy.txt` с переизданием инжекта T&R на 1360 строк. Инжекты TGR разобраны отдельно 25.08.2026 — `zz_hctr_tgr_default_strategy.txt`, см. отчёт пары TGR × KAI. Исходно: MoH делает `REPLACE:` полным телом на 9075 строк. KAI везёт свой `00_default_strategy.txt` (8736 строк, перекрытие ванильного пути), TGR инжектит тремя файлами, T&R — одним на 1360 строк. MoH последний. **Разобрать, что уцелело** — по объёму может оказаться крупнее всей остальной пары. Формально относится и к 1.1.

- [x] **1.5 HC+GoB+MoH × TGR — проверить и, вероятно, закрыть.** **Сделано, почти всё закрыто.** `ideology_jacksonian_democrat` оказался не аддитивным — HC переписывает идеологию без позиций TGR по `lawgroup_election_system` и `lawgroup_legislative_process`; они возвращены в `zz_hct_jacksonian_democrat.txt`. Остальные перечисленные записи проверены и действительно аддитивны: последний, кто их трогает, инжектит в под-блок, который никто больше не называет. Исходно: `ideology_jacksonian_democrat`: HC делает `REPLACE_OR_CREATE`, TGR инжектит `lawgroup_election_system` / `lawgroup_legislative_process` — под-блоки, которых HC не называет, должны выжить. `law_ethnostate`, `law_freedom_of_conscience`, `law_protectionism`, `law_canton_system`, `law_theocracy`, `ideology_communist`, `ig_armed_forces` / `ig_intelligentsia` / `ig_petty_bourgeoisie` — везде `INJECT:`, аддитивно.

- [x] **1.6 HC+GoB+MoH × Morgenrote — передифф 21 файла.** **Сделано.** Все 20 копий передиффены: расхождения — только гены портретов, то есть намеренные варианты GoB и HC, а не отставшие копии; не трогаем. Реально мерджить пришлось четыре записи: `common/achievement_groups.txt` (база MoH + 12 групп Morgenröte + возвращённый `achievement_exactly_100`), `gaudi_capital_expansion_level_4_modifier` (заглушка GoB гасила настоящий модификатор), `revive_olympic_games_decision` (переиздано подавление от MR) и два `character_templates` (Крайслер и Твен — работа от HC, роли и `on_created` от MR). Исходно: 18 `common/dna_data/mr_*.txt` от GoB и 2 `ecchi_usa_*.txt` от HC — чужие файлы Morgenröte, которые моды везут своими копиями под теми же именами. Похоже на намеренную совместимость, но редакция может отставать от 2.8.3e. Оставить только реально расходящиеся.
  Плюс `common/achievement_groups.txt` (MoH, косметика), `revive_olympic_games_decision`, `gaudi_capital_expansion_level_4_modifier`, `pmg_luxury_building_glassworks`, два `character_templates`.

- [x] **1.7 Сборка аддона 1.** **Сделано.** `tools/build_addon1.py`: сборка из трёх компачей, покрытие побайтово (`filecmp`), баланс скобок, кодировки, дубли ключей с декларациями, локализация по языкам, товары. 15 файлов, `ALL CHECKS PASS`. Пересечения аддона с самими модами прогнаны `scan_conflicts.py` по каждому блоку — ноль с E&F, хотфиксом, PSC, PBE и с самим мегапаком. Исходно: собрать из трёх компачей + проверки (покрытие, скобки, кодировки, локализация по языкам, товары, `scan_conflicts` против каждого блока).

- [x] **1.8 README и metadata аддона 1.** **Сделано.** `tools/addon1_metadata.py` пишет `metadata.json` всем четырём папкам из одной таблицы, без BOM, через `json.loads`. README — с порядком загрузки, строкой «MoH после HC», разбором по каждому мерджу, списком найденных чужих багов и разделом «как запустить без какого-то мода». `hailcolumbia` в `relationships` не сослать — у него пустой `id`, он назван в README и в `tested_with`. Исходно: `relationships` на HC, GoB, MoH, CMF + Morgenröte, TGR, T&R, KAI; порядок загрузки; строка «MoH после HC»; раздел «что делает только эта сборка».

### Осталось по аддону 1

- [ ] **1.9 Проверка в игре.** `role` рядом с `executive_usage` / `agitator_usage` у Крайслера и Твена — конструкции нет ни в ванили, ни у обоих авторов. Смотреть, не спавнятся ли персонажи дважды и не ругается ли `debug.log` на `PostValidate`. Единственное, что нельзя закрыть с диска.
- [ ] **1.10 Письма авторам.** GoB — заглушка `gaudi_capital_expansion_level_4_modifier` гасит настоящий модификатор; MoH — `moh_rural_folk.txt` инжектит до-1.13 тело ванили и в `achievement_groups.txt` нет `achievement_exactly_100`; TGR — `TGR_ADJUSTMENTS_oregon.txt` копия старой ванили с `months = normal_modifier_time` (152 года вместо пяти).
- [ ] **1.11 Публикация.** BBCode готов в README, id мастерской нет.

Отдельных сборок-вариантов аддона не делаем: один файл = одна пара, поэтому «вариант» — это список файлов, и он записан в README аддона человеческим языком («как запустить без какого-то мода»).

### Не делать в аддоне 1

Пары HC+GoB+MoH × E&F+hotfix, × PBE, × PSC — `noneed`, ноль пересечений. Внутри блока HC+GoB+MoH тоже чисто, кроме требования «MoH после HC».

---

# АДДОН 2 — LLWA  ← делаем сейчас

- [ ] **2.1 LLWA × TGR — десять железнодорожных методов.** `LLWA_rails.txt` делает `REPLACE:` на `pm_steam_trains`, `pm_electric_trains`, `pm_diesel_trains`, три `_principle_transport_3`, `pm_early_trains`, `pm_no_passenger_trains`, `pm_steel_passenger_carriages`, `pm_wooden_passenger_carriages`; TGR — `REPLACE_OR_CREATE` на те же. Теряется `unscaled = { state_market_access_price_impact = 0.05 }`.
  У `pm_steel_passenger_carriages` LLWA добавляет под-блок `unlocking_production_methods`, которого нет ни у ванили, ни у TGR — писать полное тело, пока не разрешён вопрос про под-блоки `REPLACE:`.
- [ ] **2.2 LLWA × T&R** — те же шесть методов, у T&R теряется `goods_input_steel_add = 1`.
- [ ] **2.3 LLWA × KAI — `03_political_strategies.txt`.** LLWA перекрывает ванильный путь копией из старой версии игры (слова «LLWA» в файле нет, диф 125 строк). Откатывает ваниль 1.13 и голыми телами стирает весь `kai_political_strategies.txt` (семь `INJECT:` + `REPLACE:ai_strategy_meiji_restoration`). Лечение: текущая ваниль по тому же пути + `zzz_addon_kai_political_strategies.txt` с копиями из KAI. Автору написать.
- [ ] **2.4** Пары LLWA × E&F, × PBE, × PSC, × HC+GoB+MoH — `noneed`, ничего не делать. LLWA × Morgenrote закрыт компачем автора.

---

# АДДОН 3 — Grey's

- [ ] **3.1 Grey's × PSC — стройсектор, критично.** `yMoG_USU_construction.txt` (USU) делает `REPLACE_OR_CREATE:building_construction_sector` и оставляет только `pmg_base_building_construction_sector`. Мегапаковский `zz_pb_ef_construction_sector.txt` — мердж PSC + PBE + E&F на 95 строк — сносится целиком. Плюс `bg_construction`, `NCountry`, `my_trigger_event`, четыре метода стройки.
- [ ] **3.2 Grey's × PBE — одна запись.** `grey_subject` делает `TRY_REPLACE:force_become_subject` и называет все под-блоки, включая `possible` и `accept_effect`; PBE переписывает это же действие голым телом (гейты `has_game_rule = vokaes_power_bloc_rule_diplo_action_changes_enabled`, `PRESTIGE_THREE_TIMES_POSSIBLE`, убранная ветка `dominion`, `create_incident`).
- [ ] **3.3 Grey's × HC+GoB+MoH — 11 ключей и один файл.**
  * **3.3a КРИТИЧНО, три строки.** `_grey_soft_pop` объявляет `REPLACE_OR_CREATE:is_usfp_active = { always = no }` в `00_SPA_mod_compat_active_triggers.txt` и грузится после Hail Columbia → **выключает Hail Columbia целиком**, каждый её гейт `is_usfp_active = yes` становится ложным, молча. Лечится файлом аддона с `always = yes`. Автору написать: третий мод не должен объявлять чужой флаг `*_is_active`.
  * **3.3b `grey_deeper_cinosphere` × Mandate of Heaven — пять культур** `han`, `hakka`, `manchu`, `min`, `yue`. cinosphere делает `TRY_REPLACE` полными телами (615 / 191 / 612 строк) и называет те же под-блоки имён, в которые MoH инжектит; у `manchu` MoH делает `REPLACE` и тоже проигрывает. Китайские имена и правки MoH пропадают. Отдельно: `noble_last_names` у `han` cinosphere не называет — под-блок останется от MoH, получится смесь, которую не задумывал ни один автор.
  * **3.3c `decree_greener_grass_campaign`** — тройной: T&R `INJECT`, Hail Columbia `REPLACE` (119L), `_grey_soft_pop` `REPLACE` (108L, побеждает). Теряются оба.
  * **3.3d** `common/history/countries/mon - montenegro.txt` везут и `grey_usu` (94), и GoB (102), ваниль 93. Плюс `pm_religious_bureaucrats`, `state_trait_columbia_river`, `NAI`, `law_ethnostate`.
- [ ] **3.4 Grey's × E&F+hotfix — 24 ключа.** Шесть зданий (`building_food_industry`, `building_livestock_ranch`, `building_port`, `building_power_plant`, `building_railway`, `building_trade_center`) теряют `pmg_market_liquidity` и `pmg_private_ownership_*`; 14 `pm_company_headquarter_*`; `company_Bankenverein`, `company_HandelsBanken` (из хотфикса), `company_standard_oil` (из мегапака); `NEconomy`.
  **Сюда же мердж семи GUI-виджетов E&F × MPM** — тело E&F, изменения MPM брать из CMF 1.63, не из `_multiline` 1.7.3.
- [ ] **3.5 Grey's × Morgenrote — 35 ключей.** Шесть зданий, 18 методов (`pm_elgar_*`, `pm_air_*`, `pm_domestic_routes`, `pm_nr_national_park`), пять `RU_BL_*`, `popneed_free_movement`, `popneed_leisure`, `elgar_company_basic_instruments`, `tesla_building_is_railway_building`.
- [ ] **3.6 Grey's × TGR — 97 ключей.** 56 методов, шесть товаров (`electricity`, `transportation`, `clippers`, `fabric`, `wood`, `merchant_marine`), три здания, четыре технологии, `monopoly_charter`, `bg_trade`, `NEconomy`/`NAI`/`NPolitics`, `trade_states`, `foreign_investment_rights`, три `popneed_*`, четыре типа компаний. **Ждёт решения №3.**
  * **3.6a `_grey_soft_pop` × TGR — восемь рангов страны.** `TRY_REPLACE` полными телами на все восемь (`great_power` … `decentralized_power`) поверх `REPLACE_OR_CREATE` TGR. Уходит `country_construction_add` (30 / 20 / …) — добавка TGR, которой в ванили нет вообще, — и возвращаются ванильные `treaty_article_cost`, `ai_pool_character_multiplier`, `country_max_unassigned_generals_add`/`_admirals_add`, `country_loan_interest_rate_mult`. Мердж: тело soft_pop + правки TGR обратно.
  * **3.6b `_grey_soft_pop` × TGR — четыре закона гражданства.** `TRY_REPLACE` на `law_ethnostate`, `law_racial_segregation`, `law_cultural_exclusion`, `law_national_supremacy` называет `acceptance_modifier` — ровно тот под-блок, куда TGR инжектит `country_political_strength_full_acceptance_mult` (−0.25 / −0.20 / …). Все четыре инжекта пропадают.
  * **3.6c два define.** `NPops.MAX_DEMAND_ADJUSTMENT_BASE_AMOUNT` (TGR 0.05 → soft_pop 0.01) и `NPops.MAX_DEMAND_ADJUSTMENT_SCALED_AMOUNT` (TGR 0.10 → soft_pop 0.09). Остальные 118 define soft_pop ни с кем не пересекаются.
- [ ] **3.7 Grey's × T&R+KAI — 128 ключей, самый большой.** 46 методов + 12 групп, 27 типов компаний, 12 типов боевых юнитов, 11 зданий, три `mobilization_options`, шесть `popneed_*`, `NEconomy`/`NWar`/`NAI`, `liberty_desire_weekly_change`, `goods_transfer`, `ai_strategy_territorial_expansion`. Из нового — `decree_greener_grass_campaign` (см. 3.3c) и те же два `NPops`.
- [ ] **3.8 LLWA × Grey's — пересобрать `usu_llwa comatch`.** Чужой компач устарел; по правилу тут **компач-замена**, а не слой. Свежая распаковка лежит в `_greys/usu_llwa out outdate`. Чинить: `building_railway` полным телом определяют трое (USU `~zzzzMoG_USU_railways.txt` 226 строк, `usu_llwa`, `llwa_morgen`); `building_airport` — двое; десять железнодорожных методов есть и у USU, и у LLWA. Оба компача LLWA нужны, значит их разведение — часть этой задачи.

### Внутри блока Grey's

- [ ] **3.9 `_grey_soft_econ` и `grey_usu` везут файл с одним именем** `common/company_charter_types/yMoG_company_charter_types.txt` с разным содержимым — перекрытие по пути, один мод молча теряет чартеры. Плюс `monopoly_charter`, `NAI`, `pm_trade_center`, `pm_trade_center_principle_external_trade_2` в обоих.
- [ ] **3.10 `grey_diplo` объявляет `Grey_DIS_is_active` голым ключом.** Для `scripted_triggers` это не переопределяет — флаг остаётся `no` из реестра CMF, чужая интеграция молча не срабатывает. Три строки + письмо автору.
- [ ] **3.12 `grey_deeper_cinosphere` объявлен для 1.12.** В `metadata.json` тег `1.12`, `supported_game_version` пустой. В телах культур нет `seal_and_signature_texture`, который есть в ванили 1.13 — признак копии из старой версии, того же класса, что стухший `03_political_strategies.txt` у LLWA. Сверить против ванили 1.13 до того, как писать мердж 3.3b.
- [ ] **3.13 `_grey_soft_pop` объявляет шесть чужих `com_law_*_alternative_trigger = { always = no }`** из реестра CMF. В нашем наборе безвредно (эти флаги объявляет только CMF, никто не ставит `yes`), но шапка файла прямо говорит, что он рассчитан грузиться «сразу после `com_law_blocker_triggers.txt`» — автор считал порядок по имени файла. Перепроверять при каждом расширении набора.
- [ ] **3.11 Пересечения внутри пачки Grey's** — `usu`×`food` (9 методов охлаждения), `food`×`ranch` (6: `pm_slaughterhouses`, `pm_mechanized_slaughtering`, `pm_butchering_tools`, `pm_open_air_stockyards`, две `refrigerated_*`), `usu`×`ranch` (2), `diplo`×`subject` (`trade_states`, `is_giftable_treaty_article`), `_grey_soft_econ`×`_grey_soft_pop` (`peasants` в `pop_types`), `grey_subject`×`grey_usu` (`total_bureaucracy_need`). Похоже на штатную модульность автора, тела не сравнивались. `grey_deeper_cinosphere` не пересекается ни с одним модом пачки.

---

## Решения (развилки)

| № | вопрос | статус |
| - | ------ | ------ |
| 1 | Берём ли USU целиком | открыт — 84 пересечения с TGR, 124 с T&R+KAI |
| 2 | Какой компач LLWA включаем | **закрыт: оба.** Значит их взаимное разведение — задача 3.8 |
| 3 | `PRICE_RANGE` (мегапак 0.85 / soft_econ 0.9) и `BUY_SELL_DIFF_AT_MAX_FACTOR` (TGR 1.65 / soft_econ 3) | открыт, «потом подумаю» |
| 4 | Законы рабства: Hail Columbia или T&R | **закрыт: оба.** `can_enact` — конъюнкция, гейты HC и T&R про разное и держатся вместе; выбирать сторону не потребовалось (задача 1.1) |
| 5 | `_multiline` | **закрыт: не включаем**, мердж делается в 3.4 |

Полная таблица расходящихся defines soft_econ (11 штук) — в `__addon/план_работ_аддон.md`, развилка №3.

## Основания для новых `noneed`

| пара | пересечения | почему не нужен |
| ---- | ----------- | --------------- |
| LLWA × E&F+hotfix | 1 ключ, 0 путей | `building_railway`, оба мода делают `INJECT:`. Инжекты складываются. Железную дорогу ломают компачи LLWA и USU — это другие пары |
| LLWA × PBE | 0/0 | ничего общего, кроме аддитивных `on_actions` |
| LLWA × PSC | 0/0 | то же |
| LLWA × HC+GoB+MoH | 1/0 | `ai_strategy_default`: MoH `REPLACE:` полным телом, LLWA `INJECT: subsidies`. Цело, **пока LLWA после MoH** |
| HC+GoB+MoH × E&F+hotfix | 0/0 | пересекаются только 7 ключей локализации, все в пользу E&F (грузится раньше, а в локализации выигрывает первый) |
| HC+GoB+MoH × PBE | 0/0 | `on_wargoal_enforced` / `on_diplomatic_play_started` и `POWER_BLOCS` — аддитивные категории |
| HC+GoB+MoH × PSC | 0/0 | ничего общего |

Дополнительно проверено на всех 21 паре: **id событий не пересекаются нигде**; товары **111 из 128** (запас 17); дублей локализации внутри одной языковой папки нет.

---

# Таблица информации

| мод        | папка                                                                                                                                                              | папка компачей                                   | есть русификатор? |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------ | ----------------- |
| Grey's     | "C:\Users\Andrey\Projects\vic3_mods_out\grey_add_alot_of_things"                                                                                                   | "C:\Users\Andrey\Projects\vic3_mods\_greys"      | +                 |
| LLWA       | "C:\Users\Andrey\Projects\vic3_mods_out\llwa"                                                                                                                      | "C:\Users\Andrey\Projects\vic3_mods\_llwa"       | нет               |
| HC+GoB+MoH | "C:\Users\Andrey\Projects\vic3_mods_out\for addon"                                                                                                                 | "C:\Users\Andrey\Projects\vic3_mods\_HC+GoB+MoH" | +                 |
| E&F+hotfix | "C:\Users\Andrey\Projects\vic3_mods_out\E&F"<br>"C:\Users\Andrey\Projects\vic3_mods\_ef\ef hotfix 1.13"<br>+ требует "C:\Users\Andrey\Projects\vic3_mods_out\_etf" | "C:\Users\Andrey\Projects\vic3_mods\_ef"         | done              |
| Morgenrote | "C:\Users\Andrey\Projects\vic3_mods_out\Morgenrote"<br>+ требует "C:\Users\Andrey\Projects\vic3_mods_out\_cmf"                                                     | "C:\Users\Andrey\Projects\vic3_mods\_morg"       | +                 |
| PBE        | "C:\Users\Andrey\Projects\vic3_mods_out\PowerBlocksExpanded"                                                                                                       | "C:\Users\Andrey\Projects\vic3_mods\_pbe"        | +                 |
| PSC        | "C:\Users\Andrey\Projects\vic3_mods_out\PSC"                                                                                                                       | "C:\Users\Andrey\Projects\vic3_mods\_psc"        | нет               |
| TGR        | "C:\Users\Andrey\Projects\vic3_mods_out\TheGreatRevision"                                                                                                          | "C:\Users\Andrey\Projects\vic3_mods\_tgr"        | done              |
| T&R+KAI    | "C:\Users\Andrey\Projects\vic3_mods_out\TechRes+Kuromi"                                                                                                            | -                                                | +                 |

## Состав блоков

| блок | моды |
| ---- | ---- |
| Grey's | `_grey_soft_econ`, `_grey_soft_pop`, `grey_usu`, `grey_deeper_cinosphere`, `grey_food`, `grey_food_2_ranch`, `grey_diplo`, `grey_subject` (без `_multiline`) |
| LLWA | `llwa` + компачи автора: `_llwa/llwa+morg out` (пара LLWA × Morgenrote), `_greys/usu_llwa out outdate` (пара LLWA × Grey's) |
| HC+GoB+MoH | `hailcolumbia`, `gatesofbosphorus`, `mandateofheaven` — все три требуют CMF |

## Открытые вопросы

* **`REPLACE:` и под-блоки** — **закрыто 24.08.2026** в пользу «патчит по под-блокам» (решение записано в `__megapacks/megapack/conflicts_megapack_internal.md`). Шапка `zzzz_ef_tr_fix_gold_minting.txt` всё ещё утверждает обратное — перечитать её вместе с вопросом, нужен ли ещё сам файл
* **TGR × T&R, `institution_scores`** — `ztr_default_strategy.txt` переписывает все семь ванильных институтов значением 10 (дословная копия ванильного блока + свой `institution_environmental_policy`) и грузится после TGR, у которого police / health_system / home_affairs подняты до 500 осознанно, с пометкой `TGR LINE`. Копия ванили молча гасит осознанную правку. Пара стоит `done`, найдено 25.08.2026 при разборе TGR × KAI
* **Свежесть 20 копий файлов Morgenröte** внутри GoB и HC — задача 1.6
* **`hailcolumbia` без `id`** в `metadata.json` — сослаться в `relationships` нельзя
* **Косметика вне девяти блоков** (Cold Water Ports, More Natural Straits, Merciful Incorporation, shaded relief map) — карту трогают трое, и Mandate of Heaven тоже везёт `map_data` (`provinces.png`, `default.map`, `province_terrains.txt`, `state_regions/`). Между собой не проверялись
