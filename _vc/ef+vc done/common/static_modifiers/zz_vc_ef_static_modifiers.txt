# E&F x VC compatch -- re-apply E&F's base_values additions
# Generated by tools/regen_vc_ef.py on 2026-08-26. Do not hand-edit: the next run
# overwrites this file. Change the generator instead.
#
# Load order: E&F -> E&F Hotfix -> ... -> Victorian Century -> THIS.
# Why this file exists: VC's REPLACE_OR_CREATE:base_values wipes E&F's INJECT (an icon and country_minting_add = -500) silently. VC's own body already sets country_minting_add = 1000 for a different reason (a flat minting baseline); country_minting_add is an _add field and Vic3 stacks repeated _add fields within one modifier rather than letting the later one win (confirmed in `Правила работы...` section 6, the multiply-twice warning) -- so re-applying E&F's line here restores what both authors would get running together without VC's REPLACE_OR_CREATE wiping the block: 1000 + (-500) = net +500, same as no compatch would have produced if VC's directive were merge-friendly. Recorded as a decision, not silently dropped -- if this reads wrong in game, the fix is to drop E&F's line instead, not to guess again.

INJECT:base_values = {
	icon = gfx/interface/icons/timed_modifier_icons/modifier_coins_positive.dds
	country_minting_add = -500
}
