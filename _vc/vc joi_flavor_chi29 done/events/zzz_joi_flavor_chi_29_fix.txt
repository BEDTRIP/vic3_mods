# !! DISABLE, NOT A DATA FIX !!
# joi_flavor_chi.29 -- ЭТО СОБСТВЕННОЕ СОБЫТИЕ Victorian Century (namespace
# joi_flavor_chi внутри самого VC, файл events/joi_flavor_chi.txt), не сторонний
# мод. Отключено 30.08.2026 после того как ДВЕ более мягкие попытки не сработали:
#
#   1. `trigger = { always = no }` -- не сработало ПРИНЦИПИАЛЬНО. Событие
#      вызывается не выборкой из pulse/пула (где `trigger` действительно
#      проверяется), а напрямую: `common/scripted_buttons/joi_china_buttons.txt`
#      дёргает `trigger_event = { id = joi_flavor_chi.29 }` из кнопки на топбаре
#      (15 мест в файле, разным колониальным державам). Прямой `trigger_event`
#      игнорирует поле `trigger` целевого события целиком.
#   2. Явный `pop_type = laborers` в обоих `create_pop` -- проверено в игре
#      30.08.2026, СИМПТОМ (SoL 100 у созданных попов, экономика принимающего
#      штата рушится) СОХРАНИЛСЯ и с этой правкой. Значит причина не в
#      отсутствии pop_type -- настоящий механизм, откуда newly-created migrant
#      pop берёт SoL 100 сразу после create_pop, не установлен (открытый
#      вопрос, см. План проекта.md).
#
# Раз почему -- не выяснено, а чем -- воспроизводимо ломает экономику каждую
# партию (симптом стабильно проявляется примерно на 4-й год), решение —
# отключить последствия события целиком, а не гадать дальше вслепую. Тело ниже
# -- ПОЛНОЕ переопределение события (голый ключ без префикса, порядок решает
# позиция МОДА в плейсете, не имя файла): всплывающее окно и текст остаются
# (обе локализационные строки VC не тронуты), но исчезают все игровые эффекты
# -- никакого create_pop, create_mass_migration, add_acceptance или
# add_modifier. Клик по кнопке на топбаре по-прежнему открывает popup и
# закрывает его любым из двух вариантов, но экономику он больше не трогает.
#
# Этот файл должен грузиться ПОСЛЕ Victorian Century (как и раньше).
#
# Не переиздано в чистом виде "пока стоит только один вопрос" -- если найдётся
# настоящая причина SoL 100, ЧИНИТЬ здесь же, вернув create_pop/create_mass_migration
# с рабочим фиксом, а не открывать отдельный файл.

joi_flavor_chi.29 = {#与中国的劳工合同
	type = country_event
	title = joi_flavor_chi.29.t
	desc = joi_flavor_chi.29.d
	flavor = joi_flavor_chi.29.f

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	dlc = dlc_vc

	event_image = {
		video = "asia_poor_people_moving"
	}

	duration = 3

	immediate = {}

	option = {
		name = joi_flavor_chi.29.a
		default_option = yes

		ai_chance = {
			base = 10
			modifier = {
				trigger = {
					c:BRZ ?= this
				}
				add = -100
			}
		}
	}

	option = {
		name = joi_flavor_chi.29.b
	}
}
