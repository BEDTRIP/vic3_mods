#############################################
# 7a. Division by zero in the financial demand values
#
# E&F computes the demand modifiers for its five stock/bond goods like this
# (common/script_values/00_financial_scripted_value.txt):
#
#   target_demand_<good>_ajusted = {
#       value    = target_demand_<good>
#       subtract = market.mg:<good>.market_goods_exports
#       divide   = building_financial_num   # <-- can be 0
#       ...
#   }
#   target_demand_<good>_for_modifier = {
#       value    = target_demand_<good>_ajusted
#       subtract = base_demande_<good>_fix
#       divide   = base_demande_<good>_fix  # <-- can be 0
#       multiply = 100
#   }
#
# The author knows about the first one -- his own comment on that line reads
# "---------------> division par zero possible". building_financial_num is a
# sum of ~90 has_building_financial_centre_<tag> flags, so it is 0 for every
# country that owns no financial centre, which is most of the world early on.
# base_demande_<good>_fix just reads var:base_demande_<good>_fix, which is 0
# until the first financial centre exists.
#
# Fix: clamp the first divisor at 1, and guard the second with a
# `> 0` check so the whole block is skipped instead of dividing.
# Values are otherwise E&F's, line for line.
#
# Scope note: `min = 1` on a `divide = { value = ... }` block clamps the
# divisor, not the result -- so with one or more financial centres the maths
# is bit-identical to E&F's.
#
# Was previously carried by the E&F + PSC compatch. It has nothing to do with
# PSC, so it lives here now.
#############################################

REPLACE_OR_CREATE:target_demand_bond_ajusted = {
	value = target_demand_bond
	subtract = market.mg:bond.market_goods_exports
	divide = { value = building_financial_num min = 1 }

	if = {
		limit = {
			or = {
				is_subject = yes
				not = { market.owner = root }
			}
		}
		multiply = 0.05
	}
	if = { limit = { has_modifier = speculative_share_modifier_2 } multiply = 0.75 }
	if = { limit = { has_modifier = speculative_share_modifier_3 } multiply = 0.5 }
	if = { limit = { has_modifier = speculative_share_modifier_4 } multiply = 0.25 }

	min = 0
}

REPLACE_OR_CREATE:target_demand_bond_for_modifier = {
	value = 0
	if = {
		limit = { base_demande_bond_fix > 0 }
		value = target_demand_bond_ajusted
		subtract = base_demande_bond_fix
		divide = base_demande_bond_fix
		multiply = 100
	}
	max = 25000
}

REPLACE_OR_CREATE:target_demand_manufacture_stock_ajusted = {
	value = target_demand_manufacture_stock
	subtract = market.mg:manufacture_stock.market_goods_exports
	divide = { value = building_financial_num min = 1 }

	if = {
		limit = {
			or = {
				is_subject = yes
				not = { market.owner = root }
			}
		}
		multiply = 0.05
	}
	if = { limit = { has_modifier = speculative_share_modifier_2 } multiply = 0.75 }
	if = { limit = { has_modifier = speculative_share_modifier_3 } multiply = 0.5 }
	if = { limit = { has_modifier = speculative_share_modifier_4 } multiply = 0.25 }

	min = 0
}

REPLACE_OR_CREATE:target_demand_manufacture_stock_for_modifier = {
	value = 0
	if = {
		limit = { base_demande_manufacture_stock_fix > 0 }
		value = target_demand_manufacture_stock_ajusted
		subtract = base_demande_manufacture_stock_fix
		divide = base_demande_manufacture_stock_fix
		multiply = 100
	}
	max = 25000
}

REPLACE_OR_CREATE:target_demand_agricultural_stock_ajusted = {
	value = target_demand_agricultural_stock
	subtract = market.mg:agricultural_stock.market_goods_exports
	divide = { value = building_financial_num min = 1 }

	if = {
		limit = {
			or = {
				is_subject = yes
				not = { market.owner = root }
			}
		}
		multiply = 0.05
	}
	if = { limit = { has_modifier = speculative_share_modifier_2 } multiply = 0.75 }
	if = { limit = { has_modifier = speculative_share_modifier_3 } multiply = 0.5 }
	if = { limit = { has_modifier = speculative_share_modifier_4 } multiply = 0.25 }

	min = 0
}

REPLACE_OR_CREATE:target_demand_agricultural_stock_for_modifier = {
	value = 0
	if = {
		limit = { base_demande_agricultural_stock_fix > 0 }
		value = target_demand_agricultural_stock_ajusted
		subtract = base_demande_agricultural_stock_fix
		divide = base_demande_agricultural_stock_fix
		multiply = 100
	}
	max = 25000
}

REPLACE_OR_CREATE:target_demand_mining_stock_ajusted = {
	value = target_demand_mining_stock
	subtract = market.mg:mining_stock.market_goods_exports
	divide = { value = building_financial_num min = 1 }

	if = {
		limit = {
			or = {
				is_subject = yes
				not = { market.owner = root }
			}
		}
		multiply = 0.05
	}
	if = { limit = { has_modifier = speculative_share_modifier_2 } multiply = 0.75 }
	if = { limit = { has_modifier = speculative_share_modifier_3 } multiply = 0.5 }
	if = { limit = { has_modifier = speculative_share_modifier_4 } multiply = 0.25 }

	min = 0
}

REPLACE_OR_CREATE:target_demand_mining_stock_for_modifier = {
	value = 0
	if = {
		limit = { base_demande_mining_stock_fix > 0 }
		value = target_demand_mining_stock_ajusted
		subtract = base_demande_mining_stock_fix
		divide = base_demande_mining_stock_fix
		multiply = 100
	}
	max = 25000
}

REPLACE_OR_CREATE:target_demand_railroad_stock_ajusted = {
	value = target_demand_railroad_stock
	subtract = market.mg:railroad_stock.market_goods_exports
	divide = { value = building_financial_num min = 1 }

	if = {
		limit = {
			or = {
				is_subject = yes
				not = { market.owner = root }
			}
		}
		multiply = 0.05
	}
	if = { limit = { has_modifier = speculative_share_modifier_2 } multiply = 0.75 }
	if = { limit = { has_modifier = speculative_share_modifier_3 } multiply = 0.5 }
	if = { limit = { has_modifier = speculative_share_modifier_4 } multiply = 0.25 }

	min = 0
}

REPLACE_OR_CREATE:target_demand_railroad_stock_for_modifier = {
	value = 0
	if = {
		limit = { base_demande_railroad_stock_fix > 0 }
		value = target_demand_railroad_stock_ajusted
		subtract = base_demande_railroad_stock_fix
		divide = base_demande_railroad_stock_fix
		multiply = 100
	}
	max = 25000
}

#############################################
# 7d. The same division by zero, in the currency
#
# target_demand_currency_for_modifier is built exactly like the five above --
#
#   value    = target_demand_currency_ajusted
#   subtract = base_demande_currency_fix
#   divide   = base_demande_currency_fix   # <-- can be 0
#   multiply = 100
#   #max = 25000                           # <-- author's own, commented out
#
# -- and it is the one the author left unguarded. base_demande_currency_fix is
# var:base_demande_currency_fix, set to 0 in history/global and only refreshed by
# central_bank_modifier_fixed_var on E&F's pulse. Any moment where it is near zero
# while demand is not -- a bank that was just rebuilt under a new owner, a country
# whose pulse has not come round yet, a central bank scope that is not set when
# base_demande_currency reads it -- turns a percentage into a number with no upper
# bound. It reaches the bank as goods_output_spe_uni_c_mult at 0.01 per point, so
# 100,000 there is a thousandfold multiplier. That is the "Britain and China print
# a million currency now and then".
#
# The currency merge did not create this, but it did widen it: demand is now the
# whole market's, split by GDP, so the numerator on a big market is far larger
# than it was when every country had its own good.
#
# Fix: same shape as the five above, and the max is the author's own number,
# uncommented. With a healthy divisor the maths is bit-identical to E&F's.
#############################################

REPLACE_OR_CREATE:target_demand_currency_for_modifier = {
	value = 0
	if = {
		limit = { base_demande_currency_fix > 0 }
		value = target_demand_currency_ajusted
		subtract = base_demande_currency_fix
		divide = base_demande_currency_fix
		multiply = 100
	}
	max = 25000
}
