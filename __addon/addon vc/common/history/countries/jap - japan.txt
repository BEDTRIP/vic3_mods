# TGR x VC compatch -- country history: jap - japan
# Generated by tools/regen_vc_tgr.py on 2026-08-26. Do not hand-edit: the next run
# overwrites this file. Change the generator instead.
#
# Load order: The Great Revision -> ... -> Victorian Century -> THIS.
# Why this file exists: VC overwrites TGR's file by path. TGR's companies and law changes would vanish; VC's amendments,
#   journal entries and modifiers are kept from VC's side of the merge.
#
# !! MAINTENANCE !! Bodies are a three-way merge with vanilla 1.13.11 as the
# common ancestor. After either mod updates, re-run the generator and read the
# conflict list it prints -- a new conflict means the authors started fighting
# over a line they used to leave alone.

COUNTRIES = {
	c:JAP ?= {
		effect_starting_technology_tier_4_tech = yes

		add_technology_researched = academia
		add_technology_researched = urban_planning
		add_technology_researched = centralization
		add_technology_researched = law_enforcement
		add_technology_researched = colonization
		add_technology_researched = sericulture

		set_tax_level = medium
		
		activate_law = law_type:law_monarchy
		activate_law = law_type:law_bakufu
		activate_law = law_type:law_freedom_of_conscience
		activate_law = law_type:law_serfdom
		activate_law = law_type:law_hereditary_bureaucrats
		activate_law = law_type:law_subjecthood
		activate_law = law_type:law_sakoku
		activate_law = law_type:law_local_police
		activate_law = law_type:law_land_based_taxation
		activate_law = law_type:law_censorship
		activate_law = law_type:law_intermediate_edo_system
		activate_law = law_type:law_closed_borders
		activate_law = law_type:law_terakoya
		activate_law = law_type:law_warrior_caste	

		set_institution_investment_level = {
			institution = institution_police
			level = 1
		}

		ig:ig_landowners ?= {
			add_ruling_interest_group = yes
		}

		active_law:lawgroup_church_and_state ?= { #寺请制度
			add_amendment = {
				type = amendment_danka_system
				sponsor = PREV.ig:ig_devout
			}
		}
		set_variable = amendment_danka_system_var

		if = {
			limit = {
				has_dlc_feature = ep2_content
			}
			add_journal_entry = {
				type = je_sakoku
			}
			add_journal_entry = {
				type = je_tenpo_crisis
			}
			trigger_event = {
				id = tenpo_events.1
				days = 1
				popup = yes
			}
			set_variable = ryukyu_rival_member
			every_scope_state = {
				limit = {
					is_in_geographic_region = geographic_region_honshu
				}
				add_devastation = 5
				state_region = {
					start_harvest_condition_with_params = {
						type = torrential_rains
						duration = 720
						intensity = 5
					}
				}
			}
		}
		
		add_company = company_type:company_mitsui
		company:company_mitsui = {
			set_company_establishment_date = 1817.1.1
			set_company_state_region = s:STATE_KANTO
		}
		
		add_journal_entry = { type = edo_period }
		add_journal_entry = { type = edo_rangaku }
		add_journal_entry = { type = tenpo_famine }

		add_modifier = ikokusen_muninen_uchiharairei
		add_modifier = {
			name = hokkaido_colonization_modifier
			days = very_long_modifier_time
		}
	}
}
