# Grey's x KAI compatch (GR.8) -- restore KAI's ai_value (bureaucracy-backlog construction priority) that grey_usu's REPLACE_OR_CREATE: drops
# Generated by tools/regen_greys_kai_fix.py on 2026-08-27. Do not hand-edit: the
# next run overwrites this file. Change the generator instead.
#
# Load order: ... -> KAI -> ... -> Grey's pack -> THIS.
# Why this file exists: grey_usu redeclares building_government_administration whole (REPLACE_OR_CREATE:) with no ai_value at all -- not a design dispute, the field is just absent. KAI's INJECT: (kai_buildings.txt) adds an ai_value block that makes the AI prioritise building administration where tax capacity is already short. Read live from KAI's own file. Separate from and additive to `_greys/greys+morg done` (GR.6), which restores this same building's pmg_panum_hospital production method group -- different sub-block, same key, no collision.

TRY_INJECT:building_government_administration = {
	ai_value = {
		# KAI: Excess bureaucracy should have lower priority than everything else
		value = 0

		# KAI: Prioritize states with insufficient tax capacity
		if = {
			limit = {
				is_incorporated = yes
			}
			# States that can use more bureaucracy
			add = {
				value = 10
				multiply = {
					# Scale inversely with surplus from -45 to 5
					value = tax_capacity_usage
					subtract = tax_capacity
					add = 5
				}
				min = 0
				max = 500
			}
			# States that can REALLY use more bureaucracy
			add = {
				value = 250
				multiply = tax_capacity_usage
				divide = {
					value = tax_capacity
					min = 100
				}
				min = 0
				max = 500
			}
		}
	}
}
