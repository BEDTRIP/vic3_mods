### ComPatch Grey's x addon-LLWA -- 8 railway companies, addon-LLWA's extension_building_types restored
###
### grey_usu's TRY_REPLACE: on each of these eight companies is a full body
### written before addon-LLWA existed. Re-issuing addon-LLWA's own
### TRY_INJECT: after the whole Grey's pack restores it additively -- grey_usu's
### own extension_building_types items are untouched either way.
### The other six railway companies that also collide with Victorian Century
### (company_great_indian_railway, company_mantetsu, company_orient_express,
### company_panama_company, company_prussian_state_railways,
### company_suez_company) are NOT here -- see zz_gvc_companies.txt in
### _greys/greys+vc done, which now also carries the LLWA layer for those six.
### Generated by tools/regen_greys_llwa.py on 2026-08-27. Do not hand-edit: the
### next run overwrites this file. Change the generator instead.
### Load order: ... -> addon-LLWA -> ... -> the whole Grey's pack -> THIS.

# company_cfr -- grey_usu's TRY_REPLACE: (own extension_building_types list, no LLWA_building_roadway) silently drops addon-LLWA's earlier TRY_INJECT:.
# Re-issued additively: does not touch any of grey_usu's own list items.
TRY_INJECT:company_cfr = {
	extension_building_types = { LLWA_building_roadway }
}

# company_cordoba_railway -- grey_usu's TRY_REPLACE: (own extension_building_types list, no LLWA_building_roadway) silently drops addon-LLWA's earlier TRY_INJECT:.
# Re-issued additively: does not touch any of grey_usu's own list items.
TRY_INJECT:company_cordoba_railway = {
	extension_building_types = { LLWA_building_roadway }
}

# company_egyptian_rail -- grey_usu's TRY_REPLACE: (own extension_building_types list, no LLWA_building_roadway) silently drops addon-LLWA's earlier TRY_INJECT:.
# Re-issued additively: does not touch any of grey_usu's own list items.
TRY_INJECT:company_egyptian_rail = {
	extension_building_types = { LLWA_building_roadway }
}

# company_gwr -- grey_usu's TRY_REPLACE: (own extension_building_types list, no LLWA_building_roadway) silently drops addon-LLWA's earlier TRY_INJECT:.
# Re-issued additively: does not touch any of grey_usu's own list items.
TRY_INJECT:company_gwr = {
	extension_building_types = { LLWA_building_roadway }
}

# company_imperial_ethiopian_railways -- grey_usu's TRY_REPLACE: (own extension_building_types list, no LLWA_building_roadway) silently drops addon-LLWA's earlier TRY_INJECT:.
# Re-issued additively: does not touch any of grey_usu's own list items.
TRY_INJECT:company_imperial_ethiopian_railways = {
	extension_building_types = { LLWA_building_roadway }
}

# company_iranian_state_railway -- grey_usu's TRY_REPLACE: (own extension_building_types list, no LLWA_building_roadway) silently drops addon-LLWA's earlier TRY_INJECT:.
# Re-issued additively: does not touch any of grey_usu's own list items.
TRY_INJECT:company_iranian_state_railway = {
	extension_building_types = { LLWA_building_roadway }
}

# company_sao_paulo_railway -- grey_usu's TRY_REPLACE: (own extension_building_types list, no LLWA_building_roadway) silently drops addon-LLWA's earlier TRY_INJECT:.
# Re-issued additively: does not touch any of grey_usu's own list items.
TRY_INJECT:company_sao_paulo_railway = {
	extension_building_types = { LLWA_building_roadway }
}

# company_tashkent_railroad -- grey_usu's TRY_REPLACE: (own extension_building_types list, no LLWA_building_roadway) silently drops addon-LLWA's earlier TRY_INJECT:.
# Re-issued additively: does not touch any of grey_usu's own list items.
TRY_INJECT:company_tashkent_railroad = {
	extension_building_types = { LLWA_building_roadway }
}
