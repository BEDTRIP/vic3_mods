# Grey's x TGR compatch (GR.7) -- restore TGR's trade-capacity state_modifiers on the 7 ownership PMs not already covered by GR.5
# Generated by tools/regen_greys_tgr.py on 2026-08-27. Do not hand-edit: the next
# run overwrites this file. Change the generator instead.
#
# Load order: TGR -> ... -> Grey's pack -> THIS.
# Why this file exists: REQUIRES BOTH TGR AND the whole Grey's pack. TGR_TRADE_private_infrastructure_investors.txt gives every ownership production method a state_modifiers.level_scaled block (state_weekly_trades_add=0.5, state_trade_capacity_add=1, state_tax_capacity_add=0.25). grey_usu/yMoG_USU_owners_tax_pms.txt REPLACE_OR_CREATEs 21 of these records; 14 pm_company_headquarter_* are already restored by _greys/greys+ef done (GR.5, shared file, needs both TGR and E&F -- not duplicated here). These are the remaining 7. pm_financial_district_privately_owned/publicly_traded/principle_divine_economics_2 and pm_manor_house_bureaucrat_ownership/clergy_ownership carry no state_modifiers at all in grey_usu -- a clean loss, all three TGR fields restored. pm_manor_house_privately_owned and pm_manor_house_principle_divine_economics_2 are a different case the report's first pass over GR.7e missed: grey_usu already gives them their own state_modifiers (workforce_scaled, state_tax_capacity_add=-1) -- a genuine design choice, not silence. Decision 27.08.2026: leave Grey's tax-capacity choice alone, restore only TGR's two fields that have no Grey's equivalent in any sub-block (state_weekly_trades_add, state_trade_capacity_add), added as a sibling level_scaled block next to Grey's workforce_scaled -- the same call the report already made on pm_trade_center (report section 2.2): a record with its own populated sub-block is a dispute, not a loss, and only genuinely absent fields get restored. !! MAINTENANCE !! if grey_usu ever adds its own level_scaled to the two disputed records, or drops state_modifiers from the five clean-loss records, the asserts fail loudly -- re-check which class each record is in before re-running.

REPLACE_OR_CREATE:pm_financial_district_privately_owned = {
	texture = "gfx/interface/icons/production_method_icons/privately_owned.dds"
	replacement_if_valid = pm_financial_district_principle_divine_economics_2
	
	building_modifiers = { 
		level_scaled = {
			building_employment_capitalists_add = 25 # 50
			building_employment_shopkeepers_add = 15 # 25
			building_employment_clerks_add = 10 # 25
		}
		unscaled = {
			building_capitalists_shares_add = 10
			building_shopkeepers_shares_add = 1		# x15 pops = 5.5% of profits
		}
	}
	state_modifiers = {
		level_scaled = {
			state_weekly_trades_add = 0.5  # TGR, restored
			state_trade_capacity_add = 1  # TGR, restored
			state_tax_capacity_add = 0.25  # TGR, restored
		}
	}
}

REPLACE_OR_CREATE:pm_financial_district_publicly_traded = {
	texture = "gfx/interface/icons/production_method_icons/publicly_traded.dds"
	
	unlocking_technologies = { mutual_funds }
	
	building_modifiers = { 
		level_scaled = {
			building_employment_capitalists_add = 25 # 50
			building_employment_bureaucrats_add = 15 # 25
			building_employment_clerks_add = 10 # 25
		}
		unscaled = {
			building_capitalists_shares_add = 10
			building_bureaucrats_shares_add = 1		# x15 pops = 5.5% of profits
		}			
	}	
	state_modifiers = {
		level_scaled = {
			state_weekly_trades_add = 0.5  # TGR, restored
			state_trade_capacity_add = 1  # TGR, restored
			state_tax_capacity_add = 0.25  # TGR, restored
		}
	}
}

REPLACE_OR_CREATE:pm_financial_district_principle_divine_economics_2 = {
	texture = "gfx/interface/icons/production_method_icons/privately_owned.dds"
	is_hidden_when_unavailable = yes
	
	unlocking_principles = {
		principle_divine_economics_2
		principle_divine_economics_3
	}

	building_modifiers = { 
		level_scaled = {
			building_employment_capitalists_add = 20 # 50
			building_employment_clergymen_add = 20 # 25
			building_employment_clerks_add = 10 # 25
		}
		unscaled = {
			building_capitalists_shares_add = 10	# 20 pops share 83.3% (4.2% ea) instead of 25 sharing 95% (3.8% ea)
			building_clergymen_shares_add = 2		# x20 total pops = 16.7% of profits (0.84%ea up from 0.37%ea)		- Church doesn't win quite as much here
		}
	}
	state_modifiers = {
		level_scaled = {
			state_weekly_trades_add = 0.5  # TGR, restored
			state_trade_capacity_add = 1  # TGR, restored
			state_tax_capacity_add = 0.25  # TGR, restored
		}
	}
}

REPLACE_OR_CREATE:pm_manor_house_bureaucrat_ownership = {
	texture = "gfx/interface/icons/production_method_icons/ownership_bureacrats.dds"

	unlocking_laws = {
		law_state_atheism
	}
	
	building_modifiers = {
		level_scaled = {
			building_employment_bureaucrats_add = 15 # 100
			building_employment_clerks_add = 5 # 0
		}
		unscaled = {
			building_bureaucrats_shares_add = 1		# x15 pops = 4.7% of profits
		}		
	}	
	state_modifiers = {
		level_scaled = {
			state_weekly_trades_add = 0.5  # TGR, restored
			state_trade_capacity_add = 1  # TGR, restored
			state_tax_capacity_add = 0.25  # TGR, restored
		}
	}
}

REPLACE_OR_CREATE:pm_manor_house_clergy_ownership = {
	texture = "gfx/interface/icons/production_method_icons/ownership_clergy.dds"

	disallowing_laws = {
		law_state_atheism
	}

	building_modifiers = {
		level_scaled = {
			building_employment_clergymen_add = 15 # 100
			building_employment_clerks_add = 5 # 0
		}
		unscaled = {
			building_clergymen_shares_add = 1		# x15 pops = 4.7% of profits
		}
	}
	state_modifiers = {
		level_scaled = {
			state_weekly_trades_add = 0.5  # TGR, restored
			state_trade_capacity_add = 1  # TGR, restored
			state_tax_capacity_add = 0.25  # TGR, restored
		}
	}
}

REPLACE_OR_CREATE:pm_manor_house_privately_owned = {
	texture = "gfx/interface/icons/production_method_icons/ownership_aristocrats.dds"
	replacement_if_valid = pm_manor_house_principle_divine_economics_2

	state_modifiers = {
		workforce_scaled = {
			state_tax_capacity_add = -1
		}
		level_scaled = {
			state_weekly_trades_add = 0.5  # TGR, restored
			state_trade_capacity_add = 1  # TGR, restored
		}
	}	
	
	building_modifiers = { 
		level_scaled = {
			building_employment_aristocrats_add = 30 # 50
		}
		unscaled = {
			building_aristocrats_shares_add = 10
		}
	}
}

REPLACE_OR_CREATE:pm_manor_house_principle_divine_economics_2 = {
	texture = "gfx/interface/icons/production_method_icons/privately_owned.dds"
	is_hidden_when_unavailable = yes
	
	unlocking_principles = {
		principle_divine_economics_2
		principle_divine_economics_3
	}
	
	state_modifiers = {
		workforce_scaled = {
			state_tax_capacity_add = -1
		}
		level_scaled = {
			state_weekly_trades_add = 0.5  # TGR, restored
			state_trade_capacity_add = 1  # TGR, restored
		}
	}	
	
	building_modifiers = { 
		level_scaled = {
			building_employment_aristocrats_add = 25 # 50	# This is only pm that gives >50 total (+10% vs vanilla +33% total)
			building_employment_clergymen_add = 10 # 50
		}
		
		unscaled = {
			building_aristocrats_shares_add = 10	# 25 pops share 77% (3.1% ea) instead of 30 sharing 95% (3.2% ea)
			building_clergymen_shares_add = 2		# x25 total pops = 23% of profits (0.92%ea up from 0.31%ea)
		}
	}
}
