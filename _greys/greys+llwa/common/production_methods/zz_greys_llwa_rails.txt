### ComPatch Grey's x addon-LLWA -- 3 rail production methods, building_job_attractiveness_mult restored
###
### grey_usu's REPLACE_OR_CREATE: on these three keeps its own numbers on every
### field it touches -- this restores only the one field it never had:
### building_modifiers.unscaled.building_job_attractiveness_mult = 2, originally
### VC's, silently dropped the same way on every full-body rewrite since.
### The other seven rail production methods are NOT here on purpose: grey_usu has
### its own progressively-scaled state_market_access_price_impact design on all
### seven (0.02..0.05) -- a genuine numeric dispute, not a silent loss. See the
### plan, GR.16.
### Generated by tools/regen_greys_llwa.py on 2026-08-27. Do not hand-edit: the
### next run overwrites this file. Change the generator instead.
### Load order: ... -> addon-LLWA -> ... -> the whole Grey's pack -> THIS.

# pm_no_passenger_trains -- grey_usu's REPLACE_OR_CREATE:, own numbers kept verbatim,
# only building_modifiers.unscaled.building_job_attractiveness_mult = 2 restored
# (originally VC's; already restored against LLWA's own body in llwa+vc done --
# same field, same value, this time against grey_usu's later body).
REPLACE_OR_CREATE:pm_no_passenger_trains = {
	texture = "gfx/interface/icons/production_method_icons/no_passenger_trains.dds"
	building_modifiers = {
		workforce_scaled = {
			goods_input_usu_train_paths_add = 1		#-200	# Putting here should keep the AI behaviour clean.
			goods_output_transportation_add = 6		#+180	# Full utilisation of train paths should be limiting.
			goods_output_usu_logistics_add = 6		#+60	# No passengers frees up capacity early-game when logistics is expensive AF.
		}
	unscaled = {
		building_job_attractiveness_mult = 2
	}
}
}

# pm_steel_passenger_carriages -- grey_usu's REPLACE_OR_CREATE:, own numbers kept verbatim,
# only building_modifiers.unscaled.building_job_attractiveness_mult = 2 restored
# (originally VC's; already restored against LLWA's own body in llwa+vc done --
# same field, same value, this time against grey_usu's later body).
REPLACE_OR_CREATE:pm_steel_passenger_carriages = {
	texture = "gfx/interface/icons/production_method_icons/passenger_carriages.dds"

	unlocking_technologies = {
		steel_railway_cars
	}

	building_modifiers = {
		workforce_scaled = {
			goods_input_usu_train_paths_add = 1		#-200	# Putting here should keep the AI behaviour clean.
			goods_output_transportation_add = 6		#+180	# Full utilisation of train paths should be limiting.
			goods_input_steel_add = 5
			goods_output_transportation_add = 15
		}
		level_scaled = {
		#	building_employment_clerks_add = 500
		#	building_employment_laborers_add = -500
		#	building_employment_machinists_add = -300
		building_employment_clerks_add = 200
		}
	unscaled = {
		building_job_attractiveness_mult = 2
	}
}
	state_modifiers = {
		level_scaled = {
			building_urban_center_throughput_add = 0.015
		}
	}
}

# pm_wooden_passenger_carriages -- grey_usu's REPLACE_OR_CREATE:, own numbers kept verbatim,
# only building_modifiers.unscaled.building_job_attractiveness_mult = 2 restored
# (originally VC's; already restored against LLWA's own body in llwa+vc done --
# same field, same value, this time against grey_usu's later body).
REPLACE_OR_CREATE:pm_wooden_passenger_carriages = {
	texture = "gfx/interface/icons/production_method_icons/passenger_trains.dds"

	building_modifiers = {
		workforce_scaled = {
			goods_input_usu_train_paths_add = 1		#-200	# Putting here should keep the AI behaviour clean.
			goods_output_transportation_add = 6		#+180	# Full utilisation of train paths should be limiting.
			goods_input_wood_add = 6				#-120
			goods_output_transportation_add = 10	#+300
		}
		level_scaled = {
		#	building_employment_clerks_add = 250
		#	building_employment_laborers_add = -500
		#	building_employment_machinists_add = -100
		building_employment_clerks_add = 100
		}
	unscaled = {
		building_job_attractiveness_mult = 2
	}
}
	state_modifiers = {
		level_scaled = {
			building_urban_center_throughput_add = 0.01
		}
	}
}
