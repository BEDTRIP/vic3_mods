# Grey's x addon-VC compatch (GR.18) -- restore the air_travel entry on popneed_leisure that grey_usu's author left unfinished
# Generated by tools/regen_greys_addonvc.py on 2026-08-27. Do not hand-edit: the
# next run overwrites this file. Change the generator instead.
#
# Load order: ... -> addon-VC -> ... -> Grey's pack -> THIS.
# Why this file exists: grey_usu's REPLACE:popneed_leisure is a deliberate redesign (fine_art / wine / transportation / usu_logistics / merchant_marine / small_arms / aeroplanes / clippers / steamers), but its own source carries an air_travel entry commented OUT with "Gets added with Morgenrote" -- the author relied on Morgenroete's original TRY_INJECT: still applying, which a REPLACE: wipes. grey_usu's own follow-up TRY_INJECT: (for elgar_music) shows the author knows this record needs a Morgenrote top-up; it only restores elgar_music, not air_travel. addon-VC's own Morgenroete-compat file carries the exact entry -- read live from there rather than hand-copied, so a future addon-VC rebalance is caught by drift. popneed_free_movement, popneed_heating and popneed_luxury_food are NOT here -- see the module docstring, checked and not silent losses.

# grey_usu's own REPLACE: (mMoG_USU_pop_needs.txt) leaves this entry commented OUT
# with the note "Gets added with Morgenrote" -- the author expected Morgenroete's own
# TRY_INJECT: to still apply after the REPLACE:. It doesn't (REPLACE: wipes it), and
# grey_usu's own follow-up TRY_INJECT: (zzzMoG_USU_MR_pop_needs.txt) restores only
# elgar_music, never this. Read live from addon-VC's own Morgenroete-compat file.
TRY_INJECT:popneed_leisure = {
	entry = {
		goods = air_travel
		
		weight = 1.0 # New Tech
		max_supply_share = 0.25 # Keeping, radio-esque		# This is what they get instead of buying private planes...
		min_supply_share = 0.0
	}
}
