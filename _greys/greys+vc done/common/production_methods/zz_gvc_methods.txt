### === VC-ONLY FILE === delete "zz_gvc_methods.txt" to play without
### Victorian Century. This whole compatch is the VC layer -- deleting
### this file changes nothing else. To drop VC entirely, delete this
### whole compatch folder (_greys/greys+vc done) and nothing else --
### no other Grey's compatch references it.

### ComPatch Grey's x Victorian Century -- 10 production methods VC injects into and grey_usu re-issues whole
###
### grey_usu rebuilds the construction methods, the three subsistence defaults
### and the passenger-train methods with full bodies, so VC's injects into the
### same records are gone. These are VC's own inject bodies, copied byte for byte
### and loaded after grey_usu: re-issuing a foreign INJECT: over a later body is
### exact restoration, and it does not depend on how the open question about
### sub-block semantics is answered (Правила работы с модами, Переопределение).
###
### Generated by tools/regen_greys_vc.py on 2026-08-27. Do not hand-edit: the next
### run overwrites this file. Change the generator instead.
### Load order: ... -> Victorian Century -> ... -> the whole Grey's pack -> THIS.

# default_building_subsistence_farm -- USU re-issues the whole record in common/production_methods/yMoG_USU_road_carts_subsistence.txt (TRY_REPLACE),
# which drops this inject of VC's (common/production_methods/joi_methods.txt). Verbatim copy, nothing added.
INJECT:default_building_subsistence_farm = {
	building_modifiers = {
		level_scaled = {
			building_employment_peasants_add = 1500
		}
	}
}

# default_building_subsistence_orchard -- USU re-issues the whole record in common/production_methods/yMoG_USU_road_carts_subsistence.txt (TRY_REPLACE),
# which drops this inject of VC's (common/production_methods/joi_methods.txt). Verbatim copy, nothing added.
INJECT:default_building_subsistence_orchard = {
	building_modifiers = {
		level_scaled = {
			building_employment_peasants_add = 1500
		}
	}
}

# default_building_subsistence_rice_farm -- USU re-issues the whole record in common/production_methods/yMoG_USU_road_carts_subsistence.txt (TRY_REPLACE),
# which drops this inject of VC's (common/production_methods/joi_methods.txt). Verbatim copy, nothing added.
INJECT:default_building_subsistence_rice_farm = {
	building_modifiers = {
		level_scaled = {
			building_employment_peasants_add = 2500
		}
	}
}

# pm_arc_welded_buildings -- USU re-issues the whole record in common/production_methods/yMoG_USU_construction.txt (REPLACE_OR_CREATE),
# which drops this inject of VC's (common/production_methods/joi_methods.txt). Verbatim copy, nothing added.
INJECT:pm_arc_welded_buildings = {
	building_modifiers = {
		workforce_scaled = {
			goods_input_steel_add = 10
			goods_input_glass_add = 10
			goods_input_explosives_add = 5
			goods_input_tools_add = 10
		}
	}

	state_modifiers = {
		workforce_scaled = {
			state_construction_mult = -0.002
		}
	}
}

# pm_iron_frame_buildings -- USU re-issues the whole record in common/production_methods/yMoG_USU_construction.txt (REPLACE_OR_CREATE),
# which drops this inject of VC's (common/production_methods/joi_methods.txt). Verbatim copy, nothing added.
INJECT:pm_iron_frame_buildings = {
	building_modifiers = {
		workforce_scaled = {
			goods_input_wood_add = 10
			goods_input_fabric_add = 5
			goods_input_iron_add = 10
			goods_input_tools_add = 5
		}
	}

	state_modifiers = {
		workforce_scaled = {
			state_construction_mult = -0.002
		}
	}
}

# pm_no_passenger_trains -- USU re-issues the whole record in common/production_methods/yMoG_USU_rail_other_pms.txt (REPLACE_OR_CREATE),
# which drops this inject of VC's (common/production_methods/joi_methods.txt). Verbatim copy, nothing added.
INJECT:pm_no_passenger_trains = {
	building_modifiers = {
		unscaled = {
			building_job_attractiveness_mult = 2
		}
	}
}

# pm_steel_frame_buildings -- USU re-issues the whole record in common/production_methods/yMoG_USU_construction.txt (REPLACE_OR_CREATE),
# which drops this inject of VC's (common/production_methods/joi_methods.txt). Verbatim copy, nothing added.
INJECT:pm_steel_frame_buildings = {
	building_modifiers = {
		workforce_scaled = {
			goods_input_steel_add = 10
			goods_input_glass_add = 10
			goods_input_tools_add = 5
		}
	}

	state_modifiers = {
		workforce_scaled = {
			state_construction_mult = -0.003
		}
	}
}

# pm_steel_passenger_carriages -- USU re-issues the whole record in common/production_methods/yMoG_USU_rail_other_pms.txt (REPLACE_OR_CREATE),
# which drops this inject of VC's (common/production_methods/joi_methods.txt). Verbatim copy, nothing added.
INJECT:pm_steel_passenger_carriages = {
	building_modifiers = {
		unscaled = {
			building_job_attractiveness_mult = 2
		}
	}
}

# pm_wooden_buildings -- USU re-issues the whole record in common/production_methods/yMoG_USU_construction.txt (REPLACE_OR_CREATE),
# which drops this inject of VC's (common/production_methods/joi_methods.txt). Verbatim copy, nothing added.
INJECT:pm_wooden_buildings = {
	building_modifiers = {
		workforce_scaled = {
			goods_input_fabric_add = 5
			goods_input_wood_add = 15
		}
	}

	state_modifiers = {
		workforce_scaled = {
			state_construction_mult = -0.001
		}
	}
}

# pm_wooden_passenger_carriages -- USU re-issues the whole record in common/production_methods/yMoG_USU_rail_other_pms.txt (REPLACE_OR_CREATE),
# which drops this inject of VC's (common/production_methods/joi_methods.txt). Verbatim copy, nothing added.
INJECT:pm_wooden_passenger_carriages = {
	building_modifiers = {
		unscaled = {
			building_job_attractiveness_mult = 2
		}
	}
}
