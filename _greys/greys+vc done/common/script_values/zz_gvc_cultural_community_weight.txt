### === VC-ONLY FILE === delete "zz_gvc_cultural_community_weight.txt" to play without
### Victorian Century. This whole compatch is the VC layer -- deleting
### this file changes nothing else. To drop VC entirely, delete this
### whole compatch folder (_greys/greys+vc done) and nothing else --
### no other Grey's compatch references it.

### ComPatch Grey's x Victorian Century -- cultural_community_creation_weight -- VC's port traits
###
### _grey_soft_pop rewrites this script value (TRY_REPLACE:, whole body:
### building scopes instead of has_port_state, if/else_if chains instead of
### stacked multipliers) and wins outright. Its harbour bonus still lists only
### the six vanilla harbour traits, so every port trait VC adds counts for
### nothing. This is soft_pop's body with VC's trait names added to that one
### list -- and nothing else: the numbers in this script are soft_pop's redesign
### and stay its own, the same rule the 60 state traits follow.
### NOTE: a plain repeat of a script_value key does NOT override -- hence
### REPLACE_OR_CREATE: here (Правила работы с модами, Переопределение).
###
### Generated by tools/regen_greys_vc.py on 2026-08-27. Do not hand-edit: the next
### run overwrites this file. Change the generator instead.
### Load order: ... -> Victorian Century -> ... -> the whole Grey's pack -> THIS.

REPLACE_OR_CREATE:cultural_community_creation_weight = {

	# Conditions that block

	if = {
		limit = {
			owner = {
				country_has_primary_culture = scope:culture
			}
			has_decree = decree_greener_grass_campaign
			is_isolated_from_market = no
		}
		value = 1
	}
	else_if = {
		limit = {
			OR = {
				devastation >= 0.5
				turmoil >= 0.6
				is_isolated_from_market = yes
			}
		}
		value = 0
	}
	else = {
		add = 0.001
	
		# Conditions that add
		# if = {
			b:building_port ?= {
				# Base amount for having a port
				add = 0.002
				
				# Additional amount per level
				add = {
					add = 0.002
					multiply = level
				}
			}
		# }
		
		if = {
			limit = {
				has_decree = decree_greener_grass_campaign
			}
			add = 0.005
		}
	
		if = {
			limit = {
				on_river = yes
			}
			add = 0.001
		}
	
		if = {
			limit = {
				is_coastal = yes
			}
			add = 0.002
		}
	
		if = {
			limit = {
				is_under_colonization = yes
			}
			add = 0.005
		}
	
		if = {
			limit = {
				free_arable_land > 20
			}
			add = 0.001
		}

		b:building_trade_center ?= {
			add = {
				add = 0.001
				multiply = level
			}
			#Result: add = 0.001 per building
		}

		# Positive Conditions that Multiply
	
		if = {
			limit = {
				is_capital = yes
			}
			multiply = 1.1
		}
		
#		# This does nothing due to the opening block.
#		if = {
#			limit = {
#				owner = {
#					country_has_primary_culture = scope:culture
#				}
#			}
#			multiply = 2.5
#		}
	
		if = {
			limit = {
				OR = {
					has_state_trait = state_trait_natural_harbors
					has_state_trait = state_trait_kiel_port	# VC
					has_state_trait = state_trait_shanghai_port	# VC
					has_state_trait = state_trait_sydney_port	# VC
					has_state_trait = state_trait_persia_port	# VC
					has_state_trait = state_trait_basrah_port	# VC
					has_state_trait = state_trait_golden_gate	# VC
					has_state_trait = state_trait_ice_free_port	# VC
					has_state_trait = state_trait_alexandria_port	# VC
					has_state_trait = state_trait_genoa_port	# VC
					has_state_trait = state_trait_venice_port	# VC
					has_state_trait = state_trait_belfast_port	# VC
					has_state_trait = state_trait_pacific_port	# VC
					has_state_trait = state_trait_holy_city	# VC
					has_state_trait = state_trait_bosphorus_srait	# VC
					has_state_trait = state_trait_chesapeake_bay	# VC
					has_state_trait = state_trait_portland_port	# VC
					has_state_trait = state_trait_invergordon_port	# VC
					has_state_trait = state_trait_good_soils
					has_state_trait = state_trait_london_docklands
					has_state_trait = state_trait_new_york_harbor
					has_state_trait = state_trait_halifax_harbor
					has_state_trait = state_trait_newark_harbor
				}
	
			}
			multiply = 1.2
		}
		
		# Negative Conditions that multiply 
		
		if = {
			limit = {
				owner = {
					cultural_acceptance_base = { target = scope:culture value <= acceptance_status_2 }
				}
			}
			if = {
				limit = {
					has_decree = decree_violent_suppression
				}
				multiply = 0.5
			}
			if = {
				limit = {
					is_slave_state = yes
					scope:culture = {
						NOT = { shares_heritage_trait_group_with_any_primary_culture = root.owner }
					}
				}
				multiply = 0.1
			}
		}
		
		if = {
			limit = {
				devastation >= 0.2
			}
			multiply = 0.5
		}
	
		if = {
			limit = {
				turmoil >= 0.3
			}
			multiply = 0.5
		}
	
#		# Unnecessary with revised migration mechanics
#		if = {
#			limit = {
#				state_unemployment_rate >= 0.1
#			}
#			multiply = 0.5
#		}

		if = {
			limit = {
				owner = {
					is_at_war = yes
				}
			}
			multiply = 0.5
		}
	
		if = {
			limit = {
				state_has_goods_shortage = yes
			}
			multiply = 0.8
		}
	
		multiply = market_access
	
		if = {
			limit = {
				is_in_revolt = yes
			}
			multiply = 0.5
		}
	
		if = {
			limit = {
				has_state_trait = state_trait_malaria
				root.owner = {
					NOT = {
						has_technology_researched = quinine
					}
				}
			}
			multiply = 0.8
		}
	
		if = {
			limit = {
				has_state_trait = state_trait_severe_malaria
				root.owner = {
					NOT = {
						has_technology_researched = malaria_prevention
					}
				}
			}
			multiply = 0.6
		}
		
		if = {
			limit = {
				root.owner = {
					cultural_acceptance_base = { target = scope:culture value < acceptance_status_2 }
				}
			}
			multiply = 0.1
		}
		else_if = {
			limit = {
				root.owner = {
					cultural_acceptance_base = { target = scope:culture value < acceptance_status_3 }
				}
			}
			multiply = 0.3
		}
		else_if = {
			limit = {
				root.owner = {
					cultural_acceptance_base = { target = scope:culture value < acceptance_status_4 }
				}
			}
			multiply = 0.5
		}
		else_if = {
			limit = {
				root.owner = {
					cultural_acceptance_base = { target = scope:culture value < acceptance_status_5 }
				}
			}
			multiply = 0.7
		}
		
		# Brought together in an if-else block so they don't check every option every time.
		if = {
			limit = {
				num_cultural_communities <5
			}
			multiply = 1
		}
		else_if = {
			limit = {
				AND = {
					num_cultural_communities >=5
					num_cultural_communities <10
				}
			}
			multiply = 0.6
		}
		else_if = {
			limit = {
				AND = {
					num_cultural_communities >=10
					num_cultural_communities <15
				}
			}
			multiply = 0.4
		}
		else_if = {
			limit = {
				AND = {
					num_cultural_communities >=15
					num_cultural_communities <21
				}
			}
			multiply = 0.2
		}
		else_if = {
			limit = {
				AND = {
					num_cultural_communities >=21
					num_cultural_communities <24
				}
			}
			multiply = 0.1
		}
		else_if = {
			limit = {
				num_cultural_communities >=24
			}
			multiply = 0.02
		}
	}

	# Bounds:
	max = define:NPops|CULTURAL_COMMUNITY_MAX_PER_MONTH_CHANCE
	min = 0
}
