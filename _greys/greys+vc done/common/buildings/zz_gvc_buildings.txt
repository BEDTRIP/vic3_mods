### === VC-ONLY FILE === delete "zz_gvc_buildings.txt" to play without
### Victorian Century. This whole compatch is the VC layer -- deleting
### this file changes nothing else. To drop VC entirely, delete this
### whole compatch folder (_greys/greys+vc done) and nothing else --
### no other Grey's compatch references it.

### ComPatch Grey's x Victorian Century -- can_build_private on the food industry and the livestock ranch
###
### VC gates private construction of these two behind its own
### zw_trigger_autonom_investment; grey_food and grey_food_2_ranch re-issue both
### buildings with REPLACE_OR_CREATE: and full bodies, so the gate disappears and
### the two buildings behave differently from every other building VC gates.
### VC's own TRY_INJECT:, copied verbatim.
###
### Generated by tools/regen_greys_vc.py on 2026-08-27. Do not hand-edit: the next
### run overwrites this file. Change the generator instead.
### Load order: ... -> Victorian Century -> ... -> the whole Grey's pack -> THIS.

# building_food_industry -- food re-issues the whole record in common/buildings/mog_food_industry.txt (REPLACE_OR_CREATE),
# which drops this inject of VC's (common/buildings/zw_pioeeb_01_industry.txt). Verbatim copy, nothing added.
TRY_INJECT:building_food_industry = {
	can_build_private = {
		zw_trigger_autonom_investment = {
			building_1 = building_food_industry
		}
	}
}

# building_livestock_ranch -- ranch re-issues the whole record in common/buildings/mog_ranch.txt (REPLACE_OR_CREATE),
# which drops this inject of VC's (common/buildings/zw_pioeeb_02_agro.txt). Verbatim copy, nothing added.
TRY_INJECT:building_livestock_ranch = {
	can_build_private = {
		zw_trigger_autonom_investment = {
			building_1 = building_livestock_ranch
		}
	}
}
