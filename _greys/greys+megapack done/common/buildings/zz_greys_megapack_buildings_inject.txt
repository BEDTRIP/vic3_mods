# Grey's x megapack `no t&r` compatch (GR.17) -- restore E&F x Morgenroete's groups on two buildings grey_usu re-declares whole
# Generated by tools/regen_greys_megapack.py on 2026-08-27. Do not hand-edit: the
# next run overwrites this file. Change the generator instead.
#
# Load order: ... -> megapack `no t&r` -> ... -> Grey's pack -> THIS.
# NOT for players using addon-LLWA on building_airport -- see the module
# docstring and the README: use _greys/greys+llwa done instead for that
# record, or you double-inject the two E&F groups onto it.
# Why this file exists: the megapack already carries E&F x Morgenroete's own TRY_INJECT: of these groups (from ef+morg done, folded into the megapack build). grey_usu's TRY_REPLACE: of both records loads after the whole Grey's pack and does not name either group, so both buildings drop out of E&F's liquidity/stock system silently. building_construction_sector and the four pm_*_buildings construction methods, the other 5 of content_holes.py's 7 reported keys here, are NOT in this file -- `_greys/greys+psc done` (GR.2) already restores them and wins outright, loading after the whole Grey's pack same as this file. building_airport is mutually exclusive with `_greys/greys+llwa done` -- see the module docstring.

# what each building loses and to whose body:
#   building_airport               loses ['pmg_market_liquidity', 'pmg_private_ownership_manufacture_stock'] to zMoG_USU_MR_airports.txt
#   building_dubois_national_park  loses ['pmg_market_liquidity'] to zMoG_USU_MR_nat_park.txt

TRY_INJECT:building_airport = {
	production_method_groups = {
		pmg_market_liquidity
		pmg_private_ownership_manufacture_stock
	}
}

TRY_INJECT:building_dubois_national_park = {
	production_method_groups = {
		pmg_market_liquidity
	}
}
