# usu_llwa out outdate

<!-- meta
пара: USU × LLWA
статус: out outdate
версии: —
позиция: —
файлов: 13
генератор: —
зависит от: —
-->

Чужой компач автора Grey's (`USU + LLWA Compatch`, id 3387021675), распакован как источник.

`version` и `supported_game_version` в `metadata.json` пустые. 11 ключей, все голыми телами без префиксов:
`building_railway`, `building_airport`, `LLWA_building_roadway`, `LLWA_building_waterway`, `LLWA_building_airway`,
`pmg_gaudi_communication`, `pmg_tourism_airport`, `pm_gaudi_no_communication`, `pm_luxury_requisitions`,
`pm_travel_agencies`, `LLWA_active`. **`LLWA_building_riverway` не покрыт вовсе.**

Пересобирается в задаче GR.9, см. `План проекта.md`.
