# Grey's x TGR compatch (GR.7) -- re-issue TGR's acceptance-mult counterweight on four citizenship laws after Grey's later TRY_REPLACE
# Generated by tools/regen_greys_tgr.py on 2026-08-27. Do not hand-edit: the next
# run overwrites this file. Change the generator instead.
#
# Load order: TGR -> ... -> Grey's pack -> THIS.
# Why this file exists: vanilla gives country_political_strength_full_acceptance_mult = 0.25/0.20/0.1/0.1 on these four laws; TGR INJECTs the same numbers negated to net the bonus to zero. _grey_soft_pop's later TRY_REPLACE restates the vanilla (positive) values, and a modifier block's repeated keys sum -- so TGR's INJECT would need to land on top of a body that still carries the vanilla value to work, and Grey's body does, so simply re-issuing TGR's own INJECT after the whole Grey's pack nets back to zero exactly (rules file, section 1: 'counterweight with the opposite sign gets silently resurrected'). !! MAINTENANCE !! this only works because Grey's value still equals vanilla -- if _grey_soft_pop ever changes these four numbers on purpose, this stops being a restore and becomes a balance decision; the generator's assert catches exactly that.

INJECT:law_ethnostate = {
	acceptance_modifier = {
		country_political_strength_full_acceptance_mult = -0.25
	}
}

INJECT:law_national_supremacy = {
	acceptance_modifier = {
		country_political_strength_full_acceptance_mult = -0.20
	}
}

INJECT:law_racial_segregation = {
	acceptance_modifier = {
		country_political_strength_full_acceptance_mult = -0.1
	}
}

INJECT:law_cultural_exclusion = {
	acceptance_modifier = {
		country_political_strength_full_acceptance_mult = -0.1
	}
}
