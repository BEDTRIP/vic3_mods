# Grey's x TGR compatch (GR.7) -- restore four defines TGR deliberately changed and Grey's silently reverts to vanilla
# Generated by tools/regen_greys_tgr.py on 2026-08-27. Do not hand-edit: the next
# run overwrites this file. Change the generator instead.
#
# Load order: TGR -> ... -> Grey's pack -> THIS.
# Why this file exists: defines merge per key, last mod loaded wins (rules file, section 1). On these four keys _grey_soft_econ / _grey_soft_pop just restate the vanilla default -- not a decision, a no-op -- while TGR made a deliberate change on each. Loading later, Grey's plain vanilla restatement wins and TGR's choice is gone with nothing in error.log. PRICE_RANGE and BUY_SELL_DIFF_AT_MAX_FACTOR are NOT here -- decision point #3, soft_econ's own values (0.9 / 3) were kept on purpose, see the plan and report section 5.

NEconomy = {
	COMPANY_MINIMUM_LEVELS_PER_HQ = 1
	AUTO_DOWNSIZE_BUILDING_MIN_UNUSED_TRADE_CAPACITY = 100
}

NPops = {
	MAX_DEMAND_ADJUSTMENT_BASE_AMOUNT = 0.05
	MAX_DEMAND_ADJUSTMENT_SCALED_AMOUNT = 0.10
}
