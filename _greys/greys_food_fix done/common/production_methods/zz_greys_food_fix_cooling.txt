# Grey's internal fix (GR.14) -- restore two transport-cost fields grey_food's TRY_REPLACE: drops from grey_usu's bodies
# Generated by tools/regen_greys_food_fix.py on 2026-08-27. Do not hand-edit: the
# next run overwrites this file. Change the generator instead.
#
# Load order: grey_usu -> ... -> grey_food -> THIS.
# Why this file exists: grey_food redeclares nine cooling production methods that grey_usu also declares, in sync for seven of them. Two aren't: pm_refrigerated_storage_building_fishing_wharf loses goods_input_transportation_add, pm_unrefrigerated loses its whole building_modifiers block (same field). Neither is a design dispute -- grey_food's body is a strict subset missing the field, not a redone number. Restored via TRY_INJECT: so a future grey_food edit that re-adds the field on its own does not double it up silently (the generator would still run, just add a redundant but harmless line -- re-check by hand if grey_food's body ever changes here).

# grey_food's own TRY_REPLACE: bodies for these two keys drop a field grey_usu's
# TRY_REPLACE: added -- grey_food loads later and wins the whole key, so the field
# silently disappears. Both values read live from grey_usu's own file.
TRY_INJECT:pm_refrigerated_storage_building_fishing_wharf = {
	building_modifiers = {
		workforce_scaled = {
			goods_input_transportation_add = 5
		}
	}
}

TRY_INJECT:pm_unrefrigerated = {
	building_modifiers = {
		workforce_scaled = {
			goods_input_transportation_add = 1
		}
	}
}
