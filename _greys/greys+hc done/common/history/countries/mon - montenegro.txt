# Grey's x HC+GoB+MoH compatch (GR.4) -- reconcile the shared file path
# common/history/countries/mon - montenegro.txt
# Generated by tools/regen_greys_hc.py on 2026-08-27. Do not hand-edit: the next run
# overwrites this file. Change the generator instead.
#
# grey_usu and Gates of Bosphorus both ship a file at this exact relative path. It is not
# REPLACE:-prefixed -- this is a raw virtual-filesystem path collision, not a by-key merge:
# whichever mod loads last provides the ONLY copy the game reads, the earlier one is
# invisible, not merged. grey_usu (Grey's block) loads after the whole HC+GoB+MoH block, so
# its 94-line body wins outright and GoB's 102-line body -- three new starting techs, a new
# journal entry (je_balkfm_mon_prince_bishopric), GoB's own IP3 state-formation integration,
# and an else-branch event (balkfm_montenegro.001/.019) -- never runs at all.
#
# Base here: GoB's body (vanilla is 93 lines, GoB's is the substantial, deliberate rework;
# USU's 94-line delta from vanilla is one new line and one changed good). USU's own two
# changes layered on top: landlocked Montenegro's first Trade Center tariff good redirected
# from g:merchant_marine to USU's own g:usu_logistics, and set_military_wage_level = low
# added after set_tax_level.

COUNTRIES = {
	c:MON ?= {
		effect_starting_technology_tier_4_tech = yes
		add_technology_researched = urban_planning
		add_technology_researched = law_enforcement
		add_technology_researched = currency_standards

		ig:ig_armed_forces = {
			add_ruling_interest_group = yes
		}
		ig:ig_landowners = {
			add_ruling_interest_group = yes
		}
		
		activate_law = law_type:law_theocracy
		activate_law = law_type:law_state_religion
		activate_law = law_type:law_national_supremacy
		activate_law = law_type:law_tenant_farmers
		activate_law = law_type:law_traditionalism
		activate_law = law_type:law_censorship
		activate_law = law_type:law_consumption_based_taxation
		activate_law = law_type:law_women_in_the_fields # Not IP3 gated?
		activate_law = law_type:law_chiefs_distribute_aid # Not IP3 gated?
		set_institution_investment_level = {
			institution = institution_social_security
			level = 4
		}
		
		add_journal_entry = {
			type = je_balkfm_mon_prince_bishopric
		}

		## IP3 Integration
		if = {
			limit = {
				has_dlc_feature = ip3_content # National Awakening
			}
			# Initialize the State Formation content from IP3
			add_modifier = {
				name = modifier_women_work_men_fight
				months = gbbf_stupidly_long_modifier_time
				is_decaying = yes
			}
			add_modifier = {
				name = modifier_lonely_mountain
				months = even_longer_modifier_time
				is_decaying = yes
			}
			add_modifier = {
				name = modifier_no_minting
				months = gbbf_stupidly_long_modifier_time
			}
			add_modifier = {
				name = mon_taxes_what_are
				months = gbbf_very_long_modifier_time
				is_decaying = yes
			}
			
			# Set Taxes from IP3
			set_tax_level = very_low
			set_military_wage_level = low # USU
			set_import_tariff_level = { # USU -- Montenegro is landlocked, its Trade Center needs logistics, not Merchant Marine
				goods = g:usu_logistics
				level = max_subventions
			}
			set_import_tariff_level = {
				goods = g:small_arms
				level = high_subventions
			}
			set_import_tariff_level = {
				goods = g:artillery
				level = high_subventions
			}
			set_import_tariff_level = {
				goods = g:clothes
				level = high_subventions
			}
			set_import_tariff_level = {
				goods = g:liquor
				level = high_subventions
			}
			set_import_tariff_level = {
				goods = g:fruit
				level = high_subventions
			}
			
			# Assign IP3 modifier in place of Mountain Roadways building
			s:STATE_MONTENEGRO.region_state:MON = {
				add_modifier = {
					name = mon_strong_backs
					months = 480
					is_decaying = yes
				}
			}

			# Fire IP3-specific variant of balkfm_montenegro.001
			trigger_event = balkfm_montenegro.019
		}
		else = {
			trigger_event = balkfm_montenegro.001
		}
	}
}
