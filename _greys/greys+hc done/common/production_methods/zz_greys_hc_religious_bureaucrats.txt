# Grey's x HC+GoB+MoH compatch (GR.4) -- restore HC's disallowing_laws entry on top of USU's rebalanced pm_religious_bureaucrats
# Generated by tools/regen_greys_hc.py on 2026-08-27. Do not hand-edit: the next
# run overwrites this file. Change the generator instead.
#
# Load order: ... -> Hail Columbia -> Gates of Bosphorus -> Mandate of Heaven
# -> addon-HC -> ... -> Grey's pack -> THIS.
# Why this file exists: grey_usu's REPLACE_OR_CREATE re-states disallowing_laws verbatim from vanilla (its own edit is only to building_modifiers) and loads later than HC's INJECT of a third law, so the third law is gone with nothing in error.log.

# base: grey_usu's REPLACE_OR_CREATE (halves clergy/bureaucrat employment, 250 -> 125).
# disallowing_laws is a verbatim copy of vanilla's two laws -- HC's INJECT of a third
# (law_usfp_nominal_separation) is added back here, since USU's full body would otherwise
# silently drop it.
REPLACE_OR_CREATE:pm_religious_bureaucrats = {
	texture = "gfx/interface/icons/production_method_icons/ownership_clergy.dds"
	
	disallowing_laws = {
		law_total_separation
		law_state_atheism
		law_usfp_nominal_separation
	}	
	
	building_modifiers = {
		level_scaled = {
			building_employment_clergymen_add = 125 # 250
			building_employment_bureaucrats_add = 125 # 250
		}
	}
}
