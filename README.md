# vic3mods — набор совместимости для Victoria 3

Точка входа. **Читать первым и, как правило, единственным** — всё остальное
подтягивается отсюда по ссылке под конкретную задачу.

Игра 1.13 (exe 1.13.11). Десять блоков, четыре аддона, целевая цепочка загрузки
в `План проекта.md`.

---

## Куда идти

| вопрос | файл |
| --- | --- |
| Какая задача следующая, что с парами, какие решения приняты | [`План проекта.md`](План%20проекта.md) |
| Как ведётся документация, чем меряется набор, шаблоны README | [`Правила — проект.md`](Правила%20—%20проект.md) |
| Как игра грузит моды, что значат префиксы, факты 1.13, диагностика | [`Правила работы с модами Victoria 3.md`](Правила%20работы%20с%20модами%20Victoria%203.md) |
| Как собрать мегапак или аддон, статусы пар, генераторы | [`Правила — сборка.md`](Правила%20—%20сборка.md) |
| Что делает конкретный чужой мод | `сводки по модам/` |
| Как была закрыта группа задач и почему именно так | [`архив/`](архив/README.md) — **только по ссылке из плана** |

## Где что лежит

```
vic3_mods/                 наша работа
  _<блок>/                 парные компачи блока: <A>+<B> done | noneed | out
  __megapacks/             сведённые мегапаки основной пачки
  __addon/                 собранные аддоны (VC, HC+GoB+MoH, LLWA)
  архив/                  закрытые группы задач
  tools/                   инструменты и генераторы
  tools/_to_delete/        то, что отслужило: разборы закрытых пар, старый кэш
vic3_mods_out/             распакованные чужие моды, руками не править
_tmp_analysis/             кэш индексов и черновики прогонов
```

Суффикс папки компача — это его статус: `done` (написан), `noneed` (проверено,
не нужен), `out` (чужой компач как источник), `out fixed` (чужой, поправленный).

## Чеклист начала сессии

1. Прочитать этот файл и раздел плана по нужному блоку — больше ничего заранее не читать.
2. Задача про совместимость двух модов → `pair_matrix.py --pair "A,B"`.
3. Задача про аддон или про «а не потерялось ли что-то» → `content_holes.py`,
   разделы `registry` и `builds` в первую очередь.
4. Правила по движку открывать, когда упёрся в конкретный префикс или порядок, а не заранее.
5. Что-то выяснилось и переживёт сессию — записать сразу: правило в правила,
   решение в план, историю в архив. Не копить до конца работы.

## Состояние на 27.08.2026

* **Набор ветвится (решение №11 от 26.08.2026):** блок HC+GoB+MoH и Victorian Century —
  **альтернатива друг другу**, вместе слишком много событий и игра идёт медленно.
  Ветка «оба сразу» оставлена рабочей. Обе ветки — в `План проекта.md`.
* **Собрано:** мегапак `no t&r`, аддон-VC, аддон-HC (15 файлов), аддон-LLWA (11 файлов).
* **Починено 27.08.2026 (VC.1k):** аддон-VC обнулял позиции ИГ по целым группам законов — мы
  расставляли позиции по законам-**вариантам** (`parent = ...`), а они наследуют мнение родителя.
  Файл убран, аддон пересобран (25 файлов), **ждёт перезаливки в мастерскую**. Правило — в
  `Правила работы с модами Victoria 3.md`, раздел 2. Та же ошибка осталась в ветке HC — **VC.1l**.
* **Закрыто 27.08.2026 (GR.1):** самая большая пара аддона-Grey's, Grey's × VC, 129 ключей —
  компач `_greys/greys+vc done` (6 файлов), генератор `tools/regen_greys_vc.py`. Правило, по
  которому разрешались споры чисел, — **решение №12** в плане: намерение это отличие от ванили,
  а не наличие поля в чужом полном теле. Попутно найдена GR.20 (`TRY_INJECT::` с двумя
  двоеточиями у трёх компаний USU — записи молча не грузятся).
* **Закрыто 27.08.2026 (GR.2 и GR.5):** Grey's × PSC — компач `_greys/greys+psc done` (4 файла,
  генератор `tools/regen_greys_psc.py`) и Grey's × E&F+hotfix — `_greys/greys+ef done` (2 файла,
  `tools/regen_greys_ef.py`). GR.2 оказалась не парой правок, а **спором двух моделей стройки**
  (PSC против USU) — **решение №13**: побеждает PSC. GR.5 закрыта **решением №14**: у 14 штаб-квартир
  компаний масштаб занятости берём у E&F, структуру у USU. Оба компача заодно закрывают записи
  из GR.1, GR.7e, GR.8 и GR.17 — одну запись нельзя `REPLACE:`-ить двумя компачами, см. план.
  GUI-мердж E&F × MPM вынесен из GR.5 в отдельную **GR.21**.
* **Аддон-Grey's собран 27.08.2026:** `__addon/addon greys` (31 файл) — все десять пар
  и оба внутренних бага (GR.1-20) разобраны, последними GR.13, GR.11, GR.14, GR.19 и GR.8.
  Двенадцать компачей слиты `tools/build_addon_greys.py` без остатка (один аддон-only
  мердж на `building_food_industry`/`building_trade_center`, тот же класс конфликта, что
  `buy_packages` в аддоне-VC). GR.1×VC, GR.18×аддон-VC и GR.4×HC намеренно не внутри —
  ветки VC/HC альтернативны выше по цепочке, эти три компача остаются отдельными и грузятся
  сразу после аддона в своей ветке, тот же приём, что `hc+vc done` для аддона-HC. Осталась
  только GR.21 (GUI-мердж E&F × MPM), она не Grey's. Разбор нового контента —
  `_greys/analysis_greys_new_content_report.md`.
* **Закрыто 27.08.2026 (GR.3 и GR.20):** Grey's × PBE — `force_become_subject`, компач
  `_greys/greys+pbe done` (1 файл, генератор `tools/regen_greys_pbe.py`): PBE грузится
  раньше grey_subject, а тот называет в `TRY_REPLACE:` все под-блоки записи и побеждает
  целиком — гейты PBE по игровому правилу (`vokaes_power_bloc_rule_diplo_action_changes_enabled`,
  **включено по умолчанию**) и инфейми-инцидент пропадали молча. Попутно найденный GR.20 —
  фикс `_greys/greys_usu_fix done` (1 файл, не пара): у трёх компаний `grey_usu` был
  битый префикс `TRY_INJECT::` (два двоеточия) — не тот же случай, что `REPLACE:REPLACE:`,
  отдельная регулярка `^[A-Z_]+::` теперь тоже в самопроверке генераторов
  (`Правила — сборка.md`, раздел 5).
* **Закрыто 27.08.2026 (GR.6):** Grey's × Morgenrote, 35 ключей — компач `_greys/greys+morg done`
  (3 файла, генератор `tools/regen_greys_morg.py`). 31 ключ уже был верно слит собственным
  компачем `grey_usu` к Morgenröte (`zMoG_USU_MR_*.txt` / `mMoG_USU_MR_*.txt`), три реальные
  потери починены: `pmg_panum_hospital` на `building_government_administration` (фикс лежал
  закомментированным в самом файле USU), пять `RU_BL_*` (русское склонение имён 30 зданий
  Morgenröte, включая ванильные airport/railway — их съедало голое тело USU), и `state_standard_of_living_add`
  на `pm_nr_national_park`. **Решение №15:** сосед по той же записи, `pm_nr_royal_reserve`
  (Morgenröte 0.1 против USU 0.2, без комментария) — оставлено число USU, тело PM там пересобрано
  целиком. `building_railway` по телу здания не тронут — сцеплён с GR.5/GR.9/GR.16/GR.17.
* **Закрыто 27.08.2026 (GR.4):** Grey's × HC+GoB+MoH, 11 ключей и один файл — компач
  `_greys/greys+hc done` (7 файлов, генератор `tools/regen_greys_hc.py`). Самое серьёзное —
  `_grey_soft_pop` гасила Hail Columbia целиком (`is_usfp_active` в ноль молча). Ещё: пять культур
  (han/hakka/manchu/min/yue) пересобраны с унионом имён cinosphere × MoH; попутно закрыт **GR.12**
  для всех пяти (стухшая до-1.13 копия без `seal_and_signature_texture`); `mon - montenegro.txt` —
  перекрытие по пути (не по ключу), тело GoB + два изменения USU; `state_trait_columbia_river`
  восстановлена HC-only, но ветка «оба сразу» (VC+HC) этой записью пока не закрыта — см. README
  компача.
* **Закрыто 27.08.2026 (GR.7):** Grey's × TGR, 97 ключей —
  компач `_greys/greys+tgr done` (8 файлов, генератор `tools/regen_greys_tgr.py`,
  самопроверка `0 problem(s)`). Три находки, которых не было в плане, обошлись дороже всего
  остального: торговый центр GR.7d (десять техов TGR раздавали прибавку к потолку — потолок
  сняло само тело `grey_usu`, техи оказались живы), 21 запись владения теряет торговую
  ёмкость GR.7e (14 закрыты вместе с GR.5, семь — здесь) и `bg_consumer_goods` у
  `building_food_industry` GR.7f. Заодно закрыт **GR.10** (`_grey_soft_econ`/`grey_usu` не
  конфликт — намеренная синхронизация файла автором) и найден **TGR.1** — устаревшая копия
  ванильных `country_ranks` внутри самого TGR, портящая мегапак в ветке без Grey's. Порты,
  рельсы, автоматизация и занятость торгового центра решены как споры двух экономик
  (TGR/USU) целиком по подсистемам — оставлено Grey's, файлов нет. `foreign_investment_rights`
  и `trade_states` ушли в GR.8/GR.14, `building_construction_sector` уже закрыт в GR.2.
* **Закрыто 27.08.2026 (GR.9 и GR.16):** Grey's × LLWA / аддон-LLWA — компач `_greys/greys+llwa done` (4 файла, генератор `tools/regen_greys_llwa.py`): 8 из 14 компаний (остальные 6 — в `_greys/greys+vc done`) и 3 из 10 ж/д производственных методов (семь остальных — спор дизайна USU, не потеря), плюс `building_railway`/`building_airport`. По ходу обнаружилось, что grey_usu (обновление того же дня) разбил `building_railway` на два здания — вокзал и новое `building_usu_railway_line`; решением пользователя линия забрала себе всю частную экономику (LLWA + обе группы E&F), закрыв заодно кусок **GR.15**. Заодно закрыт (`noneed`) устаревший чужой компач `usu_llwa out outdate` — все его 11 ключей либо почин**е**ны здесь, либо чужими авторами выше по цепочке с тех пор, как он писался, либо другими нашими парами, которых при его написании не существовало.
* **Закрыто 27.08.2026 (GR.15):** оставшиеся четыре здания USU вне ликвидности E&F —
  `building_river_port`, `ppp_building_power_grid`, `usu_building_hydro_power_plant`,
  `usu_building_public_green`, все на `manufacture_stock` (пятое, `building_usu_railway_line`,
  закрыто раньше вместе с GR.9/GR.16). Компач `_greys/greys+ef done` (+4 файла, версия
  1.13.11-2), генератор `tools/regen_greys_ef.py`, самопроверка `0 problem(s)`.
* **Закрыто 27.08.2026 (GR.17):** мегапак `no t&r` × Grey's — `building_airport` и
  `building_dubois_national_park`, оба теряют `pmg_market_liquidity` (airport ещё и
  `pmg_private_ownership_manufacture_stock`) при `TRY_REPLACE:` от `grey_usu`. Компач
  `_greys/greys+megapack done` (1 файл), генератор `tools/regen_greys_megapack.py`.
  Пять из семи ключей, которые живьём даёт `content_holes.py`, уже закрывались GR.2
  (`building_construction_sector` + четыре `pm_*_buildings`); три пункта про компании
  из старого разбора не подтвердились. `building_airport` взаимоисключим с
  `_greys/greys+llwa done` — не ставить оба сразу.
* **Закрыто 27.08.2026 (GR.18):** аддон-VC × Grey's — `popneed_leisure` теряет `air_travel`
  слоя Morgenröte при `REPLACE:` от `grey_usu` (автор сам оставил запись закомментированной
  с пометкой «Gets added with Morgenrote», но собственный `TRY_INJECT:` восстанавливает только
  `elgar_music`). Компач `_greys/greys+addon-vc done` (1 файл), генератор
  `tools/regen_greys_addonvc.py`. Три соседних `popneed_*` (`free_movement`, `heating`,
  `luxury_food`) и четыре компании подтверждены живьём не-потерей — USU либо сам
  восстанавливает то же самое, либо это спор двух дизайнов (решение №12), либо инжект
  аддитивен.
* **Закрыто 27.08.2026 (GR.14):** пересечения внутри пачки Grey's — `pair_matrix.py` и
  `content_holes.py` их не видят в принципе (вся пачка — один блок). Сверено вручную побайтово
  по порядку пачки: две реальные потери из семи. `usu`×`food` — два метода охлаждения теряют
  поле транспортных расходов (`pm_refrigerated_storage_building_fishing_wharf`,
  `pm_unrefrigerated`), фикс `_greys/greys_food_fix done`. `diplo`×`subject` — запись
  `trade_states` побеждается старой копией grey_subject без авторских правок ИИ grey_diplo и
  с опечаткой `has_port_country` вместо `has_port`, фикс `_greys/greys_subject_fix done`.
  Остальные пять пунктов — синхронизация автора или спор дизайна, файлов не требуют.
* **Закрыто 27.08.2026 (GR.11):** `grey_diplo` объявляет свой флаг реестра CMF
  (`Grey_DIS_is_active`) голым ключом — для `scripted_triggers` это не переопределяет,
  флаг остаётся `no` из реестра CMF (тот тоже объявляет его голым). Сейчас в наборе на
  флаг никто не смотрит, видимой поломки нет — но ровно для этого реестр и существует.
  Фикс `_greys/greys_diplo_fix done` (1 файл, три строки), генератор
  `tools/regen_greys_diplo_fix.py`.
* **Перепроверено и закрыто 27.08.2026 (GR.13):** `_grey_soft_pop` переиздаёт двенадцать
  чужих `com_law_*` триггеров реестра CMF через `REPLACE_OR_CREATE:` (шесть `_trigger` +
  шесть `_alternative_trigger`), с шапкой, рассчитанной на порядок «сразу после файла CMF»,
  а не на реальный мод-порядок — риск был в том, что что-то между ними могло намеренно
  переопределить те же триггеры и тихо лишиться этого переопределения. Проверено грепом по
  всему текущему набору: ни один мод, кроме CMF и самого `_grey_soft_pop`, эти шесть ключей
  не трогает, и тела побайтово совпадают с реестром CMF — переиздание безопасное и без
  содержательного эффекта. Перепроверять при каждом расширении набора.
* **Закрыто 27.08.2026 (GR.8):** × KAI, все пять ключей. `building_construction_sector`
  уже был закрыт в GR.2. `NAI` — не потеря, `defines` сливаются по полю, поля KAI не пересекаются
  по имени с soft_econ/usu. `ai_strategy_territorial_expansion` — уже смержено автором вручную
  (комментарий «Cannot INJECT weights in the correct sequence», числа те же, что у KAI).
  `building_government_administration` теряет `ai_value` KAI целиком (не спор, у grey_usu поля
  просто нет). Пятый, `foreign_investment_rights` — тело grey_diplo осознанно переспорило блок
  `ai` KAI почти везде, но без комментария откатило отдельный багфикс KAI в
  `wargoal_score_multiplier` (subject-исключение при инвестиционных правах); по решению М —
  смержено: переспор grey_diplo остаётся, багфикс возвращён. Оба ключа — компач
  `_greys/greys_kai_fix done` (2 файла), генератор `tools/regen_greys_kai_fix.py`.
* **Закрыто 27.08.2026 (GR.19):** `grey_usu` объявляет голым ключом чужой закон
  `law_forced_labour` (заглушка без `group`, нужна ради пяти ссылок `unlocking_laws` в
  собственных production methods). М проверил экран рабства в игре — закона не видно, как и
  ожидалось; по логу актуального запуска — ни одного упоминания закона и ни одной ошибки
  во всём наборе логов. Заглушка рабочая как есть, фикс не нужен, файла не будет.
  У аддона-Grey's остаётся только GR.21 (не Grey's, GUI-мердж E&F × MPM) — весь блок Grey's
  закрыт.
* Товары: 74 из 128, запас 54.

## Два инструмента, и оба нужны

`pair_matrix.py` меряет пересечение ключей, которые определяют **оба** мода.
`content_holes.py` меряет три класса, которых первый не видит по устройству:
новый контент, которого нет в чужих рукописных списках; чужие компачи вне блока;
уже собранные мегапак и аддоны, поверх которых грузится следующий блок.

Статус `noneed`, поставленный по одному из них, дважды оказывался неверным
(LLWA × E&F, HC+GoB+MoH × E&F). Ставить только после обоих.
