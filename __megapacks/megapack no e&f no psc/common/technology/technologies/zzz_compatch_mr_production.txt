# Morgenroete <-> Tech & Res: production technologies.
#
# One entry. T&R's ztr_modified_mr_production.txt keeps this technology but
# re-routes it onto its own physics chain: verrier_modern_physics_tech instead
# of Morgenroete's verrier_radioactivity_tech. Morgenroete's Verrier line runs
# radioactivity -> nuclear physics and drives the Einstein events from it, so
# with T&R's prerequisite the branch is entered from the wrong place.
#
# Everything else here is T&R's: era_5, the bg_power throughput modifier and the
# ITA/USA ai_weight are all their additions and are kept deliberately. The
# single changed line is unlocking_technologies.
REPLACE:verrier_nuclear_physics_tech = {
	# Unlocks Einstein Events
	era = era_5
	texture = "gfx/interface/icons/invention_icons/verrier_nuclear_physics_tech.dds"
	category = production

	unlocking_technologies = {
		verrier_radioactivity_tech #Morgenroete chain, T&R re-routes this to verrier_modern_physics_tech
	}

	modifier = {
		building_group_bg_power_throughput_add = 0.05
	}

	ai_weight = {
		value = 2

		if = {
			limit = {
				OR = {
					c:ITA ?= ROOT
					c:USA ?= ROOT
				}
			}
			add = 2 #Fermi-Szilard-etc
		}
	}
}
