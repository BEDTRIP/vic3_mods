########################################
# E&F + The Great Revision (TGR) - Defines compatibility
#
# Defines merge key by key and the last mod loaded wins, so only keys BOTH mods
# set need a decision here. That is one key.
#
# GOODS_SHORTAGE_PENALTY_MAX and GOLD_RESERVE_RETURNS_FACTOR used to be repeated
# here as well - pointless, TGR does not touch them and E&F loads later anyway.
########################################

NEconomy = {
	# Balance choice, not a conflict fix: vanilla 0.75, TGR 0.85, E&F 0.99.
	# TGR's whole trade rework (advantage, tariffs, Trade Center payback) is tuned
	# around a narrow band; E&F's 0.99 lets prices swing far enough that TGR's AI
	# trade events chase noise. We keep TGR's value.
	# If E&F's currency crises feel toothless, this is the first line to try at 0.99.
	PRICE_RANGE = 0.85
}
