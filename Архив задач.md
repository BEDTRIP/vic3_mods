## Что выбросить после удаления T&R

Правки в уже собранных модах — сделаны 25.08.2026. Задачи закрыты все шесть; по ходу нашлись два лишних файла и одно содержательное следствие, оба ниже.

- [x] **T&R.1 Аддон-HC: удалён `common/laws/zz_hctr_slavery.txt`.** Мердж четырёх законов рабства «тело HC + `on_activate`, `modifier` и `can_enact` от T&R». Без T&R файл придумывает поведение, которого не хочет ни один автор.
- [x] **T&R.2 Аддон-HC: удалён `zzz_hctr_tr_default_strategy.txt`** — переиздание инжекта T&R на 1360 строк. `zz_hctr_ai_strategy_default.txt` (мердж KAI × MoH) и `zz_hctr_tgr_default_strategy.txt` остались.
- [x] **T&R.2a В плане было названо два файла, а T&R-овских оказалось четыре.** Ещё два: `common/decrees/zz_hctr_greener_grass.txt` (тело HC + `country_trigger` T&R) и `common/journal_entries/zz_hctr_warlord_china.txt` (тело MoH + escape 1940 и `on_complete` T&R, который указывает на событие `the_future_of_china.201` — без T&R это мёртвая ссылка). Оба тоже удалены. Все четыре — в `_HC+GoB+MoH/hc+kai done/_to_delete/tr_removed_2026-08-25/`.
- [x] **T&R.2b Удаление сделано в компаче, а не в сборке.** По правилу «собирать из компачей, а не править предыдущую сборку»: файлы убраны из `_HC+GoB+MoH/hc+tr+kai done`, папка переименована в **`hc+kai done`**, и уже потом сборка пересобрана. Генератор тоже вычищен: `regen_addon1.py` больше не знает про T&R (`build_trkai` → `build_kai`, `P.tr` убран, функции `_slavery`, `_greener_grass`, `_warlord_china` удалены), в `build_addon1.py` и `addon1_metadata.py` поправлены пути и зависимости.
- [x] **T&R.2c Содержательное следствие: три института TGR вернулись.** `tools/tgr_default_strategy.py` намеренно НЕ восстанавливал `institution_police`, `institution_health_system` и `institution_home_affairs` (TGR поднимает их с 10 до 500): T&R переобъявлял все семь ванильных институтов на 10 в инжекте, который грузился позже, поэтому в порядке самих авторов значения TGR были уже мертвы, и восстановление решало бы пару TGR × T&R. В коде на этот случай стоял assert. С уходом T&R эти ключи ниже по цепочке не трогает никто — блок TGR теперь переиздаётся целиком, 15 утверждений вместо 12. **Правка лежит в общем модуле, значит она же приедет в мегапак, когда МП.1 соберёт пару KAI × TGR.**
- [x] **T&R.3 Аддон-HC пересобран и перепроверен.** 11 файлов вместо 15, `ALL CHECKS PASS`, покрытие компачей 4/0/0 + 5/0/0 + 2/0/0. Мердж `ai_strategy_default` — 0 конфликтов. `scan_conflicts.py` прогнан против KAI, TGR, HC, GoB, MoH и мегапака `no t&r`: с мегапаком 0/0, с HC было 10 ключей стало 5, с MoH было 3 стало 2. README, `conflicts_addon1_internal.md` и `metadata.json` переписаны, версия `1.13.11-3`. Зависимость мегапака переставлена на `3640735868` («MegaComPatch TGR + PSC + E&F + MR + PBE»). **KAI из зависимостей НЕ убран** — два оставшихся файла построены на его теле. Синхронизировано в игровую папку, `diff -rq` чист.
- [x] **T&R.4 Старые варианты мегапака помечены неподдерживаемыми.** Строка `[b]NOTE (25.08.2026)…[/b]` в начале README у `megapack`, `megapack no tgr` и `megapack no e&f no psc` (последний в плане назван не был, но T&R в нём тоже есть) со ссылкой на `3640735868`. У `megapack no t&r` — своя строка: он теперь основной, и в ней же сказано, что KAI в нём пока не пропатчен. Папки не двигали, README синхронизированы в игровую папку.
- [x] **T&R.5 Плейсет — только отчёт, правки в `launcher-v2.sqlite` не делались.** См. «Состояние плейсетов» ниже.
- [x] **T&R.6 Папки пар с T&R помечены `outdate`:** `stuff/_tr outdate`, `_ef/ef+tr+kai out fixed outdate`, `_morg/morg+tr+kai out fixed outdate`, `_psc/psc+tr+kai out fixed outdate`, `_tgr/tgr+tr+kai done outdate`. `regen_tgr_kai_ai_default.py` перенацелен на будущую папку `_tgr/tgr+kai done` (файл `zz_kai_tgr_ai_default_strategy.txt`) и больше не читает T&R; `regen_ef_tr_copies.py` помечен `# RETIRED` — его выходные папки больше не существуют. **Не тронут `_pbe/pbe+tr+kai noneed`** — в плане он назван не был, а обоснование `noneed` по паре KAI × PBE в нём живое.

### Состояние плейсетов на 25.08.2026

Плейсетов два, и описание в прошлой версии плана относилось к неправильному.

* **«Initial playset» (активен, 56 модов)** — включены только `Victorian Century` (54) и `Victorian Century [RU]` (55), всё остальное выключено. Похоже на тестовый прогон под VC.7 (проверка престижных товаров Британией). Как рабочий набор он сейчас не годится.
* **«BED_TRIP's MEGAPACK» (38 модов, 23 включены)** — это и есть рабочий набор, и порядок в нём почти целевой: CMF → Dense UI → TGR (+RUS) → PSC → **KAI** → E&F (+RUS) → E&F Hotfix → Morgenröte → **T&R (10) + T&R RUS (11)** → PBE (+RUS) → **полный мегапак (14)** → VC (+RU, 15–16) → HC (+рус) → GoB (+рус) → MoH → аддон-HC (22). VC уже стоит до Hail, Columbia! — как требует план.

**Что кликнуть в лаунчере (BED_TRIP's MEGAPACK):**

1. снять `[1.13] Tech & Res` (позиция 10);
2. снять `Tech & Res - Русский перевод` (11);
3. снять `MegaComPatch TGR + PSC + E&F + MR + T&R + PBE` (14);
4. включить `MegaComPatch TGR + PSC + E&F + MR + PBE` (id `3640735868` либо локальная копия `mod\megapack no t&r`) и поставить её на освободившееся место 14 — после PBE и до Victorian Century;
5. аддон-HC (22) — это локальная папка `mod\addon1 hc+gob+moh`, уже обновлена на диске, переключать ничего не надо.

Оба варианта мегапака зарегистрированы в лаунчере дважды — из мастерской и из локальной `mod/`. Включать надо одну копию, и лучше локальную: аддон и сборки правятся именно там.

## Мегапак no-t&r + KAI — сделано 25.08.2026

Сборка `megapack no t&r` теперь основная и знает про KAI. Steam id `3640735868`, название стало **«MegaComPatch TGR + PSC + KAI + E&F + MR + PBE»**, версия `1.13.11-1` → `1.13.11-2`. Подробный отчёт — `__megapacks/megapack no t&r/build_report_2026-08-25_kai.md`.

### Сначала пришлось разделить компачи `*+tr+kai`

Они были не устаревшими, а слипшимися: один компач обслуживал две пары сразу. Разделены 25.08.2026, **KAI-половинки остались в папке своего блока, T&R-половинки собраны в новую папку `_tr/` одним набором.**

| было | KAI-половина | T&R-половина |
| --- | --- | --- |
| `_tgr/tgr+tr+kai done` | `_tgr/tgr+kai done` — один файл, `zz_kai_tgr_ai_default_strategy.txt` | `_tr/tgr+tr done` — пять файлов, переименованы `zz_tr_kai_tgr_*` → `zz_tr_tgr_*`; плюс `_tr/kai+tr done` (`ai_strategy_resource_expansion` — это пара KAI × T&R, к TGR отношения не имеет) |
| `_ef/ef+tr+kai out fixed` | `_ef/ef+kai noneed` | `_tr/ef+tr out fixed` |
| `_morg/morg+tr+kai out fixed` | `_morg/morg+kai noneed` | `_tr/morg+tr out fixed` |
| `_psc/psc+tr+kai out fixed` | `_psc/psc+kai done` (новый файл, см. МП.3) | `_tr/psc+tr out fixed` |
| `_pbe/pbe+tr+kai noneed` | `_pbe/pbe+kai noneed` | `_tr/pbe+tr noneed` |
| `stuff/_tr/tr+kai+vc` | — | `_tr/tr+kai+vc`, пока не разделён (это задача VC.4) |

`_tr/` — отдельный живой набор для тех, кто играет с Tech & Res. Из нашего набора T&R убран, но компачи для него остаются рабочими и проверенными.

**VC.4 закрыт 26.08.2026 не разделением этого файла, а заново с нуля** — `_vc/kai+vc done`, генератор читает текущие KAI и VC живьём. `_tr/tr+kai+vc` из этой строки таблицы на диске не найден (видимо переехал в `_vc/kai+vc outdate` вместе с остальными VC-компачами января 2026); T&R-половина там, если понадобится набору `_tr/`, ещё ждёт разбора.

### Что сделано по пунктам

- [x] **МП.1 KAI × TGR — 37 ключей, патчится один.** Разбор — `_tgr/tgr+kai done/conflicts_tgr_vs_kai_report.md`. 24 закона — у KAI везде `INJECT: ai_enact_weight_modifier`, тела TGR целы. `building_construction_sector` и `foreign_investment_rights` — TGR пишет тело, KAI инжектит один под-блок, сливается. `atmospheric_engine` и `mechanical_tools` — инжекты с обеих сторон в разные под-блоки. `NAI` — сливается поключево, реально пересекаются восемь `MONEY_SPENDING_*`, оба автора выставили намеренно. Шесть остальных `ai_strategies` — у обоих полные осознанно разные стратегии. **Единственный настоящий конфликт — `ai_strategy_default`**, и он закрыт файлом `zz_kai_tgr_ai_default_strategy.txt` (генератор `tools/regen_tgr_kai_ai_default.py`). Прежний список из восьми пунктов в этом плане был составлен по именам файлов трёхстороннего компача, а не по отчёту пары; в файлах `zz_tr_kai_tgr_laws/goods/buildings/production_methods/technologies` KAI не было ни строки — всё это пары TGR × T&R.
  * **Изменение содержания:** блок TGR теперь переиздаётся целиком, 15 утверждений вместо 12 — вернулись 10 → 500 на `institution_police`, `institution_health_system`, `institution_home_affairs`. Раньше их отдавали T&R, который переобъявлял все семь ванильных на 10 позже. См. T&R.2c.
  * **`wanted_army_size_script_value` сменил владельца.** Оба автора переписали ванильную формулу целиком и оба открывают блок `value = 0` — владеет последний. Раньше это был T&R, теперь KAI. У TGR остаются за бортом бонусы ранга (+100/+50 против +25/+10 у KAI) и слагаемое `fixed_income`. Ребаланс, не поломка; записано, не патчится.
- [x] **МП.2 KAI × Morgenrote — 2 ключа, `noneed`.** `building_government_administration`: KAI `INJECT: ai_value`, MR `TRY_INJECT: production_method_groups`. `atmospheric_engine`: KAI `INJECT: ai_weight`, MR `TRY_INJECT: unlocking_technologies`, TGR `INJECT: modifier` — три автора, три разных под-блока. Обоснование — `_morg/morg+kai noneed/README.md`.
- [x] **МП.3 KAI × PSC — 1 ключ, и это НЕ `noneed`.** Прежний вывод держался на утверждении «`kai_` грузится раньше `zz_PSC_`» — это порядок имён файлов, а решает порядок модов, и **KAI грузится после PSC** (позиция 5 против 4 в плейсете, и так же в целевой цепочке). KAI инжектит `ai_value` блоком, который открывается `value = 1000`; внутри под-блока это сбрасывает всё, что накопилось раньше, то есть у PSC пропадают база 2500 и бонус за первый стройсектор. В логе — ничего. Написан компач `_psc/psc+kai done` (одна запись, полное тело PSC). **В мегапак он не входит намеренно:** сборка везёт свой `zz_pb_ef_construction_sector.txt` — полный `REPLACE:` этого здания с блоком PSC внутри — и грузится последней. Основание держится на порядке: **мегапак после Kuromi's AI**, строка есть в README сборки.
- [x] **МП.4 KAI × E&F+hotfix и KAI × PBE — 0/0.** Обоснования — `_ef/ef+kai noneed/README.md` и `_pbe/pbe+kai noneed/README.md`.
- [x] **МП.5 README, metadata, покрытие.** KAI в `relationships` (между PSC и E&F) и в порядок загрузки README. `scan_conflicts.py` сборка × KAI — два общих ключа, оба намеренные (`ai_strategy_default` — наш новый файл; `building_construction_sector` — файл сборки поверх инжекта KAI). Внутренние дубли сборки не изменились (98 `company_types` — штатная конструкция `ef+psc`). Локализация: 418 ключей, 11 языков по 38, дублей внутри языка нет. **Товары пересчитаны по виртуальной ФС на всей цепочке сборки: 69 из 128, запас 59** (ваниль 53 + хотфикс 7 + Morgenröte 5 + PSC 4). В README сборки стояло 70 — исправлено. У KAI папки `common/goods` нет вообще. Синхронизировано в игровую папку, `diff -rq` чист.

### Найдено попутно

* **`_tr/tr+kai+vc/common/company_types/zz_vc_tr_company_types.txt` был без BOM**, а в нём путь к иконке `…forges_et_chantiers_de_la_méditerranée.dds` в некомментарной строке. Без BOM игра читает такой путь неверно и иконка молча не находится. BOM добавлен.
* **`tools/regen_ef_tr_copies.py` смотрел на папки, которых нет** (`_ef/ef+tr+kai out`, `_ef/ef+tr+kai fixed`). Перенацелен на `_tr/ef+tr out` / `_tr/ef+tr out fixed`. Папки-источника `_tr/ef+tr out` на диске **нет**, и `--check` в этом случае печатает `missing:` и выходит с кодом 0 — то есть выглядит как «всё сошлось». Написано в шапке скрипта; распаковать текущую версию чужого компача туда до следующего прогона.
* **`_tr/ef+tr out fixed` определяет четыре PM золотой шахты дважды** — в `zef_mines_production_methods.txt` (копия чужого компача) и в `zzzz_ef_tr_fix_gold_minting.txt` (наша починка, имя сортируется позже и побеждает). Это задумано, но нигде не задекларировано.
* **`_tr/psc+tr out fixed` создаёт свой товар `concrete_construction`.** В нашем наборе он не считается — T&R-набор отдельный, — но при любом сведении двух наборов это +1 к потолку 128.

## аддон-VC

VC × шесть блоков основной пачки. Старые компачи января 2026 объявляли `supported_game_version 1.12.*` и числили VC 1.12.2 — они были базой и отчётами, а не готовыми файлами.

**Папки пар переехали из `stuff/` в `_vc/`** (сделано до этой сессии). Устаревшие помечены `outdate`; `_vc/tgr+vc outdate notready` после закрытия VC.1 перенесена в `_vc/_to_delete/tgr+vc_superseded_2026-08-25`.

- [x] **VC.1 VC × TGR — 172 ключа и 14 путей файлов. Закрыто 25.08.2026.** Компач — `_vc/tgr+vc done`, разбор — `conflicts_tgr_vs_vc_report.md` там же, генератор — `tools/regen_vc_tgr.py` поверх нового `tools/vic3merge3.py`. **18 файлов, 141 ключ, самопроверка `0 problem(s)`, `--check` сходится.**
  * **Страх «спор двух замыслов» не подтвердился.** Ваниль 1.13.11 оказалась общим предком обеих сторон по каждой спорной записи, так что пара закрывается трёхсторонним мерджем, а не выбором победителя. У ИГ VC переписывает крупно (141–395 строк против ванили), а TGR правит точечно (8–79) и все свои правки помечает рамками `##### TGR CHANGES #####`. Спорных кластеров на всю пару — 38, и почти все мелкие.
  * **Не понадобилось ничего: 30 ключей из 172 — не конфликт.** `common/buildings` (17, VC инжектит только `can_build_private`), `common/technology/technologies` (6, разные под-блоки или разные ключи модификатора), `common/production_methods` (3, VC инжектит `unscaled` в `building_modifiers` TGR), `common/defines` (4 группы, **ноль общих define** — старое утверждение января про столкновения внутри `NAI` на текущих версиях неверно).
  * **`manifest_destiny.txt` не восстанавливаем.** VC гасит файл 11 байтами, но дальше по цепочке Hail Columbia гасит Manifest Destiny ещё раз своей журнальной цепочкой. Обоснование держится на порядке — строка есть в README компача.
  * **Что изменилось в чужих модах с января:** TGR **выбросил папку `map_data/` целиком** (двух перекрытий по `state_regions` больше нет, карта вся за VC), VC **выбросил `brz - brazil.txt`** (бразильские правки TGR доживают сами), появились четыре новых перекрытия по путям (`canada_australia.txt`, `jap`, `net`, `spa`, `religious_party.txt`), `ideology_utilitarian_leader` из пересечения ушёл.
  * **Проверено и оказалось не багом:** `has_technology_researched = pan-nationalism` через дефис — это ванильное имя технологии (`30_society.txt`), а не опечатка VC. Правка TGR на `nationalism` + дата — осознанное смягчение. Автору писать не о чем.
  * **Аддон-VC против мегапака `no t&r` — 0/0.** Против HC 3 ключа, против MoH 5, все в `common/interest_groups` — см. HC.7 ниже.
- [x] **VC.1i Позиции идеологий по новым законам TGR — проверено 25.08.2026, писать НЕ надо.** Возник вопрос: VC добавляет 56 идеологий, ни одна не имеет позиции ни по одной из 13 новых групп законов TGR — не теряет ли TGR из-за этого свою политику. Померили, и оказалось наоборот.
  * **Нейтральность здесь — норма, а не поломка.** Идеологий в игре 236. Каждая из 13 групп TGR получает мнение от 6 до 27 из них: `lawgroup_legislative_process` — 27, `lawgroup_economic_incentives_primary` и `_secondary` — по 6. То есть по любой группе TGR молчат ~90% идеологий, и так задумано автором.
  * **Сам TGR покрывает свои группы редко.** Из его 48 идеологий 10 покрывают ноль или одну группу из 13, ещё 10 — две; медиана 2 из 13. Полных покрытий нет ни одного.
  * **Пулы ИГ VC не трогал.** В телах ИГ (наш мердж) `ideologies` и `character_ideologies` — ванильные списки. Свои 48 идеологий VC навешивает в рантайме через `on_researched` и события (`ideology_moralist` → `ideology_inclusiveness`, `ideology_reactionary` → `ideology_enlightlism`, `ideology_jingoist` → `ideology_imperialism`). Ни исходные, ни целевые ИГ-идеологии позиций по группам TGR не имеют и в ванили — терять нечего.
  * **Лидерские идеологии — единственное живое место, и там ничего не потеряно.** Из 15 лидерских идеологий в телах ИГ 11 покрыты TGR, 4 нет (`anti_slavery`, `despotic_utopian`, `humanitarian`, `utopian`) — и это ванильные, которых TGR сам не тронул: из 20 ванильных лидерских он оставил без позиции 7. Восемь новых лидерских идеологий VC (`gladstonian_liberalism`, `imperial_reformer`, `kemalism`, `one_nation_conservatism`, `paternalistic_conservative`, `polk_jingoist`, `reactionism`, `westernlism`) навешиваются только на **персонажей самого VC** — у TGR папки `common/character_templates` нет вообще. Значит это не отнятая позиция, а новый персонаж без позиции, ровно как ванильный гуманист.
  * **Почему не дописываем.** Ближайший донор по сходству позиций даёт 25–50% совпадения на выборке из 4–10 законов, и у самого донора покрыто 1–2 группы из 13. Дописать 8 × 13 — это придумать за обоих авторов мнение, которого ни один не высказывал, там где молчание штатно. Правило «не восстанавливать то, чего не хочет ни один автор» ровно про этот случай.
  * **Что осталось бы делать, если передумаем:** дописывать надо не 56 × 13, а 8 лидерских идеологий VC, и только те группы TGR, по которым высказывается сопоставимый ванильный лидер. Отдельным слоем `INJECT:` — имена групп у VC не заняты, тела не переписываются.
- [x] **VC.1j Позиции расставлены вручную — закрыто 26.08.2026. 1725 позиций, два файла.**
  `zz_vc_tgr_stances_on_tgr_laws.txt` — 56 идеологий VC, 1245 позиций по 60 законам TGR; `zz_vc_tgr_stances_on_vc_laws.txt` — 48 идеологий TGR, 480 позиций по 10 законам VC. Оба `INJECT:`, тела идеологий не переписываются. Источник — `vc_tgr_ideology_grid.xlsx` рядом с компачем, генератор читает его напрямую (`openpyxl` на машине есть), правится таблица — перегоняется генератор.
  * Проверено после сборки: ноль несуществующих идеологий, ноль несуществующих групп, **ноль законов не в своей группе** (сверено с `group =` в самих модах), ноль недопустимых значений. Компач стал 20 файлов, самопроверка `0 problem(s)`, против мегапака по-прежнему 0/0.
  * **`ideology_jacksonian_democrat` — исключение:** HC переобъявляет его полным телом ниже по цепочке, наш инжект туда не доживает. Генератор проверяет это assert'ом и печатает предупреждение. Перенос — в HC.4, см. ниже.
  * Пустая ячейка трактовалась как «мнения нет»; явный `neutral` (304 раза) перенесён как есть — это зафиксированное решение, а не пропуск.
- [x] ~~**VC.1j Таблица выдана 25.08.2026**~~ — заполнена и собрана, см. выше. Сама таблица осталась источником: `_vc/tgr+vc done/vc_tgr_ideology_grid.xlsx`, листы «Позиции», «Якорь» (кто как голосует сейчас), «Доноры TGR», «Обратно».
- [x] **VC.2 VC × E&F+hotfix — 145 ключей. Закрыто 26.08.2026.** Компач — `_vc/ef+vc done`, разбор — `conflicts_ef_vs_vc_report.md` (E&F) и `conflicts_ef_hotfix_vs_vc_report.md` (хотфикс) там же, генератор — `tools/regen_vc_ef.py`. **4 файла, самопроверка `0 problem(s)`, `--check` сходится.**
  * **141 ключ из 145 — не конфликт**, в отличие от пары с TGR это не трёхсторонний мердж: VC грузится позже и его `REPLACE`/`REPLACE_OR_CREATE` уже существуют к моменту загрузки компача, так что почти везде достаточно `INJECT:` поверх готового тела VC, а не восстановления полного тела. `common/buildings` (41 из 42 ключей — VC везде `TRY_INJECT: can_build_private`, E&F везде `INJECT: production_method_groups`, разные под-блоки, проверено программно по всем 42, а не выборочно), `common/defines` (`NEconomy` — ноль общих активных ключей, у E&F совпадающие по имени строки закомментированы), `common/technology/technologies` (`mutual_funds` — E&F переиздаёт технологию целиком, VC инжектит в тот же `modifier` четвёртое поле, блоки `modifier` накапливаются, а не конфликтуют), `common/history/global` (`GLOBAL`) и `common/on_actions` (`on_monthly_pulse_country`) — аддитивные категории, `pair_matrix.py` их не считает: 147 (E&F) + 2 (хотфикс) − 4 (обе стороны обеих категорий) = 145.
  * **4 настоящих конфликта, все одного типа: REPLACE_OR_CREATE у VC съедает более ранний INJECT E&F.** `common/buy_packages` (99 записей `wealth_1`…`wealth_99` — E&F добавляет `popneed_currency`/`popneed_financial_products`, VC их нигде не упоминает; байт-в-байт совпадает с компачем января 2026, генератор читает числа прямо из текущего файла E&F, а не из своей таблицы), `common/buildings/building_opium_plantation` (единственное здание, где VC делает голый `REPLACE:`, а не `TRY_INJECT:`, и его `production_method_groups` не несёт двух PMG от E&F), `common/company_types/company_standard_oil` (VC переиздаёт компанию целиком под свой американский ребаланс и не называет два престижных товара E&F), `common/static_modifiers/base_values` (VC не упоминает иконку E&F, а `country_minting_add` есть у обоих).
  * **`base_values` — единственное решение, а не механический перенос.** У VC уже стоит `country_minting_add = 1000`, у E&F `-500`; поле с суффиксом `_add` в Vic3 суммируется по всем источникам, а не берётся от последнего (см. правило про `multiply = 2.0` после `multiply = 2` в разделе 6 «Правил работы»), значит перенос строки E&F поверх VC восстанавливает то же самое +500, что получилось бы без REPLACE_OR_CREATE VC. Решение записано в README компача, а не спрятано в генераторе.
  * **Слот престижного товара для `company_standard_oil` — отдельный вопрос, не закрыт здесь.** Компач восстанавливает только определение (`mining_stock_usa`, `prestige_good_usa_oil` в `possible_prestige_goods`); попадут ли они в три живых слота при полной цепочке загрузки — вопрос VC.7.
- [x] **VC.3 VC × Morgenrote — 101 ключ, 1 путь. Закрыто 26.08.2026.** Компач — `_vc/morg+vc done`, разбор — `conflicts_vc_vs_morgenrote_report.md` там же, генератор — `tools/regen_vc_morg.py` (по образцу `regen_vc_ef.py`). **3 файла, самопроверка `0 problem(s)`, `--check` сходится, против мегапака `no t&r` — 0/0.**
  * **95 ключей из 101 — реальный конфликт, закрыт.** 90 `wealth_10`…`wealth_99` (VC `REPLACE_OR_CREATE` съедает `TRY_INJECT:popneed_entertainment` Morgenrote — механика та же, что в паре E&F × VC, и в старом компаче против T&R; значения совпадают байт-в-байт с тем компачем не потому, что скопированы, а потому что T&R и Morgenrote брали один сторонний источник), три `popneed_*` (`free_movement`, `leisure`, `luxury_items` — VC `REPLACE_OR_CREATE` съедает `entry` Morgenrote на `air_travel`/`elgar_instruments`/`rubber`), два `character_templates` (Диккенс — роль, два трейта, `on_created`; Карл Юхан Бернадот — один трейт; оба съедены `REPLACE_OR_CREATE` VC).
  * **5 ключей из 101 — не конфликт, ничего не потерялось.** Два теха (`civilizing_mission`, `organized_sports` — оба мода только `INJECT`/`TRY_INJECT`, поля разные, `modifier`-блоки накапливаются как `mutual_funds` в паре E&F × VC), два из трёх `mobilization_options` (`aerial_recon`, `balloon_recon` — Morgenrote трогает `group`, VC трогает `upkeep_modifier`, разные поля).
  * **1 путь — не конфликт.** `common/dna_data/ecchi_ger_hitler.txt`: тела побайтово совпадают по содержанию генов (только форматирование разное), расхождение ровно в одном косметическом значении (`gene_stubble`) и одном лишнем гене у VC (`coats`). Общий сторонний источник DNA, мерджить нечего, VC побеждает путь без потерь.
  * **1 ключ оставлен открытым, компач не пишем.** `mobilization_option_chemical_weapons` — Morgenrote `TRY_REPLACE:upkeep_modifier{fertilizer_add=2}`, VC `INJECT:upkeep_modifier{fertilizer_add=0.5,...}`, то же имя поля в том же под-блоке. Не установлено, суммируются ли повторные `_add` внутри под-блока одной записи (как в `base_values`, где это подтверждено для странового пула статических модификаторов) или второе объявление перекрывает первое — писать вслепую опасно в обе стороны (утроить или потерять правку). Дешёвая проверка в игре — как в VC.7.
- [x] **VC.4 VC × KAI — 9 ключей. Закрыто 26.08.2026.** Компач — `_vc/kai+vc done`, разбор — `conflicts_vc_vs_kai_report.md` там же, генератор — `tools/regen_vc_kai.py`. **1 файл, самопроверка `0 problem(s)`, `--check` сходится, против мегапака no t&r — 0/0.**
  * **5 `ship_types` и `NAI` — не конфликт.** KAI везде `INJECT:` только в `ai_weight`, VC везде `INJECT:` только в `modifier`/`construction_goods`(/`materiel_goods`) — разные под-блоки на всех пяти общих записях. `NAI` — дефайны сливаются поключево, у KAI и VC ни одной общей константы.
  * **`ai_strategy_great_reforms`/`ai_strategy_tanzimat_reforms` — восстановлено.** KAI инжектит `revolution_aversion = 67.5`; тело VC побайтово совпадает с ванилью везде, кроме `possible` (значения 10 и 25 — ванильные, не решение VC). Переиздано `INJECT:` поверх тела VC.
  * **`ai_strategy_meiji_restoration` — трёхсторонний мердж (Анализ 5).** Собственный вклад VC — только `possible` (свой журнал вместо `je_meiji_main`/`je_meiji_restoration`). Собственный вклад KAI — `revolution_aversion` 10→67.5, `max_regressiveness` 25→50, снятие `ig_armed_forces` из `anti_interest_groups` + новый блок `interest_group_government_weight` на `je_meiji_army`. Восстановлены только два числа (полным `REPLACE_OR_CREATE:` тела VC). **Список `anti_interest_groups`/`interest_group_government_weight` оставлен открытым** — завязан на ванильные журналы (`je_meiji_army`, `je_meiji_main`), которых VC не трогает вовсе (у VC своя параллельная система реставрации Мэйдзи), и неясно, доживают ли они до срабатывания. Дешёвая проверка в игре — см. отчёт.
- [x] **VC.5 VC × PSC — 4 ключа, `noneed` подтверждён 26.08.2026.** `pm_wooden_buildings`, `pm_iron_frame_buildings`, `pm_steel_frame_buildings`, `pm_arc_welded_buildings`. PSC `REPLACE:` полным телом, VC `INJECT:` только `building_modifiers.workforce_scaled` (те же `_add`-поля — складываются, не перезаписываются, правило раздела 3) и отдельно `state_modifiers`, которого PSC не касается. Обоснование — `_vc/psc+vc noneed/README.md`.
- [x] **VC.6 VC × PBE — 1 ключ, `noneed` подтверждён 26.08.2026 — и это не просто отсутствие конфликта.** `power_bloc_mandate_progress_by_rank` — ванильный script value. VC переопределяет правильно (`REPLACE_OR_CREATE:`), PBE — **голым повтором ключа без префикса**, а для `script_values` это не переопределяет вообще (правило раздела 3): вклад PBE мёртв независимо от VC, даже без VC в наборе. Плюс всё содержимое PBE спрятано за собственным выключенным по умолчанию `has_game_rule`. Обоснование — `_vc/pbe+vc noneed/README.md`. Автору PBE стоит написать про префикс — тот же класс мелочи, что GR.11/GR.13.
- [x] **VC.7 Престижные товары — точный список получен 26.08.2026, решение по каждому товару НЕ принято.** Потолок — три престижных товара на базовый, реальными слотами становятся первые три объявления. Инструмент — новый `tools/scan_prestige_goods.py`, отчёт — `_vc/prestige_goods VC.7/report.md` (сырой вывод там же). **Подтверждено 13 мёртвых объявлений на 10 товаров: VC 8, HC 2, MoH 1, E&F 2** — совпадает с планом дословно, точные победители/мертвецы по каждому товару — в отчёте.
  * **Правило «выигрывают первые три» — не предположение, уже проверено в этом репозитории.** Шапка `_ef/ef hotfix 1.13/common/prestige_goods/zz_ef_cm_prestige_currencies.txt` документирует три прогона в игре для валют E&F (3/10/95 объявлений) — 4-е и далее гарантированно мертвы. Открытый нюанс из той же шапки: похоже, 2-й слот тоже может не доставаться странам, кому не подошёл 1-й (при 10 объявлениях весь мир, кроме Британии, попал сразу на 3-е) — не проверено для этих 10 товаров.
  * **`manufacture_stock_usa`** у самого E&F закомментирован в основном файле и переиздан активным (с обнулённым `prestige_bonus`) в отдельном `00_ef_prestige_goods_2.txt` — похоже, автор сам знает про потолок (комментарий в его же файле: «после 3 престижных товаров игра возвращает первый»).
  * **Не сделано:** дешёвая проверка в игре для этих 10 товаров (план требовал её до починки), и решение по каждому — гасить чужой престижный товар или отдавать слот. Редакторский вопрос, не технический — намеренно не решается в этом прогоне.
  ```
  small_arms  6 -> мертвы enfield_rifle, hanyang_gun, mosin_nagant [все VC]
  engines     5 -> мертвы rr_engines, japanese_express [VC]
  artillery   4 -> abus_guns [VC]          automobiles 4 -> skoda_automobiles [VC]
  groceries   4 -> english_afternoon_tea [VC]
  fine_art    4 -> usfp_hollywood_movies [HC]   liquor 4 -> usfp_coca_cola [HC]
  grain       4 -> bicycle_flour [MoH]
  manufacture_stock 4 -> manufacture_stock_usa [E&F]   railroad_stock 4 -> railroad_stock_rus [E&F]
  ```
  Два последних — E&F переполняет собственные товары сам по себе, без чужого участия. Решать по каждому базовому товару: погасить чужой престижный товар по ключу или отдать слот. **Перед починкой проверить в игре, действительно ли выигрывают первые три, а не последние** — правило записано как «число в exe, дефайна нет» и на этом наблюдении ещё ничего не перепроверялось. Дешёвая проверка: Британия с включённым VC, посмотреть, есть ли `enfield_rifle` в списке.
- [x] **VC.8 Сборка аддона-VC — закрыто 26.08.2026.** `tools/build_addon_vc.py` (по образцу `build_addon1.py`) + `tools/regen_addon_vc.py`. **26 файлов, `ALL CHECKS PASS`.**
  * **Найден и закрыт один настоящий конфликт сборки, тот самый типовой класс из раздела 8.** `tgr+vc done`, `ef+vc done` и `morg+vc done` втроём правят одни и те же 99 записей `common/buy_packages/wealth_1..99`: TGR — полным `REPLACE_OR_CREATE:` (числа не складываются, а домножаются, тело обязано быть полным), E&F и Morgenrote — `INJECT:`/`TRY_INJECT:` пары полей. Скопированные тремя файлами, они сортируются `zz_vc_ef_...` < `zz_vc_morg_...` < `zz_vc_tgr_...` — то есть тело TGR, которое не называет ни одного поля E&F или Morgenrote, грузилось бы последним и стирало бы оба чужих восстановления заново, ровно ту же болезнь, для лечения которой эти два компача и написаны против VC. `tools/regen_addon_vc.py` генерирует один файл `common/buy_packages/zzzz_addon_vc_buy_packages.txt` (тело TGR + поля E&F + поля Morgenrote в одном `goods`-подблоке), собирается заново при каждом прогоне `build_addon_vc.py`, отдельной команды не требуется. Коллизий имён полей между тремя источниками нет ни на одной из 99 записей (проверено программно, не выборочно).
  * **Покрытие:** 28 файлов по четырём компачам (20 TGR + 4 E&F + 3 Morgenrote + 1 KAI), минус 3 источника `buy_packages`, слитых в один, плюс сам мердж-файл = 26. Отклонение от побайтового покрытия — ровно эти 3 файла, задекларировано с объяснением, не тихо.
  * **VC.5 (PSC) и VC.6 (PBE) вклада не дают** — соответствующие папки компачей пусты (`noneed`), в сборку не входят.
  * **Внутренние дубли:** `COUNTRIES` (аддитивный контейнер, то же исключение, что у аддона HC) и `base_values` (TGR и E&F оба `INJECT:` в непересекающиеся поля) — оба задекларированы, реальных дублей нет.
  * **`scan_conflicts.py` против всех шести блоков основной пачки и против мегапака `no t&r`:** TGR 191, PSC 0, KAI 3, E&F 102, Morgenrote 96 (точечный скан `common/` — полный скан по всему моду не влезает в лимит вызова на этой машине, 8200+ файлов), PBE 0, мегапак 1 (`company_standard_oil` — намеренно: сборка обязана победить копию E&F внутри мегапака). Все пересечения объяснены, ничего нового сверх уже описанного в отчётах отдельных пар.
  * Локализация и товары не пострадали: 0 ключей локализации (ни один из четырёх компачей её не везёт), 0 файлов `common/goods` (потолок остаётся 74 из 128).
  * Открытые вопросы пар (`mobilization_option_chemical_weapons` у Morgenrote×VC, `anti_interest_groups`/`interest_group_government_weight` у KAI×VC) сборкой не закрыты и не должны — оба требуют проверки в игре, не файлового анализа.
  * Отчёт: `__addon/addon vc/conflicts_addon_vc_internal.md`.
- [x] **VC.9 README и metadata — закрыто 26.08.2026.** `tools/addon_vc_metadata.py`, `__addon/addon vc/README.md`. `id` у VC пустой (как у `hailcolumbia`) — не сослаться в `relationships`, назван только в `short_description` и `tested_with`, как и было решено. `relationships` перечисляют CMF, ETF, мегапак `no t&r`, TGR, E&F, Morgenrote, KAI — PSC и PBE не входят (для них компача нет, `noneed`), тот же принцип, что у `addon1_metadata.py`. Версия аддона `1.13.11-1`, `supported_game_version` `1.13.*`. В README отдельной строкой: **аддон грузится после Victorian Century; сам VC, если используется, — до Hail, Columbia!** (эта пара, VC × HC+GoB+MoH, закрыта отдельно — см. HC.7).
- [x] **VC.10 Письмо автору VC.** **Найдено 25.08.2026:** `common/ideologies/joi_ig_ideologies.txt`, строка 1686, идеология `ideology_united_states_constitution` — `law_theocracy = approvestrongly_disapprove`, слипшиеся два значения. Движок такого слова не знает, позиция по этому закону просто не существует, в логе ничего. Плюс: 15 из 24 престижных товаров без слота; 16 сухопутных `map_data/state_regions/*` голыми копиями по ванильным путям (при обновлении игры откатывают карту); `manifest_destiny.txt` и `00_canada_australia.txt` — глушилки по 11 байт, гасят и чужие моды по этому пути.

### Не делать в аддоне-VC

VC × PSC и VC × PBE — VC.5 и VC.6 подтвердили прежний `noneed` 26.08.2026, аддон-VC совместим с урезанными вариантами мегапака без этих модов как есть.

## аддон-HC (HC + GoB + MoH)

Собран 24–25.08.2026. После удаления T&R из шести пар остаётся четыре: TGR, Morgenrote, VC и KAI.

### Сделано

- [x] **HC.1 × TGR — группы интересов.** `zz_hct_ig_landowners.txt` и `zz_hct_ig_rural_folk.txt` — тела HC с трёхсторонним мерджем правок TGR против ванили. В `ig_rural_folk` заодно вычищена до-1.13 копия ванили из `moh_rural_folk.txt` — оставлены только идеология лидера Гоминьдана и переименование Нунмин. Два мерджа по ~780 строк.
- [x] **HC.2 × TGR — три перекрытия по пути.** `chi - china.txt` — тело MoH + `company_ong_lung_sheng_tea_company` от TGR; `tur - ottoman empire.txt` — тело GoB + `company_imperial_arsenal`. `manifest_destiny.txt` осознанно не трогаем: HC везёт файл с полностью закомментированным решением и заменяет ванильное Manifest Destiny журнальной цепочкой на 1100 строк.
- [x] **HC.3 × TGR — `ai_strategy_default`.** `zz_hctr_ai_strategy_default.txt` — трёхсторонний мердж KAI и MoH против ванили как общей базы, плюс `zz_hctr_tgr_default_strategy.txt` с инжектами TGR. `zzz_hctr_tr_default_strategy.txt` (T&R) удалён 25.08.2026 задачей T&R.2. Блок TGR с 25.08.2026 переиздаётся целиком: вернулись 10 → 500 на три ванильных института, которые раньше отдавались T&R.
- [x] **HC.4a × TGR × VC — дописан `zz_hct_jacksonian_democrat.txt`. Закрыто 26.08.2026.** Не хватало позиций джексоновского демократа по десяти новым законам VC (аддон-VC их инжектит в ваниль, но HC переобъявляет идеологию полным телом и грузится позже, поэтому инжект не доживает).
  * **Сделано генератором, не руками.** `_jacksonian(p)` в `tools/regen_addon1.py` теперь читает третий источник — `_vc/tgr+vc done/common/ideologies/zz_vc_tgr_stances_on_vc_laws.txt` (сам этот файл сгенерирован `regen_vc_tgr.py` из `vc_tgr_ideology_grid.xlsx`, лист «Обратно», строка `ideology_jacksonian_democrat`, значения сверены и совпадают). Из восьми затронутых VC групп законов две (`lawgroup_bureaucracy`, `lawgroup_distribution_of_power`) у HC уже существуют — новый закон VC (`law_imperial_examination`, `law_sublime_porte_absolutism`) вписан внутрь этих блоков одной строкой; остальные шесть групп (`taxation`, `education_system`, `economic_system`, `trade_policy`, `citizenship`, `policing`) у HC отсутствуют вовсе — добавлены целиком, тем же приёмом, что раньше два блока TGR.
  * Три новых `need()`-assert'а фиксируют список из восьми групп VC и то, что ровно две из них совпадают с уже существующими блоками HC — если VC или tgr+vc-компач это когда-нибудь изменят, генератор остановится с читаемым сообщением вместо тихой порчи файла.
  * `python tools/regen_addon1.py --root ... --out ... --check` прошёл (только этот файл `differs`, всё остальное не тронуто), пересборка и `python tools/build_addon1.py --repo ...` — `ALL CHECKS PASS`, `git diff --stat` подтверждает, что изменились только `zz_hct_jacksonian_democrat.txt` и `regen_addon1.py`.
  * README и `.metadata/metadata.json` `hc+tgr done` обновлены: Victorian Century назван как зависимость (id у VC пустой, как у `hailcolumbia`, — в `tested_with`, не в `relationships`, тот же приём, что уже был для HC).
- [x] **HC.4 × TGR — мелочь, почти всё закрыто.** `ideology_jacksonian_democrat` оказался не аддитивным: HC переписывает идеологию без позиций TGR по `lawgroup_election_system` и `lawgroup_legislative_process`, они возвращены в `zz_hct_jacksonian_democrat.txt`. Остальное (`law_ethnostate`, `law_freedom_of_conscience`, `law_protectionism`, `law_canton_system`, `law_theocracy`, `ideology_communist`, три `ig_*`) — везде `INJECT:` в под-блоки, которых никто больше не называет.
- [x] **HC.5 × Morgenrote — передифф 21 файла.** Расхождения в 20 копиях — только гены портретов, намеренные варианты GoB и HC. Реально смерджено четыре записи: `common/achievement_groups.txt`, `gaudi_capital_expansion_level_4_modifier` (заглушка GoB гасила настоящий модификатор), `revive_olympic_games_decision`, два `character_templates` (Крайслер и Твен).
- [x] **HC.6 Сборка и метаданные.** `tools/build_addon1.py` и `tools/addon1_metadata.py`. `ALL CHECKS PASS`. `hailcolumbia` в `relationships` не сослать — пустой `id`. **Пересобрано 25.08.2026 без T&R: 11 файлов вместо 15, версия `1.13.11-3`, зависимость мегапака — `no t&r` (`3640735868`).**

### Осталось

- [x] **HC.7 × VC — 39 ключей и 8 путей. Закрыто 26.08.2026.** Ложится ровно на уже сделанные файлы, поэтому дописывать, а не переписывать (VC грузится раньше HC, тела HC остаются базой):
  * пути: `manifest_destiny.txt` [VC vs HC], `chi - china.txt` [VC vs MoH], `tur - ottoman empire.txt` [VC vs GoB], `usa - usa.txt` [VC vs HC], четыре `common/dna_data/ecchi_*` (Ли Хунчжан, Чжан Чжидун, Цзо Цзунтан, Полк);
  * 16 `character_templates` — все американские генералы и адмиралы плюс Линкольн [VC vs HC] и `chi_daoguang_template` [VC vs MoH];
  * пять `ig_*`: `armed_forces`, `intelligentsia`, `petty_bourgeoisie` [VC vs MoH], `landowners` [VC vs HC], `rural_folk` [VC vs HC и MoH] — сюда же вклад VC в уже смерджённые `zz_hct_ig_*`;
  * `common/dynamic_country_names` AHU/CHI/MCH/XIN, `flag_definitions` CHI, `coat_of_arms` CHI_minguo, `je_autocracy`, два opium-кнопки, `state_trait_columbia_river`, `NAI`;
  * три штата в `map_data/state_regions`: `STATE_GANSU`, `STATE_GUANGDONG`, `STATE_HINGGAN` — при порядке «VC раньше» выигрывает MoH, проверить и записать как требование к порядку;
  * **добавлено 25.08.2026 по итогам VC.1.** HC везёт `common/interest_groups/00_landowners.txt` и `00_rural_folk.txt` голыми телами по ванильным путям и грузится ПОСЛЕ аддона-VC. Значит вклад аддона-VC в `ig_landowners` и `ig_rural_folk` до игры не доживает: базой становятся `zz_hct_ig_landowners.txt` / `zz_hct_ig_rural_folk.txt` из аддона-HC. Правки TGR там уже есть (HC.1), правок VC нет — дописывать надо именно в эти два файла, а не заводить новые. С MoH мягче: он инжектит, а не переписывает, поэтому тела `ig_armed_forces`, `ig_intelligentsia`, `ig_petty_bourgeoisie` из аддона-VC выживают и инжекты MoH ложатся сверху.
  * `zz_vc_tgr_interest_groups.txt` из `_vc/tgr+vc done` — готовый источник обеих сторон для этого дописывания.
  * **Разбор пары выдан 26.08.2026** — `_HC+GoB+MoH/hc+vc wip/conflicts_hcgobmoh_vs_vc_report.md` (аналог `conflicts_tgr_vs_vc_report.md`, машинный прогон `pair_matrix.py --pair "HC+GoB+MoH,VC"` + построчная сверка). Компач ещё не написан, это только табличка. Итог по 39 ключам + 8 путям:
    * **8 — не конфликт**, подтверждено построчно (три `ig_*` [MoH только инжектит в под-блоки, которых VC не занимает], `je_autocracy`, `state_trait_columbia_river`, `NAI`, `CHI_minguo` [тела побайтово идентичны], `manifest_destiny.txt` [оба автора и так гасят decision]) — **отменяет прежнюю запись выше**, где эти позиции числились как требующие работы;
    * **1 — авто-решается порядком** (три штата `map_data/state_regions`, MoH побеждает без компача, только строка в README);
    * **1 — мердж на одну строку** (`ecchi_usa_polk.txt`, VC несёт лишний ген `coats`, тот же класс, что Гитлер в VC.3);
    * **5 — механический мердж списков** (`dynamic_country_names` AHU/CHI/MCH/XIN, `flag_definitions CHI` — MoH `REPLACE:` весь контейнер, записи VC внутри по имени не пересекаются, дописать в конец тела MoH);
    * **16 `character_templates`** — не два независимых замысла, как казалось: 11 из 16 есть в вании, мерджить `vic3merge3.py` по образцу VC.1 (техническая часть автоматом, редакторская — какие `traits` — открыта); 5 без ванильного предка (`usa_admiral_porter/sampson/sigsbee`, `usa_general_bell/miles`) — чистое редакторское решение;
    * **6 — крупный контентный мердж, не начат**: `ig_landowners`, `ig_rural_folk` (поверх уже готовых `zz_hct_ig_*`), `chi - china.txt`, `tur - ottoman empire.txt` (списки `activate_law` у VC и у MoH+TGR не совпадают дословно — требуется решение, не только дописывание), `usa - usa.txt`, два opium-кнопки.
  * **Компач написан 26.08.2026** — `tools/regen_hc_vc.py`, папка `_HC+GoB+MoH/hc+vc wip`. Закрыты первые 5 из 6 позиций выше:
    * **15 из 16 `character_templates`** (все американские, без `chi_daoguang_template` — тот отдельно, VC vs MoH, не в этом заходе): решение по трейтам взято из заполненной `hc_vc_character_traits.xlsx` (10 строк — чистый перенос VC, где HC не отличался от вании или трейтов не было вовсе; 5 строк — редакторские решения: Скотт и Джесап оставлены на VC-переработке, Грант — VC (`alcoholic, direct` честнее прилизанного HC), Линкольн — VC (`inspirational_orator` — его ключевой трейт), Белл — **HC** (документированные зверства при Батангасской реконцентрации, у VC — обобщённый `inspirational_orator` без опоры на биографию), Майлз — **свой микс** (`ambitious` от HC — известное самопиар, `experienced_offensive_planner/brave/popular_commander` от VC — Медаль Почёта и настоящая популярность у войск). Генератор проверяет `need()`, что все 15 ключей из таблицы совпадают ровно с ожидаемым списком.
    * **`ecchi_usa_polk.txt`** — тело HC + один лишний ген `coats` от VC (тот же класс, что Гитлер в VC.3).
    * **`dynamic_country_names`** AHU/CHI/MCH/XIN — список MoH + список VC дописан в конец (без REPLACE вложенных записей).
    * **`flag_definitions` CHI** — 19 записей MoH + одна новая от VC (`CHI_dictatorship`); `CHI_minguo`/`CHI_republic` совпадают по `coa`, но реально разный контент — решение в пользу MoH (её история/журнал-цепочка реально управляет политикой Китая в этой сборке, см. ещё не сделанный `chi - china.txt`).
    * **Две opium-кнопки** (`ban_opium_button`, `unban_opium_button`) — тело MoH (китайская Opium War ветка в `possible`, которой нет у VC) + гейт по журналу VC `common_opium_war` довеском через OR, `has_law_or_variant` вместо протухшего `has_law` (тот же фикс, что в HC.2 для `moh_rural_folk.txt`), эскейп `allow_ban_opium_timer` от VC рядом с `lost_opium_wars` у MoH.
    * Все пять файлов прошли `--check`/полный прогон/повторный `--check` (идемпотентно), баланс скобок проверен построчно. README папки обновлён.
    * **Осталось 5**: `ig_landowners`, `ig_rural_folk` (поверх `zz_hct_ig_*`), `chi - china.txt`, `tur - ottoman empire.txt` (решение по `activate_law`, возможно ещё одна табличка), `usa - usa.txt` (даже не сравнивался построчно), плюс отдельно `chi_daoguang_template`.
  * **`ig_landowners`/`ig_rural_folk` закрыты 26.08.2026** — VC-слой домерджен в `tools/regen_addon1.py` поверх уже собранного тела HC+TGR(+MoH), по под-блокам (`on_enable`, `pop_potential`, `pop_weight`), трёхсторонне против вании через `git merge-file` (та же техника, что и раньше в этом файле), конфликт-резолвер `_ig_resolve` уже был готов и переиспользован. В `ig_rural_folk.on_enable` был один настоящий конфликт (VC вставляет три новых `if`-блока смены идеологии там же, где у HC/MoH пусто) — резолвер разрешил его автоматически (`ours` пуст → берём `theirs`). Заодно найден и исправлен попутный баг: `scope:interest_group ?= {` → `= {` — TGR и VC независимо делают ровно этот даунгрейд в одном и том же блоке `LEADER_POPULARITY`; баннер прежней версии файла заявлял, что даунгрейд TGR «не переносится», но фактически одно вхождение (`LEADER_POPULARITY`) всё же протекало непроверенным. Теперь везде восстановлена безопасная форма `?=` (4 вхождения в каждом файле, проверено `need()`). `--check`/полный прогон/повторный `--check` — идемпотентно, баланс скобок (`brace_balance()`, с учётом комментариев) — 0 в обоих файлах. README `hc+tgr done` дополнен.
  * **`chi - china.txt` разобран 26.08.2026 -- решение: файл MoH без изменений.** activate_law у VC (12 законов) на первый взгляд не совпадает с MoH+TGR (12 законов), но при разборе выяснилось, что расхождение не техническое, а структурное: MoH меняет `law_serfdom` на `law_tenant_farmers` и тут же вешает свою поправку `amendment_chinese_traditional_land_system` именно на неё (несущая часть цепочки MoH); MoH и VC независимо ставят РАЗНЫЕ законы под бюрократию с похожими именами -- `law_imperial_examinations` (MoH, множественное число, свой закон) против `law_imperial_examination` (VC, единственное число, свой закон) -- журнал MoH (`je_keju`) написан под свой закон; VC добавляет `law_classical_learning`, но его `requires_law_or` (serfdom ИЛИ imperial_examination[VC]) не выполняется при сохранении выборов MoH -- добавлять нельзя, стартовый закон был бы с невыполненным условием. Остальное содержимое VC-файла (8 своих journal_entry, 4 поправки в других группах законов, set_variable/add_modifier) -- параллельная китайская политсистема VC, тот же класс, что уже решён для `CHI_minguo`/`CHI_republic` в flag_definitions -- не мерджится. Три need()-проверки добавлены в `regen_addon1.py` (`_check_china_activate_law_vs_vc`), ловят дрейф списков и предпосылки VC-закона. Файл `zz_hct...` не создавался -- решение зафиксировано только в баннере существующего `common/history/countries/chi - china.txt`.
  * **`tur - ottoman empire.txt` разобран и дописан 26.08.2026.** В отличие от Китая, VC здесь ближе к вании, чем GoB (сохраняет `law_autocracy`/`law_slave_trade`, не трогает `migration_controls`). Проверено программно и добавлено: `law_madrasa` (единственный новый закон VC, группа `lawgroup_education_system`; свой `requires_law_or` -- `law_millet_system` -- уже активен во всех версиях файла; у GoB уже есть поправка `amendment_gbbf_elifba` на эту же группу законов, но явного закона она не получала -- теперь есть на что вешаться) и две поправки VC (`amendment_salt_monopoly` на `lawgroup_taxation`, `amendment_kanunname_law` на `lawgroup_governance_principles` -- группы, которые GoB вообще не трогает, целевые законы уже активны в любой версии файла). Оставлены за бортом: 3 доп. стартовые технологии VC и собственная джорнал-цепочка VC (Peter the Great of Turkey, Sublime Porte и ещё 5 записей) -- параллельная османская политсистема, тот же класс решения, что и для Китая. `tools/regen_addon1.py`: `_history_merge()` получил необязательный параметр `pre` (хук трансформации тела победителя до вставки компании TGR), `_splice_vc_into_ottoman()` вставляет два фрагмента по якорям, `_check_ottoman_laws_vs_vc()` проверяет оба списка `activate_law` и обе предпосылки поимённо. `--check`/полный прогон/повторный `--check` -- идемпотентно, `brace_balance()` -- 0.
  * **`usa - usa.txt` разобран и дописан 26.08.2026.** Флагманский файл HC (весь замысел "United States Flavor Pack"). Из 15 явно активируемых групп законов 10 совпадают, 5 — настоящая конкуренция, и в трёх из пяти побеждает СВОЙ закон HC: `trade_policy` (`law_usfp_american_system` против `law_protectionism`), `economic_system` (`law_agrarianism` против `law_interventionism`), `free_speech` (`law_right_of_assembly` против `law_protected_speech`), `taxation` (свой `law_usfp_devolved_taxation` против `law_per_capita_based_taxation`), `church_and_state` (свой `law_usfp_nominal_separation` против `law_total_separation`) — везде оставлен выбор HC, тот же принцип, что и во всём аддоне: авторский флейвор остаётся. Отдельно оставлен нетронутым `start_enactment = law_universal_suffrage` HC (страна стартует в процессе перехода). Добавлено из VC: 12 из 14 поправок (`active_law:lawgroup_X ?= { add_amendment = ... }`, VC-шный "Билль о правах") — 2 из 14 оказались побайтово идентичны тому, что уже есть у HC (`amendment_american_second_amendment` на `governance_principles`, `amendment_tradition_of_free_elections` на `distribution_of_power` — оба мода явно берут из общего источника). Остальные 12 добавлены безопасно: поправка цепляется к ЛЮБОМУ активному закону в группе, а не к конкретному, так что победа HC в законе не мешает; для трёх групп (`governance_principles`, `bureaucracy`, `internal_security`), где НИ ОДИН из файлов explicit `activate_law` не ставит, сработает тот же implicit-default механизм, на который уже опирается собственная поправка HC в `governance_principles`. НЕ смерджено: конкурирующие законы VC (см. выше), tariff VC на `g:fabric` (завязан на выбор VC в trade_policy), и вся параллельная политсистема VC для США — свои переменные "real manifest destiny", активный `je_texas_usa` (у HC своя `manifest_destiny.txt` уже гасит ванильный decision собственной цепочкой на 1100 строк — второй Manifest Destiny от VC дал бы дубль), свой модификатор missouri_compromise (у HC уже есть `usfp_missouri_compromise_decaying`), `us_second_bank`, `failed_assassination_on_aj_president`, 6 своих journal_entry. `company_william_cramp` — побайтово идентична в обоих файлах, мерджить нечего. TGR этот файл вообще не трогает. `tools/regen_addon1.py`: новые `_extract_lawgroup_amendments()`, `_check_usa_laws_vs_vc()` (списки законов + счётчики дублей/новых поправок поимённо), `_splice_vc_amendments_into_usa()`, `_history_usa()` — зарегистрирован в `build_tgr`. `--check`/полный прогон/повторный `--check` — идемпотентно, `brace_balance()` — 0; отдельно проверено, что конкурирующие законы VC не просочились в код (только в баннер-прозу).
  * **`chi_daoguang_template` разобран 26.08.2026 -- решение: ничего делать не надо.** Ваниль объявляет шаблон; VC `REPLACE_OR_CREATE:`-ит его рано (добавляет `basic_political_operator` к ванильным `traits`, больше ничего не трогает); MoH `INJECT:`-ит `on_created` поздно (эффект-блок, которого в теле VC вообще нет). Порядок загрузки сам кладёт инжект MoH поверх переработки VC -- ни путь, ни ключ реально не пересекаются. Технической правки не потребовалось.
  * **Пара закрыта 26.08.2026** — все 6 пунктов из разбора закрыты (5 «крупный мердж» выше + `chi_daoguang_template`). Папка `_HC+GoB+MoH/hc+vc wip` переименована в `hc+vc done`, README переписан (включая честную правку по DNA Ли Хунчжана/Чжан Чжидуна/Цзо Цзунтана — изначально предполагалось, что это тот же класс, что Полк, при проверке `diff` оказалось, что нет: тела MoH (115-118 строк) и VC (649-661) независимо авторские, решение — оставить MoH, её собственная китайская политсистема). `tools/addon1_metadata.py` — новая запись `hc+vc done`, зависимости `cmf, gob, moh, tgr` (TGR — потому что 5 позиций живут в `hc+tgr done`), `vc=True`. `tools/build_addon1.py` — `hc+vc done` добавлена в `PAIRS`, плюс `.xlsx` добавлен в `SKIP_SUFFIX` (решение по трейтам — не файл для игры). Полный прогон: `addon1_metadata.py` — 5/5 ok; `build_addon1.py --check` (до реальной сборки) корректно поймал устаревший аддон (6 файлов `hc+vc done` отсутствовали, 4 файла `hc+tgr done` устарели); реальный прогон — скопировано 17 файлов, `ALL CHECKS PASS`; повторный `--check` — идемпотентно, тот же результат. README `hc+tgr done` дополнен разбором всех трёх history-файлов (China/Ottoman/USA) вместо одних interest groups.
  * **«Играть без VC» доделано 26.08.2026** — по просьбе пользователя. Шесть файлов существуют только из-за VC (пять из `hc+vc done` + `usa - usa.txt` из `hc+tgr done`, TGR его вообще не трогает) — теперь несут тег `=== VC-ONLY FILE ===` первой строкой, просто удалить. Три файла делят REPLACE:-путь с TGR/GoB (`zz_hct_ig_landowners.txt`, `zz_hct_ig_rural_folk.txt`, `tur - ottoman empire.txt`) — тег `=== CARRIES A VICTORIAN CENTURY LAYER ===` плюс инертный `.off`-двойник (без слоя VC) рядом, тег на нём — `=== INACTIVE -- NO-VC VARIANT (.off) ===`; `regen_addon1.py` строит «безголовое» (pre-VC) тело один раз и накладывает VC-слой на него же, так что `.off` и финальная версия не могут разойтись — заодно и scope-safety фикс (независимый от VC баг TGR) применён к обеим. `chi - china.txt` не тронут: VC туда ничего не добавляет, метить нечего. `regen_hc_vc.py` заодно починен — пути `OUT`/`GRID` ещё указывали на переименованную `hc+vc wip`. Написан README аддона `__addon/addon1 hc+gob+moh/README.md` (его не было вообще — утеряна при переименовании из старой `addon hc+gob+moh`) с разделом «Playing without Victorian Century» (таблица + BBCode для Steam) и обновлённым «Which file belongs to which pair». `hc+tgr done/README.md` и `hc+vc done/README.md` получили по абзацу с перекрёстной ссылкой на этот раздел. `build_addon1.py`: `.xlsx` добавлен в `SKIP_SUFFIX` ранее не мешал бы, но `.off`-файлы сборке специально не мешают — они не входят в проверку дублей ключей (фильтр по `.txt`), что как раз и нужно: иначе `.off` и `.txt` дали бы ложный дубль `ig_landowners`/`ig_rural_folk`/`tur - ottoman empire` на каждую пару. Полный прогон: `regen_hc_vc.py` и `regen_addon1.py` — `--check`/реальный/повторный `--check`, 0 расхождений у обоих; `build_addon1.py` — 20 файлов (было 17), `ALL CHECKS PASS`, идемпотентно. Общий приём вынесен в «Правила работы» (раздел 8) как стандарт для всех будущих аддонов — см. решение №9 выше.
  * **VC вынесен из аддона в отдельный компач, 26.08.2026** — по просьбе пользователя, решение №10 выше. Пять шагов:
    1. `regen_addon1.py` очищен от VC целиком: `_ig_landowners`/`_ig_rural_folk` разбиты на `_ig_landowners_body`/`_ig_rural_folk_body` (голое тело HC+TGR(+MoH), без VC) плюс тонкая обёртка с баннером; `_jacksonian` так же на `_jacksonian_tgr_body` плюс обёртку; `_history_china`/`_history_ottoman` вернулись к версиям без VC-проверок; удалены `_check_china_activate_law_vs_vc`, `_check_ottoman_laws_vs_vc`, `_splice_vc_into_ottoman`, `_extract_lawgroup_amendments`, `_check_usa_laws_vs_vc`, `_splice_vc_amendments_into_usa`, `_history_usa`, `_merge_vc_into_ig`, все `vc_tag_*` хелперы; `class P` лишился `self.vc`; `build_tgr()` больше не пишет `usa - usa.txt` и `.off`-файлы; `_safe_scope()` упрощена — без параметра `expect` (нечего больше защищать от дрейфа по VC).
    2. `regen_hc_vc.py` расширен с 5 файлов до 10: `import regen_addon1 as ra1`, пять новых `build_*` (`ig_landowners`, `ig_rural_folk`, `jacksonian_democrat`, `tur - ottoman empire.txt`, `usa - usa.txt`) импортируют голые тела из `ra1` и накладывают VC-слой поверх — та же гарантия «не могут разойтись», что раньше давал `.off`, но без дублирования файлов внутри аддона. `usa - usa.txt` (TGR его никогда не трогал) и проверка `chi - china.txt` (`check_china_no_op()`, файла не производит) переехали сюда почти без изменений.
    3. `build_addon1.py`: `hc+vc done` убран из `PAIRS`. Заодно найдены и убраны в `_to_delete/` файлы, которые генераторы больше не производят, но и не подчищают сами: три `.off`-файла и сам `usa - usa.txt` в `hc+tgr done`.
    4. `addon1_metadata.py` переписан: `hc+tgr done` и `addon1` лишились `vc=True` и упоминаний VC; `hc+vc done` получил новую зависимость на сам `addon1` (id `asm.addon1.hcgobmoh`, та же схема ссылки по строковому id, что у CMF/ETF) и новое описание («после Addon 1 и после VC, рассчитан на пару с отдельным `Addon: Victorian Century x MegaComPatch`»). Заодно обнаружилось и починено: у папки `addon1 hc+gob+moh` не было вообще `.metadata/metadata.json` — потерялась при переименовании из `addon hc+gob+moh`, как и README.
    5. Все три README переписаны: `hc+tgr done` — VC убран полностью, добавлен абзац «Victorian Century» со ссылкой на новый компач; `hc+vc done` — полный рецепт как самостоятельного компача (все 10 файлов, новый порядок загрузки — после Addon 1, раздел «Checked and deliberately left alone» пополнен разбором `chi - china.txt`); `addon1 hc+gob+moh/README.md` написан заново без раздела «Playing without Victorian Century» (аддон VC больше не трогает вовсе) и с разделом «Victorian Century», объясняющим, что ставить отдельно.
    Полный прогон: `regen_addon1.py` — `--check`/реальный/`--check`, 0 расхождений; `regen_hc_vc.py` — то же, 10 файлов, 0 расхождений; `build_addon1.py` — 11 файлов чистого HC+GoB+MoH×мегапак (пик был 20 с VC внутри и `.off`), `ALL CHECKS PASS`, идемпотентно. Тегов `=== VC-ONLY FILE ===`/`=== CARRIES A VICTORIAN CENTURY LAYER ===` и `.off`-файлов для этой пары в аддоне больше не осталось (проверено `grep`).
- [x] **HC.8 × KAI — 2 ключа, `noneed` подтверждён 26.08.2026.** `pair_matrix.py --pair "HC+GoB+MoH,KAI"` пересчитан без T&R: по-прежнему ровно два ключа.
  * `ai_strategy_default` — уже закрыт файлом HC.3 (`zz_hctr_ai_strategy_default.txt`, трёхсторонний мердж KAI × MoH).
  * `NAI` — сверено построчно, не только «по логике». HC: один файл `usfp_defines.txt`, один ключ `NUM_GROWING_COLONIES_MAX`. KAI: `kai_ai.txt`, 31 ключ (`REFORM_GOVERNMENT_*`, `MONEY_SPENDING_*`, `PRODUCTION_METHOD_*`, `MILITARY_UNITS_PER_FORMATION_ARMY` и другие). Пересечений — ноль. Обоснование уже стояло в `hc+kai done/README.md` («Checked and deliberately left alone») с прошлой сессии — этим прогоном подтверждено заново на актуальных файлах.
- [x] **HC.9 Проверка в игре.** `role` рядом с `executive_usage` / `agitator_usage` у Крайслера и Твена — конструкции нет ни в ванили, ни у обоих авторов. Смотреть, не спавнятся ли персонажи дважды и не ругается ли `debug.log` на `PostValidate`. Единственное, что нельзя закрыть с диска.
- [x] **HC.10 Письма авторам.** GoB — заглушка `gaudi_capital_expansion_level_4_modifier` гасит настоящий модификатор; MoH — `moh_rural_folk.txt` инжектит до-1.13 тело ванили и в `achievement_groups.txt` нет `achievement_exactly_100`; TGR — `TGR_ADJUSTMENTS_oregon.txt` копия старой ванили с `months = normal_modifier_time` (152 года вместо пяти).
- [x] **HC.11 Публикация.** BBCode готов в README, id мастерской нет.

### Не делать в аддоне-HC

Пары с E&F+hotfix, PBE и PSC — `noneed`, ноль пересечений. Внутри блока чисто, кроме требования «MoH после HC».
